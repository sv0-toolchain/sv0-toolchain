# sv0 toolchain progress rollup (meta-repository)

This file is the **cross-repo rollup** and **session run log** for the **sv0-toolchain** parent checkout. It does **not** replace submodule detail.

## Authority (hybrid 2 + 3)

| Layer | Location | Owns |
|--------|-----------|------|
| **Submodule** | `sv0doc/PROGRESS.md`, `sv0c/PROGRESS.md`, `sv0vm/PROGRESS.md`, `sv0-mcp/PROGRESS.md` | Item checklists, definitions of **done** / **total**, and **local %** for that repository. |
| **Meta** | this file (`task/sv0-toolchain-progress.md`) | **Summary table** (mirrors submodule `%`), **orchestration** rows for meta-only work, and **run log** entries for every agent session that claims toolchain progress. |

**Update order:** change the **submodule** `PROGRESS.md` first when work landed there, then update this rollup in the **same integration** (same meta-repo commit as submodule pointer bumps when applicable).

## Summary (mirror submodule files — edit only after updating sources)

| Area | Source | Done | Total | % |
|------|--------|------|-------|---|
| Specification (`sv0doc`) | `sv0doc/PROGRESS.md` | 4 | 4 | 100% |
| Compiler (`sv0c`) | `sv0c/PROGRESS.md` | 6 | 6 | 100% |
| VM (`sv0vm`) | `sv0vm/PROGRESS.md` | 3 | 3 | 100% |
| MCP / graph (`sv0-mcp`) | `sv0-mcp/PROGRESS.md` | 3 | 3 | 100% |
| Meta orchestration | ## Meta-repository checklist below | 6 | 6 | 100% |

**Overall %:** 100% (all five process-checklist areas complete). **Note:** these are **process/harness/documentation** gates for milestones 0–2 and MCP-0 — **not** M3 (self-hosting) completion, which is tracked in `task/sv0-toolchain-milestone-3-self-host.Rmd`.

## Meta-repository checklist (orchestration)

Countable items for work that stays in the **parent repo** (`task/`, `scripts/`, `.cursor/`, `Makefile`, …). Adjust **Total** when the plan changes; document rationale in the run log.

| ID | Item | Done |
|----|------|------|
| META-1 | Progress ledger files exist and slash commands reference them | 1 |
| META-2 | Roadmap index + verify script path remain consistent (`task/sv0-toolchain-roadmap-full.Rmd`) | 1 |
| META-3 | Milestone orientation: `task/milestone-orientation.json` + `./scripts/sv0 milestone-orient` + rule `37` + `AGENTS.md` | 1 |
| META-4 | `scripts/verify_milestone_orientation.py` wired into `./scripts/sv0 test-guards` + CI smoke step | 1 |
| META-5 | `scripts/verify_workspace_milestone_table.py` (JSON ↔ workspace table) + explicit `./scripts/sv0 test` step in GitHub Actions | 1 |
| META-6 | Agent workflow docs: routine **push** + **GitHub Actions** verification (`gh run watch`) before declaring integration sessions complete — `40-validation-and-proof.mdc`, slash commands **Phase 4**, `AGENTS.md` | 1 |

Set **Done** to `1` when true. Expand the table as new cross-cutting gates appear.

## Run log (newest first)

| ISO date (timezone) | Charter / focus | Areas touched | Metric delta (before → after) | Validation commands (exact) |
|---------------------|-----------------|---------------|------------------------------|-----------------------------|
|| 2026-04-20 (America/Denver) | **`continue-development`** — **ExprIndex (tag 8)** → **`Call`** **`sv0_vec_get`** (**`fn_h = 0 - 10`**) + **`VVar`**; **`vm_codegen.sv0`** **`Call`**: negative **`fn_h`** → **`CALL_BUILTIN`** id **`0-fn_h`**; **`test_lower_expr_index_effect`**; **`doc/transliteration-plan.md`**. Stage0 **`lowering.c`**, **`vm_codegen.c`**, vm-parity **97** **`.sv0b`**. **Ship:** **sv0c** `525e9c2e9ac4a900e9eb7b1060ab0286af15e747`; **meta** submodule + **`README.md`** (chore bump commit on push). | **sv0c:** `lib/lowering.sv0`, `lib/vm_codegen.sv0`, `lib/golden/stage0/lowering.c`, `lib/golden/stage0/vm_codegen.c`, `test/vm-parity/golden/sml/*.sv0b`, `doc/transliteration-plan.md`, `PROGRESS.md`; **meta:** `sv0c` submodule, `README.md`, this rollup | Metrics unchanged (bootstrap 97, self-host 97, stage0 21, vm-parity 97) | `./scripts/sv0 vm-compile lib/lowering.sv0`; `./scripts/sv0 vm-compile lib/vm_codegen.sv0`; `./scripts/sv0 compile-run lib/lowering.sv0`; `./scripts/sv0 compile-run lib/vm_codegen.sv0`; `./scripts/sv0 self-host-capture-stage0 lib/lowering.sv0` + `lib/vm_codegen.sv0` + copy to `sv0c/lib/golden/stage0/`; `bash scripts/capture_vm_parity_goldens.sh`; `./scripts/sv0 test-guards`; `SV0_SKIP_SELF_HOST_COMPILER_DIFF=1 ./scripts/sv0 test` (~6m local). **GitHub Actions:** `gh run watch` (post-push). |
|| 2026-04-20 (America/Denver) | **`continue-development`** — **`lowering.sv0`** **ExprStruct (tag 24)** multi-seg path: **`DeclNamed`** typename tok = **last** `pp` segment when **`ed2`>1**; **`test_lower_expr_struct_qualified_path`**; **`doc/transliteration-plan.md`**. Stage0 **`lowering.c`**, vm-parity **`lowering.sv0b`**. **Ship:** **sv0c** `2b9bb0c194e547b4302d0546807256d67ce8dd29`, **meta** `ea7ed52`; **`README.md`** sv0c SHA. | **sv0c:** `lib/lowering.sv0`, `lib/golden/stage0/lowering.c`, `test/vm-parity/golden/sml/lowering.sv0b`, `doc/transliteration-plan.md`, `PROGRESS.md`; **meta:** `sv0c` submodule, `README.md`, this rollup | Metrics unchanged (bootstrap 97, self-host 97, stage0 21, vm-parity 97) | `./scripts/sv0 vm-compile lib/lowering.sv0`; `./scripts/sv0 compile-run lib/lowering.sv0`; `./scripts/sv0 self-host-capture-stage0 lib/lowering.sv0` + copy `build/self-host-compare/stage0/lowering.c` → `sv0c/lib/golden/stage0/lowering.c`; `bash scripts/capture_vm_parity_goldens.sh`; `./scripts/sv0 test-guards`; `SV0_SKIP_SELF_HOST_COMPILER_DIFF=1 ./scripts/sv0 test` (~5.6m local). **GitHub Actions:** `gh run watch 24649180588` → **success** (workflow **CI**, job **verify**, ~1m28s). |
|| 2026-04-18 (America/Denver) | **`continue-development`** — **`lowering.sv0`**: **ExprMethodCall (5)** / **ExprIndex (8)** / **ExprArray (26)** `lower_for_effect` → **`VUnit`**; three small tests (**`test_lower_expr_index_effect`**, **`_method_call_effect`**, **`_array_effect`**); **`test_lower_while`** fix (bogus **expr tag 5** as while cond → literal **tag 0** — avoids self-rcv infinite recursion). **`doc/transliteration-plan.md`**, **`sv0c/PROGRESS.md`**. Stage0 **`lowering.c`**, vm-parity **97** **`.sv0b`**. **Ship:** **sv0c** `c4bb452`, **meta** `9ed96d1`; **`README.md`** sv0c SHA. | **sv0c:** `lib/lowering.sv0`, `lib/golden/stage0/lowering.c`, `test/vm-parity/golden/sml/lowering.sv0b` (97), `doc/transliteration-plan.md`, `PROGRESS.md`; **meta:** `sv0c` submodule, `README.md`, this rollup | Metrics unchanged (bootstrap 97, self-host 97, stage0 21, vm-parity 97) | `./scripts/sv0 vm-compile lib/lowering.sv0`; `./scripts/sv0 compile-run lib/lowering.sv0`; `./scripts/sv0 self-host-capture-stage0 lib/lowering.sv0` + copy `build/self-host-compare/stage0/lowering.c` → `sv0c/lib/golden/stage0/lowering.c`; `bash scripts/capture_vm_parity_goldens.sh`; `./scripts/sv0 test-guards`; `SV0_SKIP_SELF_HOST_COMPILER_DIFF=1 ./scripts/sv0 test` (~6m local). **GitHub Actions:** `gh run watch 24634305792` → **success** (workflow **CI**, job **verify**, ~1m39s). |
|| 2026-04-19 (America/Denver) | **Ship** — **`git commit --no-gpg-sign`** **sv0c** (`c620e7b`) then **meta** (`d29ff26`); push **sv0c** then **meta**; **`README.md`** sv0c SHA; repair **`task/*.Rmd`** **`updated:`** YAML spacing before commit. | **sv0c** (published `main`); **meta** (published `main`) | Metrics unchanged | Pre-push: `./scripts/sv0 test-guards`; `SV0_SKIP_SELF_HOST_COMPILER_DIFF=1 ./scripts/sv0 test`. **GitHub Actions:** `gh run watch 24633165497` → **success** (workflow **CI**, job **verify**, ~1m37s). |
|| 2026-04-18 (America/Denver) | **`continue-development`** — **ExprTupleField (tag 7)** in **`lowering.sv0`** (**`VMember`**, handle **500+field_num**); **`codegen.sv0`** **`name_of`** **500..599** → **`fN`**; **`test_lower_expr_tuple_field`**; **`doc/transliteration-plan.md`** (stub **7** off **5/8/26** list); **`task/*.Rmd`** YAML **`updated:`** (**M2 prep**, **M3**, **roadmap**). | **sv0c:** `lib/lowering.sv0`, `lib/codegen.sv0`, `lib/golden/stage0/codegen.c`, `lib/golden/stage0/lowering.c`, `test/vm-parity/golden/sml/*.sv0b` (97), `doc/transliteration-plan.md`, `PROGRESS.md`; **meta:** `task/sv0-toolchain-milestone-2-prep.Rmd`, `task/sv0-toolchain-milestone-3-self-host.Rmd`, `task/sv0-toolchain-roadmap-full.Rmd`, this rollup | Metrics unchanged (bootstrap 97, self-host 97, stage0 21, vm-parity 97) | `./scripts/sv0 vm-compile lib/lowering.sv0`; `./scripts/sv0 vm-compile lib/codegen.sv0`; `./scripts/sv0 compile-run lib/lowering.sv0`; `./scripts/sv0 compile-run lib/codegen.sv0`; `./scripts/sv0 self-host-capture-stage0 lib/codegen.sv0` + `./scripts/sv0 self-host-capture-stage0 lib/lowering.sv0` + copy from `build/self-host-compare/stage0/` to `sv0c/lib/golden/stage0/`; `bash scripts/capture_vm_parity_goldens.sh`; `./scripts/sv0 test-guards`; `SV0_SKIP_SELF_HOST_COMPILER_DIFF=1 ./scripts/sv0 test` |
|| 2026-04-19 (America/Denver) | **`continue-development`** — **ExprRange (tag 21)** in **`lowering.sv0`** (`lower_for_effect` on optional lo/hi; **`VUnit`**); **`test_lower_expr_range`**; **`doc/transliteration-plan.md`**; **`task/*.Rmd`** YAML **`updated:`** fix (**M2 prep**, **M3**, **roadmap**). | **sv0c:** `lib/lowering.sv0`, `lib/golden/stage0/lowering.c`, `test/vm-parity/golden/sml/*.sv0b` (97), `doc/transliteration-plan.md`, `PROGRESS.md`; **meta:** `task/sv0-toolchain-milestone-2-prep.Rmd`, `task/sv0-toolchain-milestone-3-self-host.Rmd`, `task/sv0-toolchain-roadmap-full.Rmd`, this rollup | Metrics unchanged (bootstrap 97, self-host 97, stage0 21, vm-parity 97) | `./scripts/sv0 vm-compile lib/lowering.sv0`; `./scripts/sv0 compile-run lib/lowering.sv0`; `./scripts/sv0 self-host-capture-stage0 lib/lowering.sv0` + copy to golden; `bash scripts/capture_vm_parity_goldens.sh`; `./scripts/sv0 test-guards`; `SV0_SKIP_SELF_HOST_COMPILER_DIFF=1 ./scripts/sv0 test` |
|| 2026-04-18 (America/Denver) | **`continue-development`** — **ExprAssert (tag 23)** in **`lowering.sv0`** (`lower_for_effect` on condition → **`VUnit`**; **`test_lower_expr_assert`**); **`doc/transliteration-plan.md`**; **`task/*.Rmd`** YAML **`updated:`** spacing fixes (M2 prep, M3, roadmap). | **sv0c:** `lib/lowering.sv0`, `lib/golden/stage0/lowering.c`, `test/vm-parity/golden/sml/*.sv0b` (97 via capture script), `doc/transliteration-plan.md`, `PROGRESS.md`; **meta:** `task/sv0-toolchain-milestone-2-prep.Rmd`, `task/sv0-toolchain-milestone-3-self-host.Rmd`, `task/sv0-toolchain-roadmap-full.Rmd`, this rollup | Metrics unchanged (bootstrap 97, self-host 97, stage0 21, vm-parity 97) | `./scripts/sv0 vm-compile lib/lowering.sv0`; `./scripts/sv0 compile-run lib/lowering.sv0`; `./scripts/sv0 self-host-capture-stage0 lib/lowering.sv0` + copy to golden; `bash scripts/capture_vm_parity_goldens.sh`; `./scripts/sv0 test-guards`; `SV0_SKIP_SELF_HOST_COMPILER_DIFF=1 ./scripts/sv0 test` |
|| 2026-04-18 (America/Denver) | **`continue-development`** — **ExprStruct lowering** (`lowering.sv0` **`lit_sf_names`** threading, tag 24 DeclNamed+StoreField; **`lower()`** → **`body_sf`**); **parser `ExprStruct` `ed4`** = **`fc_before`**; **`test_lower_expr_struct`**; **`doc/transliteration-plan.md`**; **`task/*.Rmd`** YAML **`updated:`** fix (`updated:"` → valid spacing). | **sv0c:** `lib/lowering.sv0`, `lib/parser.sv0`, `lib/golden/stage0/lowering.c`, `lib/golden/stage0/parser.c`, `test/vm-parity/golden/sml/*.sv0b` (97), `doc/transliteration-plan.md`, `PROGRESS.md`; **meta:** `task/sv0-toolchain-milestone-3-self-host.Rmd`, `task/sv0-toolchain-milestone-2-prep.Rmd`, `task/sv0-toolchain-roadmap-full.Rmd`, this rollup | Metrics unchanged (bootstrap 97, self-host 97, stage0 21, vm-parity 97) | `./scripts/sv0 vm-compile lib/lowering.sv0`; `./scripts/sv0 compile-run lib/lowering.sv0`; `./scripts/sv0 test-guards`; `SV0_SKIP_SELF_HOST_COMPILER_DIFF=1 ./scripts/sv0 test` |
|| 2026-04-18 (America/Denver) | **`thoroughly-execute-document`** — **`task/sv0-toolchain-milestone-3-self-host.Rmd`** (**## validation (agents)**, throughput + SML inventory note, YAML **`updated:`**); **`task/sv0-toolchain-roadmap-full.Rmd`** refinement line; **M2 prep** YAML. | **meta:** `task/sv0-toolchain-milestone-3-self-host.Rmd`, `task/sv0-toolchain-roadmap-full.Rmd`, `task/sv0-toolchain-milestone-2-prep.Rmd`, this rollup | Metrics unchanged | `./scripts/sv0 milestone-orient show M3`; `./scripts/sv0 test-guards` |
|| 2026-04-18 (America/Denver) | **`thoroughly-execute-document`** — **`task/sv0-toolchain-roadmap-full.Rmd`** (verify script, M3 orient, snapshot **101→97**, M2 formatter doc pointer, push+CI graph-sync bullet, YAML fixes). | **meta:** `task/sv0-toolchain-roadmap-full.Rmd`, `task/sv0-toolchain-milestone-2-prep.Rmd`, `task/sv0-toolchain-milestone-3-self-host.Rmd`, this rollup; **sv0c:** `PROGRESS.md`, `test/vm-parity/README.md` | Doc-only count alignment (vm-parity **101→97** in prose); submodule metrics unchanged | `bash task/sv0-toolchain-roadmap-full/01-verify-roadmap-artifacts.sh`; `./scripts/sv0 milestone-orient show M3`; `./scripts/sv0 test-guards` |
|| 2026-04-18 (America/Denver) | **Formatter style — stakeholder C** — project-configurable layout; defer numeric defaults to first formatter PR; fix **`task/*.Rmd`** YAML **`updated:`** spacing. | **sv0c:** `doc/tooling-formatter-linter.md`; **meta:** `task/sv0-toolchain-milestone-2-prep.Rmd`, `task/sv0-toolchain-milestone-3-self-host.Rmd`, this rollup | Metrics unchanged | `./scripts/sv0 test-guards` |
|| 2026-04-18 (America/Denver) | **User request** — **Routine GitHub push + CI** in agent rules/commands; **formatter/linter** design doc expansion + milestone cross-links. | **meta:** `.cursor/rules/40-validation-and-proof.mdc` (Routine GitHub ship loop), `.cursor/rules/33-continue-development-workflow.mdc`, `.cursor/commands/continue-development.md`, `.cursor/commands/thoroughly-execute-document.md`, `AGENTS.md`, `task/sv0-toolchain-milestone-2-prep.Rmd`, `task/sv0-toolchain-milestone-3-self-host.Rmd`, this rollup; **sv0c:** `doc/tooling-formatter-linter.md` | Meta checklist **5 → 6** items (**META-6**); all **Done** = 1. Submodule metrics unchanged. | `./scripts/sv0 test-guards` |
|| 2026-04-18 (America/Denver) | **`continue-development`** — **Deep-function transliteration batch 70 — lowering.sv0 ExprFor real implementation.** `lowering.sv0`: `lower_expr_to_value` ExprFor (tag 13) now properly desugars to WhileLoop with ctr_fresh temps, range lo/hi lowering, and Assign+increment pattern (replaces VUnit stub). 1 new test group (test_lower_for_loop). lower_expr_to_value now handles 23 of ~27 AST forms. | **sv0c:** `lib/lowering.sv0` (+1 real case replacing stub, +1 test), `lib/golden/stage0/lowering.c` (regen), `test/vm-parity/golden/sml/lowering.sv0b` (regen), `doc/transliteration-plan.md`, `PROGRESS.md`; **meta:** `README.md` (sv0c SHA), this rollup | Metrics unchanged: bootstrap 97, self-host 97, stage0 21, vm-parity 97. **Qualitative:** lower_expr_to_value now handles 23 of ~27 AST forms — remaining ~4: ExprStruct (needs sf_names threading), ExprCall builtins/2-seg ctor, ExprMatch, ExprTry | `./scripts/sv0 compile-run lib/lowering.sv0` (exit 0), `./scripts/sv0 vm-compile lib/lowering.sv0` (OK), `./scripts/sv0 test-guards` (all green), `SV0_SKIP_SELF_HOST_COMPILER_DIFF=1 ./scripts/sv0 test` (exit 0) |
|| 2026-04-18 (America/Denver) | **`continue-development`** — **Deep-function transliteration batch 69 — lowering.sv0 top-level program lowering.** `lowering.sv0`: `lower` (SML `Lowering.lower` — orchestrator: builds struct/enum data via build_struct_order + build_enum_variants_data, collects C typedefs via collect_typedefs_str, iterates ItemFn items extracting label handles). Body instruction lowering deferred — needs per-function expression arenas from parser. 1 new test group. **Also:** added `.cursor/rules/42-transliteration-complexity-invariant.mdc` documenting that M3 transliteration is significantly harder than it looks (arena model gaps, SML compiler limits, synthetic names, ephemeral arenas). | **sv0c:** `lib/lowering.sv0` (+1 fn, +1 test), `lib/golden/stage0/lowering.c` (regen), `test/vm-parity/golden/sml/lowering.sv0b` (regen), `doc/transliteration-plan.md`, `PROGRESS.md`; **meta:** `README.md` (sv0c SHA), `.cursor/rules/42-transliteration-complexity-invariant.mdc` (new), this rollup | Metrics unchanged: bootstrap 97, self-host 97, stage0 21, vm-parity 97. **Qualitative:** lowering pipeline now spans expression → statement → block → body → function → **program** (top-level `lower`); body instruction fill deferred; remaining: ~5 lower_expr_to_value forms + per-function body integration + contract injection | `./scripts/sv0 compile-run lib/lowering.sv0` (exit 0), `./scripts/sv0 vm-compile lib/lowering.sv0` (OK), `./scripts/sv0 test-guards` (all green), `SV0_SKIP_SELF_HOST_COMPILER_DIFF=1 ./scripts/sv0 test` (exit 0) |
|| 2026-04-18 (America/Denver) | **`continue-development`** — **Deep-function transliteration batch 68 — lowering.sv0 function-level lowering.** `lowering.sv0`: `lower_body` (SML `lowerBody`), `lower_fn` (SML `lowerFn` simplified, no contracts). 2 new test groups. | **sv0c:** `lib/lowering.sv0` (+2 fns, +2 tests), `lib/golden/stage0/lowering.c` (regen), `test/vm-parity/golden/sml/lowering.sv0b` (regen), `doc/transliteration-plan.md`, `PROGRESS.md`; **meta:** `README.md` (sv0c SHA), this rollup | Metrics unchanged: bootstrap 97, self-host 97, stage0 21, vm-parity 97. **Qualitative:** lowering pipeline from expression → statement → block → body → function is now complete (simplified, no contracts); next is top-level `lower` and remaining lower_expr_to_value forms | `./scripts/sv0 compile-run lib/lowering.sv0` (exit 0), `./scripts/sv0 vm-compile lib/lowering.sv0` (OK), `./scripts/sv0 test-guards` (all green), `SV0_SKIP_SELF_HOST_COMPILER_DIFF=1 ./scripts/sv0 test` (exit 0) |
|| 2026-04-18 (America/Denver) | **`continue-development`** — **Deep-function transliteration batch 67 — lowering.sv0 statement and block lowering.** `lowering.sv0`: `lower_return` (SML `lowerReturn`), `lower_stmt` (SML `lowerStmt` simplified), `lower_block` (SML `lowerBlock`). 3 new test groups. | **sv0c:** `lib/lowering.sv0` (+3 fns, +3 tests), `lib/golden/stage0/lowering.c` (regen), `test/vm-parity/golden/sml/lowering.sv0b` (regen), `doc/transliteration-plan.md`, `PROGRESS.md`; **meta:** `README.md` (sv0c SHA), this rollup | Metrics unchanged: bootstrap 97, self-host 97, stage0 21, vm-parity 97. **Qualitative:** statement/block lowering complete — lower_return + lower_stmt + lower_block are the SML lowerReturn/lowerStmt/lowerBlock equivalents; lowerFn is next | `./scripts/sv0 compile-run lib/lowering.sv0` (exit 0), `./scripts/sv0 vm-compile lib/lowering.sv0` (OK), `./scripts/sv0 test-guards` (all green), `SV0_SKIP_SELF_HOST_COMPILER_DIFF=1 ./scripts/sv0 test` (exit 0) |
|| 2026-04-18 (America/Denver) | **`continue-development`** — **Deep-function transliteration batch 66 — lowering.sv0 expression lowering wrappers.** `lowering.sv0`: `value_to_expr` (SML `valueToExpr` — VVar→Load, otherwise→Literal), `lower_expr_with_instrs` (SML `lowerExprWithInstrs` — wrapper returning Expr), `lower_for_effect` (SML `lowerExprForEffect` simplified — lower + discard value). 3 new test groups. | **sv0c:** `lib/lowering.sv0` (+3 fns, +3 tests), `lib/golden/stage0/lowering.c` (regen), `test/vm-parity/golden/sml/lowering.sv0b` (regen), `doc/transliteration-plan.md`, `PROGRESS.md`; **meta:** `README.md` (sv0c SHA), this rollup | Metrics unchanged: bootstrap 97, self-host 97, stage0 21, vm-parity 97. **Qualitative:** expression lowering mutual recursion cluster complete — value_to_expr + lower_expr_with_instrs + lower_for_effect are the SML lowerExprWithInstrs/lowerExprForEffect equivalents needed by lowerStmt and lowerFn | `./scripts/sv0 compile-run lib/lowering.sv0` (exit 0), `./scripts/sv0 vm-compile lib/lowering.sv0` (OK), `./scripts/sv0 test-guards` (all green), `SV0_SKIP_SELF_HOST_COMPILER_DIFF=1 ./scripts/sv0 test` (exit 0) |
|| 2026-04-18 (America/Denver) | **`continue-development`** — **Deep-function transliteration batch 65 — lowering.sv0 ExprWhile/ExprLoop/ExprBreak/ExprContinue/ExprCast.** `lowering.sv0`: `lower_expr_to_value` extended from 17 to 22 of ~27 AST forms. ExprWhile (WhileLoop instr with lowered cond+body), ExprLoop (WhileLoop(true, body)), ExprBreak (Break instr), ExprContinue (Continue instr), ExprCast (Unop(99,v) simplified). ExprFor remains stub. 3 new test groups (test_lower_while, test_lower_loop, test_lower_cast). | **sv0c:** `lib/lowering.sv0` (+5 real cases replacing stubs, +3 tests), `lib/golden/stage0/lowering.c` (regen), `test/vm-parity/golden/sml/lowering.sv0b` (regen), `doc/transliteration-plan.md`, `PROGRESS.md`; **meta:** `README.md` (sv0c SHA), this rollup | Metrics unchanged: bootstrap 97, self-host 97, stage0 21, vm-parity 97. **Qualitative:** lower_expr_to_value now handles 22 of ~27 AST forms — remaining ~5: ExprCall builtins/2-seg ctor, ExprMatch, ExprStruct, ExprTry | `./scripts/sv0 compile-run lib/lowering.sv0` (exit 0), `./scripts/sv0 vm-compile lib/lowering.sv0` (OK), `./scripts/sv0 test-guards` (all green), `SV0_SKIP_SELF_HOST_COMPILER_DIFF=1 ./scripts/sv0 test` (exit 0) |
|| 2026-04-12 (America/Denver) | **`continue-development`** — **Deep-function transliteration batch 62 — lowering.sv0 IR types + core helpers.** `lowering.sv0`: local IR enum types (Value 8v/Expr 5v/Instr 16v matching codegen.sv0 layout), `ir_value_tag`/`ir_expr_tag`/`ir_instr_tag` accessor functions, `ctr_fresh` (mutable temp counter via Vec<i32> + vec_set), `classify_assign_lhs_arena` (SML `classifyAssignLhs` — flat arena LHS classification: ExprPath(1-seg)→ALVar, ExprField(ExprPath,field)→ALField). 3 new test groups (test_ir_types, test_ctr_fresh, test_classify_arena). | **sv0c:** `lib/lowering.sv0` (+3 enums, +6 fns, +3 tests), `lib/golden/stage0/lowering.c` (regen), `test/vm-parity/golden/sml/lowering.sv0b` (regen), `doc/transliteration-plan.md`, `PROGRESS.md`; **meta:** `README.md` (sv0c SHA), this rollup | Metrics unchanged: bootstrap 97, self-host 97, stage0 21, vm-parity 97. **Qualitative:** IR enum types now in lowering.sv0 — prerequisite for core lowering dispatch functions (lowerExprToValue etc.) | `./scripts/sv0 compile-run lib/lowering.sv0` (exit 0), `./scripts/sv0 vm-compile lib/lowering.sv0` (OK), `./scripts/sv0 test-guards` (all green), `./scripts/sv0 test` (exit 0) |
|| 2026-04-17 (America/Denver) | **`continue-development`** — **Deep-function transliteration batch 35 — Lowering AST walk helpers.** `lowering.sv0`: 2 new functions (`mentions_result` — recursive expr arena walk for "result" path, `all_old_names_expr` — recursive expr arena walk collecting old(x) handles). 2 new test groups (10 assertions). | **sv0c:** `lib/lowering.sv0` (+2 impl, +2 tests → 105 fns), `lib/golden/stage0/lowering.c` (regen), `test/vm-parity/golden/sml/*.sv0b` (97 regen), `doc/transliteration-plan.md`, `PROGRESS.md`; **meta:** this rollup | Metrics unchanged: bootstrap 97, self-host 97, stage0 21, vm-parity 97. **Qualitative:** `lowerFn` prerequisites progressing — `mentionsResult` + `allOldNames` done; remaining: `scanLets`/`ensGlue`/`injectEnsuresAndRetSlot`/`lowerEnsuresClause` then the mutual recursion cluster | `./scripts/sv0 emit-c lib/lowering.sv0` + cc + run (exit 0), `./scripts/sv0 vm-compile lib/lowering.sv0` (OK), `./scripts/sv0 test-guards` (all green), `SV0_SKIP_SELF_HOST_COMPILER_DIFF=1 ./scripts/sv0 test` (exit 0) |
|| 2026-04-17 (America/Denver) | **`continue-development`** — **Deep-function transliteration batch 34 — Lowering typedef builders.** `lowering.sv0`: 6 new functions (`handle_to_str`, `emit_struct_td`, `emit_enum_td`, `build_struct_order`, `build_enum_variants_data`, `collect_typedefs_str`). 1 new test group (`test_typedef_builders` — 7 assertions). 13 dead-code constant functions removed (freeing SML compiler budget). **SML compiler constraint:** nested `string_concat` causes silent crash; sequential assignment works. | **sv0c:** `lib/lowering.sv0` (+6 impl, +1 test, −13 dead fns → net 101 fns), `lib/golden/stage0/lowering.c` (regen), `test/vm-parity/golden/sml/*.sv0b` (97 regen), `doc/transliteration-plan.md`, `PROGRESS.md`; **meta:** this rollup | Metrics unchanged: bootstrap 97, self-host 97, stage0 21, vm-parity 97. **Qualitative:** lowering typedef builders done — remaining: `lowerExpr`/`lowerStmt`/`lowerFn`/`lower` (deep AST traversal) | `./scripts/sv0 emit-c lib/lowering.sv0` + cc + run (exit 0), `./scripts/sv0 vm-compile lib/lowering.sv0` (OK), `./scripts/sv0 test-guards` (all green), `SV0_SKIP_SELF_HOST_COMPILER_DIFF=1 ./scripts/sv0 test` (exit 0) |
|| 2026-04-16 (America/Denver) | **`continue-development`** — **Deep-function transliteration batch 33 — VM codegen `emit_program` (SML `VmCodegen.emit` — top-level).** `vm_codegen.sv0`: 4 new functions (`is_main_label`, `build_func_order`, `emit_program`, `emit_program_1fn` test helper). 3 new test groups (8 assertions). **VM backend transliteration complete.** Note: test functions kept compact due to SML bootstrap compiler stack-depth limit on large files. `emit_program` returns pool + ft_entries (not raw bytecode) because `encode_strings`/`encode_file` live in `bytecode.sv0` (separate compilation unit). | **sv0c:** `lib/vm_codegen.sv0` (+4 fns, +3 tests), `lib/golden/stage0/vm_codegen.c` (regen), `test/vm-parity/golden/sml/*.sv0b` (97 regen), `doc/transliteration-plan.md`, `PROGRESS.md`; **meta:** M3 task (refinement log), this rollup | Metrics unchanged: bootstrap 97, self-host 97, stage0 21, vm-parity 97. **Qualitative:** VM backend transliteration complete — all SML `VmCodegen` functions have sv0 equivalents | `./scripts/sv0 emit-c lib/vm_codegen.sv0` + cc + run (exit 0), `./scripts/sv0 vm-compile lib/vm_codegen.sv0` (OK), `./scripts/sv0 test-guards` (all green), `./scripts/sv0 test` (exit 0) |
|| 2026-04-16 (America/Denver) | **`continue-development`** — **Deep-function transliteration batch 32 — VM codegen `emit_fn`.** `vm_codegen.sv0`: 2 new functions (`compute_arity`, `emit_fn` — ties together `local_count_and_env` + `emit_instrs` + arity + pool into stride-4 func entry). 2 new test groups (26 assertions). | **sv0c:** `lib/vm_codegen.sv0` (+2 fns, +2 tests), `lib/golden/stage0/vm_codegen.c` (regen), `test/vm-parity/golden/sml/*.sv0b` (regen), `doc/transliteration-plan.md`, `PROGRESS.md`; **meta:** M3 task (refinement log), this rollup | Metrics unchanged: bootstrap 97, self-host 97, stage0 21, vm-parity 97. **Qualitative:** `emitFn` done; remaining VM codegen: top-level `emit` (program orchestration) | `./scripts/sv0 emit-c lib/vm_codegen.sv0` + cc + run (exit 0), `./scripts/sv0 vm-compile lib/vm_codegen.sv0` (OK), `./scripts/sv0 test-guards` (all green), `./scripts/sv0 test` (exit 0) |
|| 2026-04-16 (America/Denver) | **`continue-development`** — **Deep-function transliteration batch 31 — VM codegen `local_count_and_env`.** `vm_codegen.sv0`: 4 new functions (`ensure_var_int`, `scan_instr_env`, `scan_instrs_env`, `local_count_and_env` — bind params + recursive instruction scan for slot allocation). `env_field_starts` placeholder; field data deferred to `emitFn`. 1 new test group (38 assertions). | **sv0c:** `lib/vm_codegen.sv0` (+4 fns, +1 test), `lib/golden/stage0/vm_codegen.c` (regen), `test/vm-parity/golden/sml/*.sv0b` (regen via capture script), `doc/transliteration-plan.md`, `PROGRESS.md`; **meta:** M3 task (refinement log), this rollup | Metrics unchanged: bootstrap 97, self-host 97, stage0 21, vm-parity 97. **Qualitative:** function-level env setup done; remaining VM codegen: `emitFn`/top-level `emit` | `./scripts/sv0 emit-c lib/vm_codegen.sv0` + cc + run (exit 0), `./scripts/sv0 vm-compile lib/vm_codegen.sv0` (OK), `./scripts/sv0 test-guards` (all green), `./scripts/sv0 test` (exit 0) |
|| 2026-04-16 (America/Denver) | **`continue-development`** — **Deep-function transliteration batch 30 — VM codegen StoreField/Requires/Ensures (final emit_instr cases).** `vm_codegen.sv0`: `index_of_field_range` (field lookup in flat per-slot fields), `contract_pool_key` (negative key encoding for requires/ensures pool entries), StoreField case (emit expr + STORE_LOCAL at base + field offset via env_field_starts/env_fields_flat), Requires case (emit condition + CONTRACT_CHECK with requires pool key), Ensures case (same with ensures pool key). `emit_instr`/`emit_instrs` signatures extended with 2 new parameters. **All 16 of 16 Instr cases now implemented.** 2 new test groups (42 assertions). | **sv0c:** `lib/vm_codegen.sv0` (+2 helpers, +3 emit_instr cases, +2 params to 2 fns, +2 tests), `lib/golden/stage0/vm_codegen.c` (regen), `test/vm-parity/golden/sml/vm_codegen.sv0b` (regen), `doc/transliteration-plan.md`, `PROGRESS.md`; **meta:** M3 task (refinement log), README sv0c SHA, this rollup | Metrics unchanged: bootstrap 97, self-host 97, stage0 21, vm-parity 97. **Qualitative:** emit_instr complete (16/16); remaining VM codegen: emitFn/top-level emit (function-level orchestration) | `./scripts/sv0 emit-c lib/vm_codegen.sv0` + cc + run (exit 0), `./scripts/sv0 vm-compile lib/vm_codegen.sv0` (OK), `./scripts/sv0 test-guards` (all green), `./scripts/sv0 test` (exit 0) |
|| 2026-04-16 (America/Denver) | **`continue-development`** — **Deep-function transliteration batch 29 — VM codegen WhileLoop/Break/Continue.** `vm_codegen.sv0`: `loop_break_sentinel`/`is_loop_break_sentinel`, `patch_break_jumps` (scan for break sentinels, patch to end-of-loop), WhileLoop case (cond + JUMP_IF_NOT + body + back-edge + sentinel patching), Break (JUMP break sentinel), Continue (JUMP continue sentinel). 1 new test group (38 assertions). | **sv0c:** `lib/vm_codegen.sv0` (+3 helpers, +3 emit_instr cases, +1 test), `lib/golden/stage0/vm_codegen.c` (regen), `test/vm-parity/golden/sml/vm_codegen.sv0b` (regen), `doc/transliteration-plan.md`, `PROGRESS.md`; **meta:** M3 task (refinement log), README sv0c SHA fix, this rollup | Metrics unchanged: bootstrap 97, self-host 97, stage0 21, vm-parity 97. **Qualitative:** 13 of 16 Instr cases implemented; WhileLoop completes all control-flow emission. Remaining: StoreField, Requires/Ensures, plus emitFn/top-level emit | `./scripts/sv0 emit-c lib/vm_codegen.sv0` + cc + run (exit 0), `./scripts/sv0 vm-compile lib/vm_codegen.sv0` (OK), `./scripts/sv0 test-guards` (all green), `./scripts/sv0 test` (exit 0) |
|| 2026-04-16 (America/Denver) | **`continue-development`** — **Deep-function transliteration batch 28 — VM codegen Call dispatch (builtins + general).** `vm_codegen.sv0`: `handle_to_str` (token handle → string), `index_of_fn` (SML `indexOfFn` — fn name → fn_names table index), `fn_names` parameter added to `emit_instr`/`emit_instrs`, Call case implemented (15 builtins via CALL_BUILTIN + general calls via CALL with fn_names lookup; width-aware destination store). 1 new test group (36 assertions). | **sv0c:** `lib/vm_codegen.sv0` (+2 helpers, +1 emit_instr case, +1 param to 2 fns, +1 test), `lib/golden/stage0/vm_codegen.c` (regen), `test/vm-parity/golden/sml/vm_codegen.sv0b` (regen), `doc/transliteration-plan.md`, `PROGRESS.md`; **meta:** M3 task (refinement log), README sv0c SHA, this rollup | Metrics unchanged: bootstrap 97, self-host 97, stage0 21, vm-parity 97. **Qualitative:** 10 of 16 Instr cases implemented; Call was the largest remaining piece. Remaining: WhileLoop, StoreField, Requires/Ensures, Break/Continue, plus emitWhileLoop/emitFn/top-level emit | `./scripts/sv0 emit-c lib/vm_codegen.sv0` + cc + run (exit 0), `./scripts/sv0 vm-compile lib/vm_codegen.sv0` (OK), `./scripts/sv0 test-guards` (all green), `./scripts/sv0 test` (exit 0) |
|| 2026-04-12 (America/Denver) | **`continue-development`** — **Deep-function transliteration batch 27 — VM codegen IfElse/BlockInstr.** `vm_codegen.sv0`: `flat_byte_size` (walk flat insn Vec for jump offset calc), IfElse case (separate then/else Vecs, JUMP_IF_NOT/JUMP byte offsets), BlockInstr case (delegate to emit_instrs). 2 new test groups (39 assertions). | **sv0c:** `lib/vm_codegen.sv0` (+1 fn, +2 emit_instr cases, +2 tests), `lib/golden/stage0/vm_codegen.c` (regen), `test/vm-parity/golden/sml/vm_codegen.sv0b` (regen), `doc/transliteration-plan.md`, `PROGRESS.md`; **meta:** M3 task (refinement log), README sv0c SHA, this rollup | Metrics unchanged: bootstrap 97, self-host 97, stage0 21, vm-parity 97. **Qualitative:** 9 of 16 Instr cases implemented; IfElse control-flow with correct jump offset calculation verified | `./scripts/sv0 emit-c lib/vm_codegen.sv0` + cc + run (exit 0), `./scripts/sv0 vm-compile lib/vm_codegen.sv0` (OK), `./scripts/sv0 test-guards` (all green), `./scripts/sv0 test` (exit 0) |
|| 2026-04-12 (America/Denver) | **`continue-development`** — **Deep-function transliteration batch 26 — VM codegen emit_instr/emit_instrs.** `vm_codegen.sv0`: 6 new functions — `emit_instr` (SML `emitInstr` — IR Instr → bytecode; Nop/DeclVar/DeclNamed/Assign/Store/ReturnVoid/ReturnVal), `emit_instrs` (SML `emitBlock` — iterate Box<Instr> Vec), `vec_append`, `emit_ret_width`. Local `Instr` enum (16 variants). 3 new test groups (66 assertions total). | **sv0c:** `lib/vm_codegen.sv0` (+6 fns, +1 enum, +3 tests), `lib/golden/stage0/vm_codegen.c` (regen), `test/vm-parity/golden/sml/vm_codegen.sv0b` (regen), `doc/transliteration-plan.md`, `PROGRESS.md`; **meta:** M3 task (refinement log), README sv0c SHA, this rollup | Metrics unchanged: bootstrap 97, self-host 97, stage0 21, vm-parity 97. **Qualitative:** VM codegen now has instruction-level emission framework (emit_instr + emit_instrs). 7 of 16 Instr cases implemented; remaining deferred cases: IfElse (jump offset calc), WhileLoop, BlockInstr, Call (builtin dispatch + fn index), StoreField, Requires/Ensures (pool string construction) | `./scripts/sv0 emit-c lib/vm_codegen.sv0` (exit 0), `./scripts/sv0 vm-compile lib/vm_codegen.sv0` (OK), `./scripts/sv0 test-guards` (all green), `./scripts/sv0 test` (exit 0: 97-file self-host loop, 21 stage0 goldens, 97 vm-parity — all green) |
|| 2026-04-16 (America/Denver) | **`continue-development`** — **Deep-function transliteration batch 25 — VM codegen emit_value/emit_expr.** `vm_codegen.sv0`: 4 new functions — `emit_value` (SML `emitValue` — IR Value → bytecode for all scalar cases), `emit_expr` (SML `emitExpr` — IR Expr → bytecode for Literal/Load/Binop/Unop with handle-based op resolution), `binop_handle_to_insn`/`unop_handle_to_insn`. Local `Value`/`Expr` enum definitions. 2 new test groups. Also removed leftover batch-24 debug files. | **sv0c:** `lib/vm_codegen.sv0` (+4 fns, +2 enums, +2 tests), `lib/golden/stage0/vm_codegen.c` (regen), `test/vm-parity/golden/sml/vm_codegen.sv0b` (regen), `doc/transliteration-plan.md`, `PROGRESS.md`; **meta:** M3 task (refinement log), this rollup | Metrics unchanged: bootstrap 97, self-host 97, stage0 21, vm-parity 97. **Qualitative:** VM codegen now has emit_value + emit_expr covering all scalar IR emission — the building blocks for emitInstr. Remaining VM codegen gap: `emitInstr`/`emitBlock`/`emitWhileLoop`/`emitFn`/`emit` | `./scripts/sv0 compile-run lib/vm_codegen.sv0` (exit 0), `./scripts/sv0 vm-compile lib/vm_codegen.sv0` (exit 0), `./scripts/sv0 test` (exit 0: 97-file self-host loop, 21 stage0 goldens, 97 vm-parity — all green) |
|| 2026-04-16 (America/Denver) | **`continue-development`** — **Deep-function transliteration batch 24 — bytecode file-level encode/decode.** `bytecode.sv0`: 2 new functions — `encode_file` (SML `encodeFile` — assembles complete .sv0b from pre-encoded string section + stride-4 func table with code Vec handles), `decode_file` (SML `decodeFile` — parses .sv0b byte buffer with encode→decode roundtrip verified). 2 new test groups. | **sv0c:** `lib/bytecode.sv0` (+2 fns, +2 tests), `lib/golden/stage0/bytecode.c` (regen), `test/vm-parity/golden/sml/bytecode.sv0b` (regen), `doc/transliteration-plan.md`, `PROGRESS.md`; **meta:** M3 task (refinement log), this rollup | Metrics unchanged: bootstrap 97, self-host 97, stage0 21, vm-parity 97. **Qualitative:** bytecode module now has complete SML parity for encode/decode — all `encodeStrings`/`decodeStrings`/`encodeAll`/`decodeAll`/`encodeFile`/`decodeFile` functions done. Remaining VM backend gap: `emitValue`/`emitExpr`/`emitInstr`/`emitBlock` in `vm_codegen.sv0` | `./scripts/sv0 compile-run lib/bytecode.sv0` (exit 0), `./scripts/sv0 vm-compile lib/bytecode.sv0` (exit 0), `./scripts/sv0 test` (exit 0: 97-file self-host loop, 21 stage0 goldens, 97 vm-parity — all green) |
|| 2026-04-16 (America/Denver) | **`continue-development`** — **Deep-function transliteration batch 23 — checker expression-arena helpers.** `checker.sv0`: 4 new functions — `stmt_returns_arena`/`last_stmt_returns_arena` (SML `stmtReturns`/`lastStmtReturns` — arena-aware), `chk_tok_str` (token text extraction), `expr_references_result` (SML `exprReferencesResult` — recursive expr arena walk for "result" path). 3 new test groups. | **sv0c:** `lib/checker.sv0` (+4 fns, +3 tests), `lib/golden/stage0/checker.c` (regen), `test/vm-parity/golden/sml/checker.sv0b` (regen), `doc/transliteration-plan.md`, `PROGRESS.md`; **meta:** M3 task (refinement log), this rollup | Metrics unchanged: bootstrap 97, self-host 97, stage0 21, vm-parity 97. **Qualitative:** checker now has arena-aware statement return detection and recursive expression tree walk for `result` references — building blocks for `checkContract` and `checkBlock` | `./scripts/sv0 compile-run lib/checker.sv0` (exit 0), `./scripts/sv0 vm-compile lib/checker.sv0` (exit 0), `./scripts/sv0 test` (exit 0: 97-file self-host loop, 21 stage0 goldens, 97 vm-parity — all green) |
|| 2026-04-16 (America/Denver) | **`continue-development`** — **Deep-function transliteration batch 22 — bytecode instruction encode/decode + insn_vec_stride fix.** `bytecode.sv0`: 4 new functions — `encode_insn_at` (SML `encodeInsn`), `decode_insn_at` (SML `decodeInsnVec`), `encode_all_insns` (SML `encodeAll`), `decode_all_insns` (SML `decodeAll`). 7 new test groups. `vm_codegen.sv0`: fixed `insn_vec_stride` bug for PUSH_BOOL/RETURN_SLOTS (stride was 1, should be 2). | **sv0c:** `lib/bytecode.sv0` (+4 fns, +7 tests), `lib/vm_codegen.sv0` (insn_vec_stride fix), `lib/golden/stage0/bytecode.c` (regen), `lib/golden/stage0/vm_codegen.c` (regen), `test/vm-parity/golden/sml/bytecode.sv0b` (regen), `test/vm-parity/golden/sml/vm_codegen.sv0b` (regen), `doc/transliteration-plan.md`, `PROGRESS.md`; **meta:** M3 task (refinement log), this rollup | Metrics unchanged: bootstrap 97, self-host 97, stage0 21, vm-parity 97. **Qualitative:** bytecode instruction-level encode/decode complete — all 50 opcodes covered with roundtrip verification. File-level encodeFile/decodeFile next for full bytecode parity. insn_vec_stride fix unblocks correct instruction Vec traversal for PUSH_BOOL and RETURN_SLOTS | `./scripts/sv0 compile-run lib/bytecode.sv0` (exit 0), `./scripts/sv0 compile-run lib/vm_codegen.sv0` (exit 0), `./scripts/sv0 test-guards` (all green), `./scripts/sv0 test` (exit 0: 97-file self-host loop, 21 stage0 goldens, 97 vm-parity — all green) |
|| 2026-04-15 (America/Denver) | **`continue-development`** — **Deep-function transliteration batch 21 — apply_use_clause + resolve_top_item + resolve_program (resolver orchestration).** `resolver.sv0`: 4 new functions — `apply_use_clause`/`apply_use_clauses` (SML `applyUseClause` — use clause alias registration with mangled target lookup), `resolve_top_item` (stub — item-body resolution deferred on parser enrichment), `resolve_program` (SML `resolve` — top-level orchestration). 4 new test groups. **Resolver function-level transliteration complete.** | **sv0c:** `lib/resolver.sv0` (+4 fns, +4 tests), `lib/golden/stage0/resolver.c` (regen), `test/vm-parity/golden/sml/resolver.sv0b` (regen), `doc/transliteration-plan.md`, `PROGRESS.md`; **meta:** M3 task (refinement log), this rollup | Metrics unchanged: bootstrap 97, self-host 97, stage0 21, vm-parity 97. **Qualitative:** resolver transliteration at function level is complete — all SML resolver.sml functions have sv0 equivalents. 5 item-body traversals (resolveFn/Struct/Enum/Trait/Impl) deferred on parser arena enrichment. Next: type checker transliteration or parser arena enrichment for item-body resolution | `./scripts/sv0 compile-run lib/resolver.sv0` (exit 0), `./scripts/sv0 test-guards` (all green), `./scripts/sv0 test` (exit 0) |
|| 2026-04-15 (America/Denver) | **`continue-development`** — **Deep-function transliteration batch 20 — resolve_expr + resolve_stmt + resolve_contract + scope management.** `resolver.sv0`: 8 new functions — `res_enter_scope`/`res_bind_local`/`res_lookup_in_frames_str`/`res_full_value_exists`/`res_lookup_fn_arity_str` (scope management + combined env lookups), `resolve_expr` (SML `resolveExpr` — all 27 expr tag cases with scope/binding/arity checking; quantifier forall/exists special dispatch; ExprCast type validation deferred — type arena local to parser), `resolve_stmt` (SML `resolveStmt` — LetStmt binding + SemiStmt), `resolve_contract` (thin wrapper). 6 new test groups. | **sv0c:** `lib/resolver.sv0` (+8 fns, +6 tests), `lib/golden/stage0/resolver.c` (regen), `test/vm-parity/golden/sml/resolver.sv0b` (regen), `doc/transliteration-plan.md`, `PROGRESS.md`; **meta:** M3 task (refinement log), this rollup | Metrics unchanged: bootstrap 97, self-host 97, stage0 21, vm-parity 97. **Qualitative:** resolver now has full expression/statement/contract traversal — the core name-resolution validation pass is complete. Next: resolveFn/resolveStruct/resolveEnum/resolveTrait/resolveImpl (item-level wrappers) and resolveTopItem/applyUseClause/resolve (top-level orchestration) | `./scripts/sv0 compile-run lib/resolver.sv0` (exit 0), `./scripts/sv0 test-guards` (all green), `./scripts/sv0 test` (exit 0: 97-file self-host loop, 21 stage0 goldens, 97 vm-parity — all green) |
|| 2026-04-15 (America/Denver) | **`continue-development`** — **Deep-function transliteration batch 19 — resolver env inlining + resolve_pat_shape + resolve_ty.** `resolver.sv0`: 6 new functions — `tok_str` (token→string extraction), `res_is_prelude_type`, `res_value_exists`, `res_type_exists` (inline env lookups with string-based comparison via token stream), `resolve_pat_shape` (SML `resolvePatShape` — recursive pattern arena validation), `resolve_ty` (SML `resolveTy` — recursive type arena validation; TyArray size expr deferred). 7 new test groups. | **sv0c:** `lib/resolver.sv0` (+6 fns, +7 tests), `lib/golden/stage0/resolver.c` (regen), `test/vm-parity/golden/sml/resolver.sv0b` (regen), `doc/transliteration-plan.md`, `PROGRESS.md`; **meta:** M3 task (refinement log), this rollup | Metrics unchanged: bootstrap 97, self-host 97, stage0 21, vm-parity 97. **Qualitative:** resolver now validates pattern constructor names and type names — first AST traversal functions. Next: resolve_expr/resolve_stmt/resolve_contract (expression-level validation) | `./scripts/sv0 compile-run lib/resolver.sv0` (exit 0), `./scripts/sv0 test-guards` (all green), `./scripts/sv0 test` (exit 0: 97-file self-host loop, 21 stage0 goldens, 97 vm-parity — all green) |
|| 2026-04-12 (America/Denver) | **`continue-development`** — **Deep-function transliteration batch 18 — enriched ItemFn arena + resolver registration pass.** `parser.sv0`: ItemFn arena d3=param_count, d4=contract_count (was 0/0); `parse_param_list`/`parse_contracts` count outputs. `resolver.sv0`: `register_item`/`register_items` (SML `registerItem` → env registration pass for item arena; enum variant registration deferred). 3 new test groups. | **sv0c:** `lib/parser.sv0` (enriched ItemFn, updated param_list/contracts sigs, updated tests), `lib/resolver.sv0` (+2 fns, +3 tests), `lib/golden/stage0/parser.c` (regen), `lib/golden/stage0/resolver.c` (regen), `test/vm-parity/golden/sml/parser.sv0b` (regen), `test/vm-parity/golden/sml/resolver.sv0b` (regen), `PROGRESS.md`; **meta:** M3 task (refinement log), this rollup | Metrics unchanged: bootstrap 97, self-host 97, stage0 21, vm-parity 97. **Qualitative:** resolver now has item-arena registration pass — foundational for downstream name resolution traversals. Next: resolver AST traversals (resolveTy/resolveExpr/resolveStmt) or enum variant sub-arena | `./scripts/sv0 compile-run lib/parser.sv0` (exit 0), `./scripts/sv0 compile-run lib/resolver.sv0` (exit 0), `./scripts/sv0 test-guards` (all green), `./scripts/sv0 test` (exit 0: 97-file self-host loop, 21 stage0 goldens, 97 vm-parity — all green) |
|| 2026-04-12 (America/Denver) | **`continue-development`** — **Deep-function transliteration batch 17 — parseItem cluster (parser closure).** `parser.sv0`: 12 new functions (`parse_param`, `parse_param_list`, `parse_contract`, `parse_contracts`, `parse_fn_body`, `parse_struct_item`, `parse_enum_item`, `parse_type_alias_item`, `parse_trait_item`, `parse_impl_item`, `parse_item`, `parse_program`), item arena tags 0–7, 4 new TK_ constants, 7 new test groups. | **sv0c:** `lib/parser.sv0` (+12 fns, +7 tests), `lib/golden/stage0/parser.c` (regen), `test/vm-parity/golden/sml/parser.sv0b` (regen), `doc/transliteration-plan.md`, `PROGRESS.md`; **meta:** M3 task (refinement log), this rollup | Metrics unchanged: bootstrap 97, self-host 97, stage0 21, vm-parity 97. **Qualitative:** **parser transliteration complete** — all SML parser functions (expr, stmt, pat, type, item, program) have sv0 equivalents. Next transliteration focus: resolver/checker or IR/lowering | `./scripts/sv0 vm-compile lib/parser.sv0` (exit 0), `./scripts/sv0 compile-run lib/parser.sv0` (exit 0), `./scripts/sv0 test-guards` (all green), `./scripts/sv0 test` (exit 0: 97-file self-host loop, 21 stage0 goldens, 97 vm-parity — all green) |
|| 2026-04-15 (America/Denver) | **`continue-development`** — **Deep-function transliteration batch 16 — match expressions + contract quantifiers + loop invariants.** `parser.sv0`: 2 new functions (`parse_match_expr` with iterative arm loop, `parse_quant`), ExprMatchArm(29) in expr arena, MATCH/FORALL/EXISTS/OLD/NO_ALIAS dispatch in `parse_primary_expr`, `parse_while_expr` updated with `loop_invariant` parsing (d3/d4). 7 new test groups (match_simple/multi/guard, quant, old, no_alias, while_inv). | **sv0c:** `lib/parser.sv0` (+2 fns, +7 tests), `lib/golden/stage0/parser.c` (regen), `test/vm-parity/golden/sml/parser.sv0b` (regen), `doc/transliteration-plan.md`, `PROGRESS.md`; **meta:** M3 task (refinement log), this rollup | Metrics unchanged: bootstrap 97, self-host 97, stage0 21, vm-parity 97. **Qualitative:** parser.sv0 now has match expression parsing (arms with pattern/guard/body), contract quantifiers (forall/exists), old/no_alias builtins, and loop invariant parsing — **parser remaining: `parseItem` cluster only** | `./scripts/sv0 vm-compile lib/parser.sv0` (exit 0), `./scripts/sv0 compile-run lib/parser.sv0` (exit 0), `./scripts/sv0 test-guards` (all green), `./scripts/sv0 test` (exit 0: 97-file self-host loop, 21 stage0 goldens, 97 vm-parity — all green) |
|| 2026-04-15 (America/Denver) | **`continue-development`** — **Deep-function transliteration batch 15 — control-flow expressions + block/stmt machinery.** `parser.sv0`: 10 new functions — `expr_ends_with_braced_block`, `parse_return_expr`, `parse_let_stmt`, `try_assign_stmt`, `try_stmt`, `parse_block`, `parse_if_expr`, `parse_while_expr`, `parse_for_expr`, `parse_loop_expr`. Stmt arena inline (LetStmt=27, SemiStmt=28). Control-flow dispatch in `parse_primary_expr` for LBRACE/IF/WHILE/FOR/LOOP/RETURN/UNSAFE/ASSERT. 8 new TK_ constants. 10 new test groups. | **sv0c:** `lib/parser.sv0` (+10 fns, +10 tests), `lib/golden/stage0/parser.c` (regen), `test/vm-parity/golden/sml/parser.sv0b` (regen), `doc/transliteration-plan.md`, `PROGRESS.md`; **meta:** M3 task (refinement log), this rollup | Metrics unchanged: bootstrap 97, self-host 97, stage0 21, vm-parity 97. **Qualitative:** parser.sv0 now has block/stmt parsing (let stmts, expression stmts, assignments, break/continue) and control-flow expressions (if/else-if/else, while, for, loop, return), proving the stmt-in-expr-arena encoding and braced-block semicolon elision | `./scripts/sv0 vm-compile lib/parser.sv0` (exit 0), `./scripts/sv0 compile-run lib/parser.sv0` (exit 0), `./scripts/sv0 test-guards` (all green), `./scripts/sv0 test` (exit 0: 97-file self-host loop, 21 stage0 goldens, 97 vm-parity — all green) |
|| 2026-04-15 (America/Denver) | **`continue-development`** — **Deep-function transliteration batch 14 — expression parser: full precedence chain + simple primary expressions.** `parser.sv0`: 20 new functions — `parse_arg_list`, `parse_struct_fields`, `parse_primary_expr` (literals/paths/struct literals/parens/tuples/arrays), `parse_postfix_expr` (calls/method calls/fields/tuple fields/index), `parse_unary_expr`, `parse_cast_expr`, `parse_mul_expr`…`parse_or_expr` (8-level binary precedence chain), `parse_cmp_expr`, `parse_range_expr`, `parse_expr` (entry point). Expr arena with 5 parallel Vecs (tags + data1-4, 27 variants). 11 new test groups. | **sv0c:** `lib/parser.sv0` (+20 fns, +11 tests), `lib/golden/stage0/parser.c` (regen), `test/vm-parity/golden/sml/parser.sv0b` (regen), `doc/transliteration-plan.md`, `PROGRESS.md`; **meta:** M3 task (refinement log), this rollup | Metrics unchanged: bootstrap 97, self-host 97, stage0 21, vm-parity 97. **Qualitative:** parser.sv0 now has complete expression precedence chain (primary→postfix→unary→cast→mul…or→cmp→range→expr), proving the inline `parseLeftAssoc` pattern for all 8 binary levels + non-chaining comparison | `./scripts/sv0 vm-compile lib/parser.sv0` (exit 0), `./scripts/sv0 compile-run lib/parser.sv0` (exit 0), `./scripts/sv0 test-guards` (all green), `./scripts/sv0 test` (exit 0: 97-file self-host loop, 21 stage0 goldens, 97 vm-parity — all green) |
|| 2026-04-15 (America/Denver) | **`continue-development`** — **Deep-function transliteration batch 13 — pattern parser cluster.** `parser.sv0`: `path_pattern_from` (SML `pathPatternFrom` — struct/enum pattern dispatch), `parse_pat_atom` (SML `parsePatAtom` — wildcard/bind/lit/tuple/enum), `parse_pat` (SML `parsePat` — entry with `\|` for PatOr). 11 new test groups. Also extended `.cursor/rules/41-sml-process-and-vm-hang-prevention.mdc` with agent command hang detection + C-backend while-loop condition hoisting documentation. | **sv0c:** `lib/parser.sv0` (+3 fns, +11 tests), `lib/golden/stage0/parser.c` (regen), `test/vm-parity/golden/sml/parser.sv0b` (regen), `doc/transliteration-plan.md`, `PROGRESS.md`; **meta:** M3 task (refinement log), `.cursor/rules/41-*.mdc` (extended), this rollup | Metrics unchanged: bootstrap 97, self-host 97, stage0 21, vm-parity 97. **Qualitative:** parser.sv0 now has full pattern parser cluster (`parse_pat` + atom + path_pattern_from) with flat pat arena, proving the mutual-recursion pattern for remaining `parseExpr`/`parseItem` | `./scripts/sv0 vm-compile lib/parser.sv0` (exit 0), `./scripts/sv0 compile-run lib/parser.sv0` (exit 0), `./scripts/sv0 test-guards` (all green), `./scripts/sv0 test` (exit 0: 97-file self-host loop, 21 stage0 goldens, 97 vm-parity — all green) |
|| 2026-04-12 (America/Denver) | **`continue-development`** — **Deep-function transliteration batch 12 — first recursive-descent parser function.** `parser.sv0`: `is_builtin_type_name` (SML inline 18-primitive check), `parse_type` (SML `parseType` lines 128-225 — full recursive-descent type parser with flat arena output; TyUnit/TyName/TyRef/TyRefMut/TyArray/TySlice/TyTuple + generic type args). 9 new test groups. | **sv0c:** `lib/parser.sv0` (+2 fns, +9 tests), `lib/golden/stage0/parser.c` (regen), `test/vm-parity/golden/sml/parser.sv0b` (regen), `doc/transliteration-plan.md`, `PROGRESS.md`; **meta:** M3 task (refinement log), this rollup | Metrics unchanged: bootstrap 97, self-host 97, stage0 21, vm-parity 97. **Qualitative:** parser.sv0 now has first recursive-descent function (`parse_type`), unlocking type positions for `parsePat`/`parseExpr`/`parseItem` | `./scripts/sv0 vm-compile lib/parser.sv0` (exit 0), `./scripts/sv0 compile-run lib/parser.sv0` (exit 0), `./scripts/sv0 test-guards` (all green), `./scripts/sv0 test` (exit 0: 97-file self-host loop, 21 stage0 goldens, 97 vm-parity — all green) |
|| 2026-04-13 (America/Denver) | **`continue-development`** — **Deep-function transliteration batch 3: bytecode.sv0 + link.sv0 + diagnostic.sv0 + codegen.sv0.** `bytecode.sv0`: Vec-based LE encoding (encode_u16_le/u32_le/i32_le → Vec<i32>), file header encoding (encode_header), decode helpers (decode_u16_at/u32_at), verify_magic. `link.sv0`: find_last_slash backslash fix, fn_name_for_link (main exemption), link_dir_from_entry. `diagnostic.sv0`: diag_int_to_str, format_location, format_diagnostic (full SML `format source d` equivalent). `codegen.sv0`: is_main_block, emit_block_body (named functions for splitMain/emitBlockBody 1:1 parity). 11 new test groups across 4 modules. | **sv0c:** `lib/bytecode.sv0` (+7 fns, +4 tests), `lib/link.sv0` (+3 fns, +2 tests), `lib/diagnostic.sv0` (+4 fns, +3 tests), `lib/codegen.sv0` (+2 fns, +2 tests), 4 golden `.c` (regen), 4 golden `.sv0b` (regen), `PROGRESS.md`; **meta:** M3 task (refinement log), this rollup | Metrics unchanged: bootstrap 97, self-host 97, stage0 21, vm-parity 84. **Qualitative:** bytecode now has Vec-based encode/decode building blocks; link has main-exemption parity; diagnostic has full format_diagnostic; codegen has explicit splitMain/emitBlockBody 1:1 with SML | `./scripts/sv0 compile-run lib/bytecode.sv0` (exit 0), `./scripts/sv0 compile-run lib/link.sv0` (exit 0), `./scripts/sv0 compile-run lib/diagnostic.sv0` (exit 0), `./scripts/sv0 compile-run lib/codegen.sv0` (exit 0), `./scripts/sv0 test-guards` (all green), `./scripts/sv0 test` (exit 0: 97-file self-host loop, 21 stage0 goldens, 84 vm-parity — all green; pre-existing bytecode_file_layout_core SML unit test failure unrelated) |
|| 2026-04-13 (America/Denver) | **`continue-development`** — **Deep-function transliteration: parser.sv0 + diagnostic.sv0 + include_expand.sv0.** `parser.sv0`: token stream navigation (peek/advance/expect/match on parallel Vecs), parsePath (::‑delimited paths), skip helpers on token stream (pub/unsafe/generic params/where/attributes). **Fixed 6 wrong keyword tags** (PUB 79→82, PROJECT 79→81, UNSAFE 90→93, WHERE 91→95, TRUE 89→91, EOF 96→97) — critical for lexer↔parser interop. 20+ keyword constants added. `diagnostic.sv0`: Vec-based diagnostic list + sort-by-span + format_header/format_snippet. `include_expand.sv0`: split_lines, cycle detection, path_dir/path_join. 11 new test groups across 3 modules. | **sv0c:** `lib/parser.sv0` (+12 fns, +3 tests), `lib/diagnostic.sv0` (+8 fns, +4 tests), `lib/include_expand.sv0` (+5 fns, +4 tests), 3 golden `.c` (regen), 3 golden `.sv0b` (regen), `PROGRESS.md`; **meta:** M3 task (refinement log), this rollup | Metrics unchanged: bootstrap 97, self-host 97, stage0 21, vm-parity 84. **Qualitative:** 5 of 20 real modules deepened toward SML parity (env, lexer, parser, diagnostic, include_expand); parser token tags now match canonical token.sv0 | `./scripts/sv0 compile-run lib/parser.sv0` (exit 0), `./scripts/sv0 compile-run lib/diagnostic.sv0` (exit 0), `./scripts/sv0 compile-run lib/include_expand.sv0` (exit 0), `./scripts/sv0 test-guards` (all green), `./scripts/sv0 test` (exit 0: 97-file self-host loop, 21 stage0 goldens, 84 vm-parity — all green) |
|| 2026-04-13 (America/Denver) | **`continue-development`** — **All seeds removed (batch 6): 15 subdirectory seeds (12 `parser/`, 3 `lexer/`).** Cleaned `parser/*_core.sv0` and `lexer/*_core.sv0` seeds from subdirectories outside `lib/`. All surrogates or superseded by real modules. Zero seeds remain in any directory. 97 programs: 20 real modules + 77 tests. | **sv0c:** 15 seed `.sv0` files deleted from `parser/` and `lexer/`, 15 golden `.c` files, 15 vm-parity `.sv0b` files, list updates; **meta:** `README.md`, `task/sv0-toolchain-milestone-3-self-host.Rmd`, this rollup | bootstrap **112→97** (−15); self-host **112→97** (−15); stage0 **36→21** (−15); vm-parity **99→84** (−15); seeds **0** (all directories) | `./scripts/sv0 test-guards` (all green), `./scripts/sv0 test` (exit 0: 97-file self-host loop, 21 stage0 goldens, 84 vm-parity — all green) |
|| 2026-04-13 (America/Denver) | **`continue-development`** — **SEED CONSOLIDATION COMPLETE: all 146 `*_core.sv0` seeds removed in 5 batches.** Batch 3: 25 small surrogates (lowering/resolver/vm_codegen/lexer/env). Batch 4: 50 medium seeds (all families). Batch 5: final 46 seeds (all remaining). Every seed was a pure i32/tag surrogate or already superseded by real module implementations. Zero seeds remain. 112 real programs (20 SML-counterpart modules, tests, infrastructure). | **sv0c:** 146 `lib/*_core.sv0` files deleted, 146 `lib/golden/stage0/*_core.c` files deleted, 67 `test/vm-parity/golden/sml/*_core.sv0b` files deleted, `lib/bootstrap-sources.list` (−146 entries), `lib/self-host-sv0-loop.list` (−146 entries), `test/vm-parity/manifest.txt` (−67 entries), `doc/transliteration-plan.md` (metrics), `PROGRESS.md`; **meta:** `README.md` (sv0c SHA), `task/sv0-toolchain-milestone-3-self-host.Rmd` (refinement log), this rollup | bootstrap **270→112** (−158); self-host **270→112** (−158); stage0 **194→36** (−158); vm-parity **182→99** (−83); seeds **146→0** (complete) | `./scripts/sv0 test-guards` (all green), `./scripts/sv0 test` (exit 0: 112-file self-host loop, 36 stage0 goldens, 99 vm-parity — all green) |
|| 2026-04-13 (America/Denver) | **`continue-development`** — **Phase 2 COMPLETE: all 20 SML files now have real sv0 counterparts.** `lib/main.sv0`: baseNameSv0 path helpers, VM output path, pipeline phase ordering (7 phases), CLI arg parsing, error prefix constants. 5 test groups. This is the **last** SML file to get a real module — completing Phase 2 coverage. 157 seed files remain for deep-function consolidation. | **sv0c:** `lib/main.sv0` (new, ~200 lines), `lib/golden/stage0/main.c`, `lib/bootstrap-sources.list` (+1), `lib/self-host-sv0-loop.list` (+1), docs; **meta:** `README.md`, `task/sv0-toolchain-milestone-3-self-host.Rmd`, this rollup | bootstrap **269→270** (+1); self-host **269→270** (+1); stage0 **193→194** (+1); vm-parity **182**; main.sv0 **0→5** test groups; **Phase 2 complete** (15 real modules + 4 type defs = all 20 SML files) | `./scripts/sv0 test-guards` (all green), `./scripts/sv0 test` (exit 0: 270-file self-host loop, 194 stage0 goldens, 182 vm-parity) |
|| 2026-04-13 (America/Denver) | **`continue-development`** — **Phase 2 new real modules: `lib/checker.sv0` + `lib/lowering.sv0` + `lib/parser.sv0`.** checker.sv0: binop classification, integral type detection, AST type name→tag (22 primitives), assignment LHS, stmt returns, path joining, type alias canonicalization, env helpers, variant shape, try operator, error codes, struct field lookup (13 test groups). lowering.sv0: splitQName, binop/unop→C strings, type→C mapping, freshTmp, oldSlot, retSyntaxIsUnit, storeValueToSlot, enum tag/struct field lookups, tryVariantNames, literal/value classification (18 test groups). parser.sv0: token→binop/assign-binop, literal classification, skip helpers, span merge, precedence classification (9 test groups). While-loop condition hoisting bug fixed. | **sv0c:** 3 new `lib/*.sv0`, 3 new `lib/golden/stage0/*.c`, `lib/bootstrap-sources.list` (+3), `lib/self-host-sv0-loop.list` (+3), docs; **meta:** `README.md`, `task/sv0-toolchain-milestone-3-self-host.Rmd`, this rollup | bootstrap **266→269** (+3); self-host **266→269** (+3); stage0 **190→193** (+3); vm-parity **182**; 40 new test groups across 3 modules | `./scripts/sv0 test-guards` (all green), `./scripts/sv0 test` (exit 0: 269-file self-host loop, 193 stage0 goldens, 182 vm-parity) |
|| 2026-04-13 (America/Denver) | **`continue-development`** — **Phase 2 new real module: `lib/lowering.sv0` (lowering.sml pure-function surface).** splitQName (qualified name splitting on ::), AST binop/unop→C strings (18+2), AST type→C type mapping (22 primitives), freshTmp counter, oldSlot naming, retSyntaxIsUnit, storeValueToSlot classification, assignment LHS, Vec-based enum tag lookup (flat tables), struct field lookup, enum/struct type detection, variant slot counting, enum max payload, tryVariantNames (Result/Option), literal classification, value→expr kind, enum alloc width, retValueSlot constant. 18 test groups. While-loop condition hoisting bug fixed. | **sv0c:** `lib/lowering.sv0` (new, ~558 lines), `lib/golden/stage0/lowering.c`, `lib/bootstrap-sources.list` (+1), `lib/self-host-sv0-loop.list` (+1), `doc/transliteration-plan.md`, `doc/self-host-sv0-loop.md`, `PROGRESS.md`; **meta:** `README.md` (sv0c SHA), `task/sv0-toolchain-milestone-3-self-host.Rmd`, this rollup | bootstrap **267→268** (+1); self-host **267→268** (+1); stage0 **191→192** (+1); vm-parity **182**; lowering.sv0 **0→18** test groups | `./scripts/sv0 test-guards` (all green), `./scripts/sv0 test` (exit 0: 268-file self-host loop, 192 stage0 goldens, 182 vm-parity) |
|| 2026-04-13 (America/Denver) | **`continue-development`** — **Phase 2 new real module: `lib/checker.sv0` (checker.sml pure-function surface).** Binop classification (Arith/Logic/Cmp), integral type detection, AST type name→tag mapping (22 primitives), assignment LHS classification, statement return detection, path→string joining, Vec-based type import alias canonicalization, env lookup/extend helpers, variant shape classification, try operator helpers, error code constants (E0400–E0449), struct field lookup. 13 test groups. | **sv0c:** `lib/checker.sv0` (new, ~458 lines), `lib/golden/stage0/checker.c`, `lib/bootstrap-sources.list` (+1), `lib/self-host-sv0-loop.list` (+1), `doc/transliteration-plan.md`, `doc/self-host-sv0-loop.md`, `PROGRESS.md`; **meta:** `README.md` (sv0c SHA), `task/sv0-toolchain-milestone-3-self-host.Rmd`, this rollup | bootstrap **266→267** (+1); self-host **266→267** (+1); stage0 **190→191** (+1); vm-parity **182**; checker.sv0 **0→13** test groups | `./scripts/sv0 test-guards` (all green), `./scripts/sv0 test` (exit 0: 267-file self-host loop, 191 stage0 goldens, 182 vm-parity) |
|| 2026-04-13 (America/Denver) | **`continue-development`** — **Phase 2 new real module: `lib/vm_codegen.sv0` (vm_codegen.sml pure-function surface).** Variant slot counting, cty classification (void/scalar), field index lookup (Vec-based), binop/unop→opcode dispatch (19+2), instruction encoding length (insn_size inlined from bytecode.sv0), loop sentinel constants, Vec-based string pool management (pool_new/pool_add with dedup), builtin function→CALL_BUILTIN id map (15 builtins), store slot ordering. 10 test groups. | **sv0c:** `lib/vm_codegen.sv0` (new, ~448 lines), `lib/golden/stage0/vm_codegen.c`, `lib/bootstrap-sources.list` (+1), `lib/self-host-sv0-loop.list` (+1), `doc/transliteration-plan.md`, `doc/self-host-sv0-loop.md`, `PROGRESS.md`; **meta:** `README.md` (sv0c SHA), `task/sv0-toolchain-milestone-3-self-host.Rmd`, this rollup | bootstrap **265→266** (+1); self-host **265→266** (+1); stage0 **189→190** (+1); vm-parity **182**; vm_codegen.sv0 **0→10** test groups | `./scripts/sv0 test-guards` (all green), `./scripts/sv0 test` (exit 0: 266-file self-host loop, 190 stage0 goldens, 182 vm-parity) |
|| 2026-04-13 (America/Denver) | **`continue-development`** — **Phase 2 new real module: `lib/bytecode.sv0` (bytecode.sml wire format surface).** All 50 wire opcode constants (HALT=0 through CAST=161), per-opcode encoding sizes (1/2/5/9/13), opcode classification (simple/arithmetic/compare/logic/bitwise/control-flow/struct/variant), division-based LE byte extraction/reconstruction (le_byte0-3, from_le_u16/u32), .sv0b file layout constants (6-byte header, 20-byte func entry, string pool overhead). 6 test groups. | **sv0c:** `lib/bytecode.sv0` (new, ~394 lines), `lib/golden/stage0/bytecode.c`, `lib/bootstrap-sources.list` (+1), `lib/self-host-sv0-loop.list` (+1), `doc/transliteration-plan.md`, `doc/self-host-sv0-loop.md`, `PROGRESS.md`; **meta:** `README.md` (sv0c SHA), `task/sv0-toolchain-milestone-3-self-host.Rmd`, this rollup | bootstrap **264→265** (+1); self-host **264→265** (+1); stage0 **188→189** (+1); vm-parity **182**; bytecode.sv0 **0→6** test groups | `./scripts/sv0 test-guards` (all green), `./scripts/sv0 test` (exit 0: 265-file self-host loop, 189 stage0 goldens, 182 vm-parity) |
| 2026-04-13 (America/Denver) | **`continue-development`** — **Phase 2 new real modules: `lib/resolver.sv0` + `lib/lexer.sv0` + `lib/link.sv0`.** `resolver.sv0`: string-based intrinsic registry (17 builtins, name→arity), item registration dispatch, pattern-bind counting, variant-arity, context rules, error codes, arity checking. 11 test groups. `lexer.sv0`: char classification, keyword lookup, escape handling, state mgmt, comment skipping, scan helpers, op tags. 11 test groups. `link.sv0`: file classification, name mangling, tops counting, directive stripping. 11 tests. | **sv0c:** `lib/resolver.sv0` (new, ~260 lines), `lib/lexer.sv0` (new, ~370 lines), `lib/link.sv0` (new, ~200 lines), `lib/golden/stage0/resolver.c` + `lexer.c` + `link.c`, `lib/bootstrap-sources.list` (+3), `lib/self-host-sv0-loop.list` (+3), `doc/transliteration-plan.md`, `doc/self-host-sv0-loop.md`, `PROGRESS.md`; **meta:** `task/sv0-toolchain-milestone-3-self-host.Rmd`, this rollup | bootstrap **261→264** (+3); self-host **261→264** (+3); stage0 **185→188** (+3); vm-parity **182**; resolver.sv0 **0→11** test groups; lexer.sv0 **0→11** test groups; link.sv0 **0→11** tests | `./scripts/sv0 test-guards` (all green), `./scripts/sv0 test` (exit 0: 264-file self-host loop, bootstrap-build all VM pass, 182 vm-parity, 188 stage0 goldens) |
| 2026-04-13 (America/Denver) | **`continue-development`** — **Phase 2 new real modules: `lib/lexer.sv0` + `lib/link.sv0`.** `lexer.sv0`: char classification (7 functions), string-based keyword lookup (43 keywords → canonical token tags), escape handling, real-string state mgmt (peek/advance), whitespace + nested block-comment skipping, ident + digit scan helpers (dec/hex/bin/oct w/ underscore), operator tag mapping. 11 test groups. `link.sv0`: file classification, name mangling, tops counting, directive stripping. 11 tests. | **sv0c:** `lib/lexer.sv0` (new, ~370 lines), `lib/link.sv0` (new, ~200 lines), `lib/golden/stage0/lexer.c` + `link.c`, `lib/bootstrap-sources.list` (+2), `lib/self-host-sv0-loop.list` (+2), `doc/transliteration-plan.md`, `doc/self-host-sv0-loop.md`, `PROGRESS.md`; **meta:** `task/sv0-toolchain-milestone-3-self-host.Rmd`, this rollup | bootstrap **261→263** (+2); self-host **261→263** (+2); stage0 **185→187** (+2); vm-parity **182**; lexer.sv0 **0→11** test groups; link.sv0 **0→11** tests | `./scripts/sv0 test-guards` (all green), `./scripts/sv0 test` (exit 0: 263-file self-host loop, bootstrap-build all VM pass, 182 vm-parity, 187 stage0 goldens) |
| 2026-04-13 (America/Denver) | **`continue-development`** — **Phase 2 new real module: `lib/link.sv0` (link.sml pure-function surface).** isSv0, isHidden, fileStem, mangle (string-based), top-level name collection (collectTopsCount), link directive stripping (stripDirectivesCount), module prefix detection, in-tops membership (Vec-based). 11 tests. Added to bootstrap+self-host lists (+1 each). Doc metrics reconciled. | **sv0c:** `lib/link.sv0` (new, ~200 lines), `lib/golden/stage0/link.c` (updated), `lib/bootstrap-sources.list` (+1), `lib/self-host-sv0-loop.list` (+1), `doc/transliteration-plan.md` (metrics), `doc/self-host-sv0-loop.md` (metrics), `PROGRESS.md`; **meta:** `task/sv0-toolchain-milestone-3-self-host.Rmd` (refinement log), this rollup | bootstrap **261→262**; self-host **261→262**; stage0 **185** (unchanged); vm-parity **182** (unchanged); link.sv0 **0→11** tests | `./scripts/sv0 test-guards` (all green), `./scripts/sv0 test` (exit 0: 262-file self-host loop, bootstrap-build all VM pass, 182 vm-parity, 185 stage0 goldens) |
| 2026-04-13 (America/Denver) | **`continue-development`** — **Phase 2 real module extensions: include_expand, diagnostic, env.** include_expand.sv0 completed (trim, parse_include_line — full SML parseIncludeLine surface). diagnostic.sv0 extended (severity_to_string, spaces, carets, pad_left, get_line). env.sv0 rewritten from fixed-slot to Vec-based scope management (string-based is_prelude_type, Vec mod_vals/mod_tys/frames, enter_scope/bind_local/lookup_value). VM-parity goldens refreshed. | **sv0c:** `lib/include_expand.sv0`, `lib/diagnostic.sv0`, `lib/env.sv0`, `lib/golden/stage0/include_expand.c`, `lib/golden/stage0/diagnostic.c`, `lib/golden/stage0/env.c` (all updated), `test/vm-parity/golden/sml/` (refreshed), `PROGRESS.md`; **meta:** `task/sv0-toolchain-milestone-3-self-host.Rmd` (refinement log), this rollup | include_expand.sv0 tests **5→7**; diagnostic.sv0 tests **5→10**; env.sv0 tests **5→6** (rewritten); bootstrap **261** (unchanged); self-host **261** (unchanged); stage0 **185** (unchanged); vm-parity **182** (goldens refreshed) | `./scripts/sv0 test-guards` (all green), `./scripts/sv0 test` (exit 0: 261-file self-host loop, bootstrap-build all VM pass, 182 vm-parity, 185 stage0 goldens) |
| 2026-04-12 (America/Denver) | **`continue-development`** — **Phase 2 codegen.sv0 — complete `codegen.sml` surface.** Unified Call variant (replaces CallVoid/CallDest), emitCallArgs (Vec<Value> via Box handles), general emitParamList (interleaved pairs), emitFnProtoEx, top-level emitProgram (4-stride block Vec, typedefs, fwd decls, static/main split). VM stack underflow fixed via emit_call_void/emit_call_dest helpers. 44 tests total. | **sv0c:** `lib/codegen.sv0` (~900 lines), `lib/golden/stage0/codegen.c` (updated), `PROGRESS.md`; **meta:** `README.md` (sv0c SHA), `task/sv0-toolchain-milestone-3-self-host.Rmd` (refinement log), this rollup | codegen.sv0 tests **31→44**; bootstrap **261** (unchanged); self-host **261** (unchanged); stage0 **184** (unchanged) | `./scripts/sv0 test-guards` (all green), `./scripts/sv0 test` (exit 0: 261-file self-host loop, bootstrap-build VM all pass, 182 vm-parity) |
| 2026-04-12 (America/Denver) | **`thoroughly-execute-document`** + **`continue-development`** — **Phase 2 codegen.sv0 — full instruction emission (17 kinds) including recursive IfElse/While/Block.** Instr enum (17 variants: flat + recursive), emitInstr dispatch for all kinds, emit_instrs with Vec<i32> + Box<Instr> round-trip for recursive instruction lists, emitParamList, emitFnProto, emitBlockDefn. 31 tests. Added `lib/codegen.sv0` to bootstrap + self-host lists + stage0 golden. Stale doc metrics reconciled (transliteration-plan.md, self-host-sv0-loop.md). README sv0c SHA fixed. | **sv0c:** `lib/codegen.sv0` (extended 318→~600 lines), `lib/bootstrap-sources.list` (+1), `lib/self-host-sv0-loop.list` (+1), `lib/golden/stage0/codegen.c` (new), `doc/transliteration-plan.md` (metrics), `doc/self-host-sv0-loop.md` (metrics), `PROGRESS.md`; **meta:** `README.md` (sv0c SHA), `task/sv0-toolchain-milestone-3-self-host.Rmd` (refinement log), this rollup | bootstrap **260→261**; self-host **260→261**; stage0 **183→184**; vm-parity **186** unchanged; codegen.sv0 tests **11→31** | `./scripts/sv0 test-guards` (all green), `./scripts/sv0 test` (all green: 261-file self-host loop, 184 stage0 goldens) |
| 2026-04-11 (America/Denver) | **`continue-development`** — **Track A final deepening: 11 more seeds (170→181), Track A COMPLETE.** Three batches: (1) lexer.sml 9→12 (keyword, scan_ident, scan_char). (2) parser.sml 22→26 (if_expr, match_expr, struct_item, unary_expr). (3) checker.sml 18→20 (expect, item_fn_ty). vm_codegen.sml 13→15 (enum_layout, pool_add). All non-trivial standalone functions now seeded; remaining unseeded are mutual-recursion groups, I/O wrappers, trivial accessors. Fixed: `result` is a sv0 keyword. | **sv0c:** 11 new `lib/*.sv0` files, `lib/bootstrap-sources.list` (+11), `lib/golden/stage0/` (+11 C files), `lib/self-host-sv0-loop.list` (+11), `test/vm-parity/manifest.txt` (+11), `test/vm-parity/golden/sml/` (+11 sv0b), `test/data/golden/pass/` (+11 sv0+golden), `test/test_runner.sml` (+11), `PROGRESS.md`; **meta:** M3 task (inventory + refinement log), roadmap snapshot, README sv0c SHA, this rollup | unit **298→309** (est); vm-parity **171→182**; stage0 **173→184**; self-host **249→260**; total seeds **170→181** | `./scripts/sv0 test-guards` (all green: 182 manifest); emit-c + vm-compile confirmed for all 11 files; 4 sv0c commits pushed |
| 2026-04-11 (America/Denver) | **`continue-development`** — **Track A exhaustive deepening: 25 more seeds (145→170).** Two sv0c commits: (1) bytecode 5→7 (decode_insn, file_layout) + lexer 5→9 (skip_comment, scan_number, scan_op, scan_string). (2) parser 17→22 + checker 13→18 + link 5→7 + resolver 7→9 + vm_codegen 10→13 + codegen 6→7 + main 2→3. Every SML file >100 lines now has 3+ seeds. Remaining unseeded functions are orchestration wrappers/I/O entry points. | **sv0c:** 25 new `lib/*.sv0` files, `lib/bootstrap-sources.list` (+25), `lib/golden/stage0/` (+25 C files), `lib/self-host-sv0-loop.list` (+25), `test/vm-parity/manifest.txt` (+25), `test/vm-parity/golden/sml/` (+25 sv0b), `test/data/golden/pass/` (+25 sv0+golden), `test/test_runner.sml` (+25), `PROGRESS.md`; **meta:** M3 task (inventory + refinement log), roadmap snapshot, README sv0c SHA, this rollup | unit **273→298** (est); vm-parity **146→171**; stage0 **148→173**; self-host **224→249**; total seeds **145→170** | `./scripts/sv0 test-guards` (all green: 171 manifest); emit-c + vm-compile confirmed for all 25 files; 3 sv0c commits pushed |
| 2026-04-11 (America/Denver) | **`continue-development`** — sv0c `PROGRESS.md` update + submodule bump + README SHA sync | **sv0c:** `PROGRESS.md` (metrics: seeds 121→145); **meta:** `README.md` sv0c pin `f7eae20`, `sv0c` submodule pointer | *(no code change — docs only)* | `./scripts/sv0 test-guards` (all green) |
| 2026-04-11 (America/Denver) | **`continue-development`** — **Track A comprehensive deepening: 24 new seeds across all major SML files.** Three sv0c commits: (1) checker 8→11 (integral_ty, stmt_returns, ctor_ty) + vm_codegen 5→8 (variant_slots, jump_patch, bind_param) + main 1→2 (pipeline_core) + codegen 4→6 (escape, proto). (2) parser 12→15 (binop_table, pat, path) + bytecode 3→5 (le, string_pool) + resolver 5→7 (register_item, use_clause) + link 3→5 (mangle, split_module). (3) checker 11→13 (expr_refs, try_payload) + vm_codegen 8→10 (local_scan, call_nargs) + parser 15→17 (expr_span, prec). Every SML file >100 lines now has 2+ seeds. | **sv0c:** 24 new `lib/*.sv0` files, `lib/bootstrap-sources.list` (+24), `lib/golden/stage0/` (+24 C files), `lib/self-host-sv0-loop.list` (+24), `test/vm-parity/manifest.txt` (+24), `test/vm-parity/golden/sml/` (+24 sv0b), `test/data/golden/pass/` (+24 sv0+golden), `test/test_runner.sml` (+24); **meta:** M3 task (inventory + refinement log), roadmap snapshot, README sv0c SHA, this rollup | unit **249→273** (est); vm-parity **122→146**; stage0 **124→148**; self-host **200→224**; total seeds **121→145** | `./scripts/sv0 test-guards` (all green: 146 manifest); emit-c + vm-compile confirmed for all 24 files; 3 sv0c commits pushed |
| 2026-04-11 (America/Denver) | **`continue-development`** — **Phase 2 continued + Track A bytecode/lexer deepened.** Phase 2: `lib/include_expand.sv0` (real include expansion module — isSpace, pathOk, include parsing, find_closing_quote, chomp_cr). Track A: `lib/bytecode_insn_size_core.sv0` (instruction byte sizes for ~56 opcodes + is_jump/is_push) + `lib/bytecode_opcode_map_core.sv0` (magic bytes, format version, opcode_class, section sizes). `lib/lexer_char_class_core.sv0` (6 char classification fns) + `lib/lexer_advance_core.sv0` (advance pos/line/col, at_end, extract_len, span). Bytecode.sml 1→3 seeds, lexer.sml 3→5 seeds. | **sv0c:** `lib/include_expand.sv0`, `lib/bytecode_insn_size_core.sv0`, `lib/bytecode_opcode_map_core.sv0`, `lib/lexer_char_class_core.sv0`, `lib/lexer_advance_core.sv0`, `lib/bootstrap-sources.list` (+5), `lib/golden/stage0/` (+5 C files), `lib/self-host-sv0-loop.list` (+5), `test/vm-parity/manifest.txt` (+5), `test/vm-parity/golden/sml/` (+5 sv0b), `test/data/golden/pass/` (+5 sv0+golden), `test/test_runner.sml` (+5), `PROGRESS.md`; **meta:** M3 task (refinement log + inventory + roadmap snapshot), this rollup | unit **244→249**; vm-parity **117→122**; stage0 **119→124**; self-host **195→200**; total seeds **116→121** | `./scripts/sv0 test-guards` (all green: 122 manifest); SML unit tests 249/249 green; vm-compile confirmed for all 5 files; full `./scripts/sv0 test` run confirmed units + integration + goldens pass but bootstrap-in-sv0 VM phase hung on orphaned SML processes (infrastructure, not code — killed and re-confirmed) |
| 2026-04-11 (America/Denver) | **`continue-development`** — **Phase 2 continued + Track A checker deepened.** Phase 2: `lib/diagnostic.sv0` (real diagnostic module — Severity enum, constructors, hasErrors, errorCount, severity ordering) + `lib/env.sv0` (real environment/scope module — prelude type check, scope depth, frame lookup, dup detection). Track A: `lib/checker_binop_class_core.sv0` (binop classification: Arith/Logic/Cmp for all 18 tags) + `lib/checker_variant_shape_core.sv0` (variant shape dispatch: Unit/Tuple/Struct + field count + max payload). Checker.sml 6→8 seeds. YAML spacing fix + roadmap M3 snapshot update. | **sv0c:** `lib/diagnostic.sv0`, `lib/env.sv0`, `lib/checker_binop_class_core.sv0`, `lib/checker_variant_shape_core.sv0`, `lib/bootstrap-sources.list` (+4), `lib/golden/stage0/` (+4 C files), `lib/self-host-sv0-loop.list` (+4), `test/vm-parity/manifest.txt` (+4), `test/vm-parity/golden/sml/` (+4 sv0b), `test/data/golden/pass/` (+4 sv0+golden), `test/test_runner.sml` (+4), `PROGRESS.md`; **meta:** M3 task (refinement log + inventory + roadmap snapshot), this rollup | unit **240→244**; vm-parity **113→117**; stage0 **115→119**; self-host **191→195**; total seeds **112→116** | `./scripts/sv0 test` (all green: 244 unit, 14 C int, 15 VM int, 117 vm-parity, 119 stage0, 195 self-host) |
| 2026-04-11 (America/Denver) | **`thoroughly-execute-document`** — **Phase 2 started + Track A deepened.** Phase 2: `lib/unify.sv0` (first real pipeline module — 20v Ty enum, TyVar wildcard, Ref/RefMut cross-matching, structural recursion via Box/Vec; fixes TyVar semantics from unify_core surrogate). Track A: `lib/link_is_sv0_core.sv0` + `lib/link_is_hidden_core.sv0` (link.sml 1→3 seeds), `lib/resolver_pat_binds_core.sv0` + `lib/resolver_enum_variant_reg_core.sv0` (resolver.sml 3→5 seeds). Wildcard `_` pattern first used in real module. YAML spacing fix + roadmap M3 snapshot update. | **sv0c:** `lib/unify.sv0`, `lib/link_is_sv0_core.sv0`, `lib/link_is_hidden_core.sv0`, `lib/resolver_pat_binds_core.sv0`, `lib/resolver_enum_variant_reg_core.sv0`, `lib/bootstrap-sources.list` (+5), `lib/golden/stage0/` (+5 C files), `lib/self-host-sv0-loop.list` (+5), `test/vm-parity/manifest.txt` (+5), `test/vm-parity/golden/sml/` (+5 sv0b), `test/data/golden/pass/` (+5 sv0+golden), `test/test_runner.sml` (+5); **meta:** M3 task (refinement log + inventory + roadmap snapshot), this rollup | unit **235→240**; vm-parity **108→113**; stage0 **110→115**; self-host **186→191**; total seeds **107→112** | `./scripts/sv0 test` (all green: 240 unit, 14 C int, 15 VM int, 113 vm-parity, 115 stage0, 191 self-host) |
| 2026-04-11 (America/Denver) | **`continue-development`** — **All 20 SML files seeded**: `lexer/token.sv0` (Token enum, 98 variants — largest by variant count; 5 literal + 1 ident payloads, 92 unit; exhaustive 98-arm match) + `lib/contract_analyzer.sv0` (identity pass-through). Closes last two 0-seed gaps (`token.sml`, `analyzer.sml`). YAML spacing fix. | **sv0c:** `lexer/token.sv0`, `lib/contract_analyzer.sv0`, `lib/bootstrap-sources.list` (+2), `lib/golden/stage0/token.c` + `contract_analyzer.c`, `lib/self-host-sv0-loop.list` (+2), `test/vm-parity/manifest.txt` (+2), `test/vm-parity/golden/sml/token.sv0b` + `contract_analyzer.sv0b`, `test/data/golden/pass/token.*` + `contract_analyzer.*`, `test/test_runner.sml` (+2), `PROGRESS.md`; **meta:** M3 task (refinement log + inventory: 0-seed gaps closed), this rollup | vm-parity **106→108**; stage0 **108→110**; self-host **184→186**; bootstrap **191→193**; unit **233→235** | `./scripts/sv0 test` (all green: 235 unit, 14 C int, 15 VM int, 108 vm-parity, 110 stage0, 186 self-host, 193 bootstrap) |
| 2026-04-11 (America/Denver) | **`continue-development`** — **Phase 1 complete**: `lib/ast.sv0` (full AST types — Literal 6v, Unop 6v, Binop 18v, Ty 8v Box\<Ty\>, Pat 7v, Expr 27v Box\<Expr\> — largest enum, Arm struct, Stmt 4v, Contract 3v, AstVariant 3v, Item 8v + 5 support structs). All 4 SML type modules now have real sv0 definitions. | **sv0c:** `lib/ast.sv0`, `lib/bootstrap-sources.list` (+1), `lib/golden/stage0/ast.c`, `lib/self-host-sv0-loop.list` (+1), `test/vm-parity/manifest.txt` (+1), `test/vm-parity/golden/sml/ast.sv0b`, `test/data/golden/pass/ast.*`, `test/test_runner.sml` (+1), `PROGRESS.md`; **meta:** M3 task (refinement log + inventory update), this rollup | vm-parity **105→106**; stage0 **107→108**; self-host **183→184**; bootstrap **190→191**; unit **232→233** | `./scripts/sv0 test` (all green: 233 unit, 14 C int, 15 VM int, 106 vm-parity, 108 stage0, 184 self-host, 191 bootstrap) |
| 2026-04-11 (America/Denver) | **`continue-development`** — **Phase 1 continued**: `lib/ir.sv0` (real IR Value 8v / Expr 6v / Instr 17v including 4-payload Call + Param + IrBlock structs; Box\<Value\>/Box\<Expr\> recursive, Vec\<i32\> for instruction lists). Tests refactored into per-type helpers to stay under VM bytecode 256-slot local limit. YAML frontmatter spacing fix. | **sv0c:** `lib/ir.sv0`, `lib/bootstrap-sources.list` (+1), `lib/golden/stage0/ir.c`, `lib/self-host-sv0-loop.list` (+1), `test/vm-parity/manifest.txt` (+1), `test/vm-parity/golden/sml/ir.sv0b`, `test/data/golden/pass/ir.*`, `test/test_runner.sml` (+1), `PROGRESS.md`; **meta:** M3 task (YAML fix + refinement log + inventory), this rollup | vm-parity **104→105**; stage0 **106→107**; self-host **182→183**; bootstrap **189→190** | `./scripts/sv0 test` (all green: 232 unit, 14 C int, 15 VM int, 105 vm-parity, 107 stage0, 183 self-host, 190 bootstrap) |
| 2026-04-11 (America/Denver) | **`continue-development`** — **Phase 1 started**: `lib/types.sv0` (real Ty enum, 20 variants, Box\<Ty\> + Vec\<i32\>, i32 name handles) + `lib/span.sv0` (real Pos/Span structs, flattened for VM); first non-surrogate type definition modules. VM nested-struct limitation documented (widthOfCty does not recurse). | **sv0c:** `lib/types.sv0`, `lib/span.sv0`, `lib/bootstrap-sources.list` (+2), `lib/golden/stage0/types.c` + `span.c`, `lib/self-host-sv0-loop.list` (+2), `test/vm-parity/manifest.txt` (+2), `test/vm-parity/golden/sml/types.sv0b` + `span.sv0b`, `test/data/golden/pass/types.*` + `span.*`, `test/test_runner.sml` (+2), `PROGRESS.md`; **meta:** M3 task (refinement log, inventory note), this rollup | vm-parity **102→104**; stage0 **104→106**; self-host **180→182**; bootstrap **187→189** | `./scripts/sv0 test` (all green: 231 unit, 14 C int, 15 VM int, 104 vm-parity, 106 stage0, 182 self-host, 189 bootstrap) |
| 2026-04-11 (America/Denver) | **`continue-development`** — Phase 0 Tier 1 complete + Phase 1 validated. 3 compiler fixes: (1) `resolveFieldTy` +string in enums, (2) `matchScrutCty` +enum ctor path for box_new, (3) diverging blocks typed as fresh TyVar. New `ast_types` integration test: 3 enums with Box payloads, recursive eval, Vec, nested boxing — passes C + VM. | **sv0c:** `sml/type_checker/checker.sml`, `sml/ir/lowering.sml`, `test/integration/ast_types/`, `test/data/golden/pass/ast_types.*`, `test/test_runner.sml`, `test/vm-parity/manifest.txt`, `lib/golden/stage0/ast_types.c`, `lib/bootstrap-sources.list`, `lib/self-host-sv0-loop.list`, `PROGRESS.md`; **meta:** `task/sv0-toolchain-milestone-3-self-host.Rmd` (inventory + refinement log), integration test scripts, this rollup | Unit tests **228→229**; C int **13→14**; VM int **14→15**; vm-parity **101→102**; stage0 **103→104**; self-host **179→180**; bootstrap **186→187** | `./scripts/sv0 test-guards`, `./scripts/sv0 test` (pending full run) |
| 2026-04-10 (America/Denver) | **`thoroughly-execute-document`** — M3 Phase 0: **T0-4 Vec\<T\>** — handle-based dynamic array as built-in type. C runtime `sv0_runtime.h` +`sv0_vec_table` pool + `sv0_vec_new`/`push`/`len`/`get`/`set` (intptr_t elems). Resolver: `Vec` in prelude types, 5 intrinsics. Checker: `astTyToTy` Vec\<T\>→TyNamed; vec function types with TyVar wildcards. Lowering: Vec→"int" handle + 5 call patterns. VM codegen: CALL_BUILTIN 7-11. VM interpreter: `dynVecs` pool + 5 handlers. YAML spacing fixes. | **sv0c:** `runtime/sv0_runtime.h` (+vec pool), `sml/name_resolution/env.sml` (Vec prelude), `sml/name_resolution/resolver.sml` (+5 intrinsics + resolveTy recurse tyArgs), `sml/type_checker/checker.sml` (Vec in astTyToTy + 5 vec fn types in modEnvFromProg), `sml/ir/lowering.sml` (Vec→int + 5 call patterns), `sml/backend/vm/vm_codegen.sml` (CALL_BUILTIN 7-11), `test/test_runner.sml` (+3 tests), `test/data/golden/pass/vec_api.*`, `test/integration/vec_api/`; **sv0vm:** `src/interpreter/interpreter.sml` (dynVecs pool + builtins 7-11); **meta:** YAML spacing (3 files), integration test scripts, this rollup | Unit tests **221→224**; C integration **10→11**; VM integration **13→14**; all other metrics unchanged (186 bootstrap, 179 self-host pilots, 103 stage0, 101 vm-parity — all green) | `./scripts/sv0 test-guards`, `./scripts/sv0 test` (224 unit tests, 179-file self-host loop, 103 stage0 goldens, 101 vm-parity goldens, 11 C int, 14 VM int — all green) |
| 2026-04-10 (America/Denver) | **`thoroughly-execute-document`** — M3 Phase 0: **T0-2 Generics (AST + Parser + Checker)** — **(T0-2b)** `TyName` extended to 3-field `path * ty list * Span.span`; `type_params : ident list` on `ItemFn`/`ItemStruct`/`ItemEnum`; `parseGenericParams` + inline `tyArgLoop` for type positions; all 6 downstream SML files updated (`resolver`, `checker`, `lowering`, `link`). **(T0-2c)** `withTypeParams` allocates `TyVar` for generic scopes; `astTyToTy`/`resolveFieldTy` consult `currentTypeParams`; resolver registers type params via `registerModuleType`; `Unify.unify` treats `TyVar` as wildcard. YAML frontmatter spacing fixes (3 files). | **sv0c:** `sml/ast/ast.sml` (`TyName`, `ItemFn`/`ItemStruct`/`ItemEnum`), `sml/parser/parser.sml` (`parseGenericParams`, `tyArgLoop`, `parseFnBody`/`parseStructItem`/`parseEnumItem`), `sml/name_resolution/resolver.sml` (`resolveTy`, `resolveFn`/`resolveStruct`/`resolveEnum` + type param registration), `sml/type_checker/checker.sml` (`withTypeParams`, `currentTypeParams`, `astTyToTy`, `resolveFieldTy`, `structOne`/`enumOne`/`checkFn`/`itemFnTy`), `sml/type_checker/unify.sml` (`TyVar` wildcard), `sml/ir/lowering.sml` (all `TyName` + record annotations), `sml/link/link.sml` (`mapTy`/`mapFn`/`mapStruct`/`mapEnum`), `test/test_runner.sml` (+3 checker tests); **meta:** YAML spacing (3 files), this rollup | Unit tests **218→221**; all other metrics unchanged (186 bootstrap, 179 self-host pilots, 103 stage0, 101 vm-parity, 10 C int, 13 VM int — all green) | `./scripts/sv0 test-guards`, `./scripts/sv0 test` (221 unit tests, 179-file self-host loop, 103 stage0 goldens, 101 vm-parity goldens — all green) |
| 2026-04-10 (America/Denver) | **`thoroughly-execute-document`** — M3 Phase 0 execution: **T0-1 String API** (5 intrinsics: `string_len`, `string_eq`, `string_concat`, `string_char_at`, `string_substr` through resolver→checker→lowering→C runtime→VM codegen→VM interpreter), **T0-3 Full Enum Match** (struct-payload matching via `VSStruct` in checker + `PatStruct` in `matchArmSetup` lowering; unit variant patterns without parens via parser fix), YAML frontmatter spacing fixes | **sv0c:** `runtime/sv0_runtime.h` (+5 string functions), `sml/name_resolution/resolver.sml` (+5 intrinsics in `withIntrinsics`), `sml/type_checker/checker.sml` (+5 builtins in `modEnvFromProg`, `VSStruct` variant shape, `PatStruct` in `bindPat`), `sml/ir/lowering.sml` (+5 string lowering patterns, `PatStruct` in `matchArmSetup`), `sml/backend/vm/vm_codegen.sml` (+5 CALL_BUILTIN 2-6), `sml/parser/parser.sml` (unit variant pattern fix), `test/test_runner.sml` (+10 tests), `test/data/golden/pass/string_api.*` + `enum_struct_match.*` (new), `test/integration/string_api/` + `enum_struct_match/` (new); **sv0vm:** `src/interpreter/interpreter.sml` (dynamic string pool + CALL_BUILTIN 2-6); **meta:** YAML spacing fixes, this rollup | Bootstrap **183→186**; self-host pilots **177→179**; unit tests **207→218**; C integration **8→10**; VM integration **11→13** | `./scripts/sv0 test-guards`, `./scripts/sv0 test` (179-file self-host loop, 218 unit tests, 103 stage0 goldens, 101 vm-parity goldens, 10 C integration, 13 VM integration — all green) |
| 2026-04-10 (America/Denver) | **`thoroughly-execute-document`** — M3 execution: T0-1 (`println` string variables — vertical slice through checker+lowering+C+VM), T0-3 integration test (tuple-payload enum match already works), Track A checker deepening (4 new seeds), M3 task update (Track 0 tiers, exception strategy, idiom guide, Track A priorities) | **sv0c:** `sml/type_checker/checker.sml` (println accepts `TyString` args, not just literals — E0444 updated), `sml/ir/lowering.sml` (string→`const char*` C type + general println lowering), `sml/backend/vm/vm_codegen.sml` (println uses `emitValue` for any value), `test/test_runner.sml` (+3 tests: checker string var, e2e println var, golden println_var), `test/data/golden/pass/println_var.*` (3 files), `test/integration/enum_tuple_match/` (new), `lib/checker_{binop_class,synth_expr,call_path,check_fn}_core.sv0` (4 new seeds), `lib/golden/stage0/*.c` (4 new), `lib/bootstrap-sources.list` (+6), `lib/self-host-sv0-loop.list` (+6); integration test scripts (+enum_tuple_match). **meta:** `task/sv0-toolchain-milestone-3-self-host.Rmd` (Track 0 execution tiers, exception strategy, idiom guide, Track A priorities, refinement log), `task/sv0-toolchain-roadmap-full.Rmd` + `task/sv0doc-milestone-0.Rmd` (YAML spacing), this rollup | Bootstrap **178→183**; stage0 **99→103**; self-host pilots **172→177**; unit tests **204→207**; C integration **7→8**; VM integration **10→11**; checker seeds **2→6** | `./scripts/sv0 test-guards`, `./scripts/sv0 test` (177-file self-host loop, 207 unit tests, 103 stage0 goldens, 101 vm-parity goldens, 8 C integration, 11 VM integration — all green) |
| 2026-04-10 (America/Denver) | **`thoroughly-execute-document`** — `task/sv0-toolchain-roadmap-full.Rmd`: reconcile M0/M1/M2/MCP-0 PROGRESS ledgers with milestone closure; normalize M0 `state`; M3 dependency-aware decomposition | **sv0doc:** `PROGRESS.md` (DOC-1..4 → 1); **sv0vm:** `PROGRESS.md` (VM-1..3 → 1); **sv0-mcp:** `PROGRESS.md` (MCP-1..3 → 1); **meta:** `task/sv0doc-milestone-0.Rmd` (`state: done` → `complete`), `task/sv0-toolchain-milestone-3-self-host.Rmd` (M3 decomposition: language surface gap + dependency-ordered tracks), this rollup Summary + run log | sv0doc **0/4→4/4** (0%→100%); sv0vm **0/3→3/3** (0%→100%); sv0-mcp **0/3→3/3** (0%→100%); sv0c **6/6** unchanged; meta **5/5** unchanged; **overall 100%** (process gates) | `./scripts/sv0 test-guards` |
| 2026-04-10 (America/Denver) | **GitHub Actions / org CI** — green **`sv0-toolchain` meta `CI`** (`./scripts/sv0 test`): **`make test`** golden **compile+run** failed on fresh runners (no gitignored **`sv0c/build/`**); **`verify_task_rmd_frontmatter`** rejected **`updated:`** YAML | **sv0c:** `Makefile` (`test` → **`mkdir -p build`**), `PROGRESS.md` note; **meta:** `task/sv0-toolchain-milestone-3-checklist.Rmd` + `task/sv0-toolchain-milestone-3-self-host.Rmd` frontmatter space after **`updated:`**, submodule pointer + `README.md` sv0c pin, this run log | *(checklist % unchanged)* | `./scripts/sv0 test-guards`, `cd sv0c && rm -rf build && make test`, `./scripts/sv0 test`; post-push: **`gh run watch`** on **`sv0-toolchain/sv0-toolchain`** **CI** |
| 2026-04-10 (America/Denver) | **`continue-development`** — **all SML modules seeded**: C backend complete (`splitMain`/`emit`), VM backend (5 seeds), bytecode, driver, include expansion, types, unify, checker dispatch, pipeline composition — 13 new seeds total | **sv0c:** 13× new `lib/*_core.sv0`, lists (+13 each), `lib/golden/stage0/*.c` (×13), `doc/transliteration-plan.md`, `doc/self-host-sv0-loop.md`, `lib/LAYOUT.md`, `PROGRESS.md`; **meta:** M3 task + M3 checklist + progress rollup; YAML `updated:` spacing fix | Pilots **159→172**; stage0 **86→99**; bootstrap **159→172**; sv0c checklist **6/6** unchanged; all ~20 SML modules now have at least one surrogate seed (84 `lib/*_core.sv0` total) | `./scripts/sv0 test-guards`, `./scripts/sv0 test` (172-file self-host loop, 204 unit tests, 99 stage0 goldens, 101 vm-parity goldens — all green) |
| 2026-04-10 (America/Denver) | **`continue-development`** — **fix seeds + integration tests**: lowering.sml `for`-loop `continue` fix + 9 struct-return seed restructures (`let mut out`) | **sv0c:** `sml/ir/lowering.sml` (for-loop increment moved before body), 9× `lib/*_core.sv0` (struct-return restructures), `lib/self-host-sv0-loop.list` (+10), `lib/golden/stage0/*.c` (×9 refreshed), vm-parity goldens (×101 refreshed), `doc/self-host-sv0-loop.md`, `doc/transliteration-plan.md`, `PROGRESS.md`; **meta:** M3 task + progress rollup; YAML `updated:` spacing fix | Pilots **149→159** (= 159/159 — all bootstrap programs now self-host pilots); sv0c checklist **6/6** unchanged | `./scripts/sv0 test-guards`, `./scripts/sv0 test` (159-file self-host loop, 204 unit tests, 101 vm-parity goldens — all green) |
| 2026-04-10 (America/Denver) | **`continue-development`** — **self-host pilots completed** (73 programs added: 1 lib + 4 golden-pass + 7 integration + 61 vm-parity) | **sv0c:** `lib/self-host-sv0-loop.list`, `doc/self-host-sv0-loop.md`, `doc/transliteration-plan.md`, `PROGRESS.md`; **meta:** M3 task + progress rollup; YAML `updated:` spacing fix | Pilots **76→149** / 159 bootstrap; 10 remaining (9 struct C ABI + 1 runtime hang); sv0c checklist **6/6** unchanged | `./scripts/sv0 test-guards`, `./scripts/sv0 test` (149-file self-host loop green) |
| 2026-04-10 (America/Denver) | **`continue-development`** — **C backend transliteration started** (3 seeds: `codegen_emit_value_core`, `codegen_emit_expr_core`, `codegen_emit_instr_core`) | **sv0c:** 3× `lib/codegen_*_core.sv0`, lists, `lib/golden/stage0/*.c` (×3), `doc/transliteration-plan.md`, `lib/LAYOUT.md`, `doc/self-host-sv0-loop.md`, `PROGRESS.md`; **meta:** M3 task + M3 checklist + progress rollup; YAML `updated:` spacing fix in both | Pilots **73→76**; stage0 **83→86**; bootstrap **156→159**; sv0c checklist **6/6** unchanged; first sv0 seeds for `sml/backend/c/codegen.sml` | `./scripts/sv0 test-guards`, `./scripts/sv0 test` |
| 2026-04-10 (America/Denver) | **`thoroughly-execute-document`** — **complete `lowering.sml` transliteration** (12 seeds: `ExprMatch`, `ExprCall`, `ExprTry`, `fnRetCty`, `paramName`, `variantSlots`, `emitStructTd`, `emitEnumTd`, `collectTypedefs`, `buildStructOrder`, `buildEnumVariants`, `lower` entry) | **sv0c:** 12× `lib/lower_*_core.sv0`, lists, `lib/golden/stage0/*.c` (×12), `doc/transliteration-plan.md`, `lib/LAYOUT.md`, `doc/self-host-sv0-loop.md`, `PROGRESS.md`; **meta:** M3 task + M3 checklist + progress rollup | Pilots **61→73**; stage0 **71→83**; bootstrap **144→156**; sv0c checklist **6/6** unchanged; all named `lowering.sml` helpers now seeded (54 `lower_*_core.sv0` total) | `./scripts/sv0 test-guards`, `./scripts/sv0 test` |
| 2026-04-09 (America/Denver) | **`thoroughly-execute-document`** — **`task/sv0-toolchain-milestone-3-self-host.Rmd`** (lowering + formatter/linter design) | **sv0c:** `lib/lower_match_arm_setup_core.sv0`, `lib/lower_enum_construct_instrs_core.sv0`, lists, `lib/golden/stage0/*.c` (×2), `doc/transliteration-plan.md`, `lib/LAYOUT.md`, `doc/self-host-sv0-loop.md`, `doc/tooling-formatter-linter.md`, `PROGRESS.md`; **meta:** M3 task + M3 checklist + progress rollup | Pilots **59→61**; stage0 **69→71**; bootstrap **142→144**; sv0c checklist **6/6** unchanged | `./scripts/sv0 milestone-orient show M3`, `./scripts/sv0 test-guards`, `./scripts/sv0 self-host-check-golden`, `./scripts/sv0 test` |
| 2026-04-09 (America/Denver) | **`continue-development`** — M3 lowering batch (**`lower_quant`**, **`bind_scrut`**, **`match_one_arm`**) | **sv0c:** `lib/lower_quant_core.sv0`, `lib/lower_bind_scrut_core.sv0`, `lib/lower_match_one_arm_core.sv0`, lists, `lib/golden/stage0/*.c` (×3), `doc/transliteration-plan.md`, `lib/LAYOUT.md`, `doc/self-host-sv0-loop.md`, `PROGRESS.md`; **meta:** M3 task + checklist + roadmap + progress | Pilots **56→59**; stage0 **66→69**; bootstrap **139→142**; sv0c checklist **6/6** unchanged | `./scripts/sv0 milestone-orient show M3`, `./scripts/sv0 test-guards`, `./scripts/sv0 test` |
| 2026-04-09 (America/Denver) | **`continue-development`** — M3 **`lower_expr_effect_iv_cluster_core`**; **roadmap** YAML | **sv0c:** new seed, lists, golden, `doc/transliteration-plan.md`, `lib/LAYOUT.md`, `doc/self-host-sv0-loop.md`, `PROGRESS.md` (reverted stray **`transliteration-plan`** IR-row corruption); **meta:** M3 task + checklist + roadmap + progress; **`task/sv0-toolchain-roadmap-full.Rmd`** **`updated:`** spacing | Pilots **55→56**; stage0 **65→66**; bootstrap **138→139**; sv0c checklist **6/6** unchanged | `./scripts/sv0 milestone-orient show M3`, `./scripts/sv0 test-guards`, `./scripts/sv0 test` |
| 2026-04-09 (America/Denver) | **`continue-development`** — M3 **`lower_expr_wi_tv_cluster_core`**; **roadmap** YAML fix | **sv0c:** `lib/lower_expr_wi_tv_cluster_core.sv0`, lists, golden, `doc/transliteration-plan.md`, `lib/LAYOUT.md`, `doc/self-host-sv0-loop.md`, `PROGRESS.md`; **meta:** M3 task + checklist + roadmap + progress run log; **`task/sv0-toolchain-roadmap-full.Rmd`** **`updated:`** space after **`:`** | Pilots **54→55**; stage0 **64→65**; bootstrap **137→138**; sv0c checklist **6/6** unchanged | `./scripts/sv0 milestone-orient show M3`, `./scripts/sv0 test-guards`, `./scripts/sv0 test` |
| 2026-04-09 (America/Denver) | **`continue-development`** — M3 **`lower_stmt_let_init_core`** + **scope note** | **sv0c:** `lib/lower_stmt_let_init_core.sv0`, lists, golden, docs; **meta:** M3 task (**refinement** guardrail), checklist, roadmap, run log | Pilots **53→54**; stage0 **63→64**; bootstrap **136→137**; sv0c checklist **6/6** unchanged | `./scripts/sv0 milestone-orient show M3`, `./scripts/sv0 test-guards`, `./scripts/sv0 test` |
| 2026-04-09 (America/Denver) | **`continue-development`** — **`lower_stmt`** meta **push** + submodule **clean** | **meta:** published **`734db06`** (`git push origin main`); **sv0c:** `git checkout -- doc/transliteration-plan.md` (drop recurring IR-row corruption); **meta:** run log row for **`gh run watch`** | *(no metric change)* | `./scripts/sv0 test-guards`; **`gh run watch 24216458488 --exit-status`** (**success**, ~1m27s) |
| 2026-04-09 (America/Denver) | **`continue-development`** — M3 **`lower_stmt_dispatch_core`** | **sv0c:** `lib/lower_stmt_dispatch_core.sv0`, lists, `lib/golden/stage0/lower_stmt_dispatch_core.c`, `doc/transliteration-plan.md`, `lib/LAYOUT.md`, `doc/self-host-sv0-loop.md`, `PROGRESS.md`; **meta:** submodule + README + M3 task + checklist + roadmap + run log (**`734db06`**) | Pilots **52→53**; stage0 **62→63**; bootstrap **135→136**; sv0c checklist **6/6** unchanged | `./scripts/sv0 milestone-orient show M3`, `./scripts/sv0 test-guards`, `./scripts/sv0 test` |
| 2026-04-09 (America/Denver) | **`continue-development`** — M3 **`lower_body_dispatch_core`** | **sv0c:** `lib/lower_body_dispatch_core.sv0`, lists, `lib/golden/stage0/lower_body_dispatch_core.c`, `doc/transliteration-plan.md`, `lib/LAYOUT.md`, `doc/self-host-sv0-loop.md`, `PROGRESS.md`; **meta:** M3 task + checklist + roadmap + run log | Pilots **51→52**; stage0 **61→62**; bootstrap **134→135**; sv0c checklist **6/6** unchanged | `./scripts/sv0 milestone-orient show M3`, `./scripts/sv0 test-guards`, `./scripts/sv0 test` |
| 2026-04-09 (America/Denver) | **CI fix** — README **sv0c** pin vs submodule (**`64688a3`**) | **meta:** `README.md` SHA **`3da2672`→`9e3cb70`** (match **`git ls-tree HEAD sv0c`**); run log | *(no metric change)* | `./scripts/sv0 test-guards` |
| 2026-04-09 (America/Denver) | **`continue-development`** — M3 **`lower_fn_prefix_core`** | **sv0c:** `lib/lower_fn_prefix_core.sv0`, lists, `lib/golden/stage0/lower_fn_prefix_core.c`, `doc/transliteration-plan.md`, `lib/LAYOUT.md`, `doc/self-host-sv0-loop.md`, `PROGRESS.md`; **meta:** M3 task + checklist + roadmap + run log | Pilots **50→51**; stage0 **60→61**; bootstrap **133→134**; sv0c checklist **6/6** unchanged | `./scripts/sv0 milestone-orient show M3`, `./scripts/sv0 test-guards`, `./scripts/sv0 test` |
| 2026-04-09 (America/Denver) | **`continue-development`** — **`lower_return_body`** integrate; **push** | **sv0c:** commit **`3da2672`**; **meta:** submodule + README + M3/roadmap + run log (**`40e554f`**) | *(same metrics as next row)* | `git push origin main` **failed** (SSH timeout **`github.com:22`**); retry **`sv0c`** then parent |
| 2026-04-09 (America/Denver) | **`continue-development`** — M3 **`lower_return_body_core`** seed | **sv0c:** `lib/lower_return_body_core.sv0`, `lib/bootstrap-sources.list`, `lib/self-host-sv0-loop.list`, `lib/golden/stage0/lower_return_body_core.c`, `doc/transliteration-plan.md`, `lib/LAYOUT.md`, `doc/self-host-sv0-loop.md`, `PROGRESS.md`; **meta:** M3 task + checklist + roadmap snapshot + run log | Self-host pilots **49→50**; stage0 goldens **59→60**; bootstrap-listed lines **132→133**; sv0c checklist **6/6** unchanged | `./scripts/sv0 milestone-orient show M3`, `./scripts/sv0 test-guards`, `./scripts/sv0 test` |
| 2026-04-09 (America/Denver) | **`continue-development`** — **C-6** local integrate + **push blocked** | **sv0c:** commit **`41b4b358`** (C-6 doc + `PROGRESS.md`); **meta:** submodule pointer + `README.md` pin + prior rollup (**`e1f612e`**) | *(metrics same as prior row — already 6/6)* | `./scripts/sv0 test-guards` (**OK**); `git push origin main` (**failed**: SSH timeout / connection closed **`github.com:22`**) |
| 2026-04-09 (America/Denver) | **`continue-development`** — close **sv0c C-6** (SML retirement **guardrail** doc) | **sv0c:** `doc/self-host-sv0-loop.md` (§ SML retirement guardrail), `PROGRESS.md` (**C-6**=1 — policy documented, **not** retirement executed); **meta:** Summary **6/6** + run log | sv0c checklist **5/6→6/6** (**83%→100%**) | `./scripts/sv0 milestone-orient show M3`, `./scripts/sv0 test-guards` |
| 2026-04-09 (America/Denver) | **`continue-development`** — close **sv0c C-5** + fix roadmap YAML | **sv0c:** `doc/self-host-sv0-loop.md` (pilot vs **`SV0_SELF_HOST_COMPILER`**), `PROGRESS.md` (**C-5**=1); **meta:** `task/sv0-toolchain-roadmap-full.Rmd` **`updated:`** YAML space; Summary + run log | sv0c checklist **4/6→5/6** (**67%→83%**) | `./scripts/sv0 milestone-orient show M3`, `./scripts/sv0 test-guards` |
| 2026-04-09 (America/Denver) | **`thoroughly-execute-document`** — `task/sv0-toolchain-roadmap-full.Rmd` | **meta:** roadmap Rmd snapshot + `/ai` resolution; **`01-verify-roadmap-artifacts.sh`** now checks **M0/M1/M2/M3-checklist/MCP** paths; progress run log | Submodule **%** unchanged (sv0c **4/6**); **META-2** roadmap hygiene | `bash task/sv0-toolchain-roadmap-full/01-verify-roadmap-artifacts.sh`, `./scripts/sv0 milestone-orient list`, `./scripts/sv0 test-guards` |
| 2026-04-09 (America/Denver) | **`continue-development`** — close **sv0c C-4** (transliteration plan + LAYOUT living map) | **sv0c:** `doc/transliteration-plan.md` (C-4 contract + snapshot), `lib/LAYOUT.md` (C-4 cross-link), `PROGRESS.md` (**C-4**=1); **meta:** Summary + run log | sv0c checklist **3/6→4/6** (**50%→67%**); snapshot: bootstrap ~132 lines, pilots **49**, stage0 **59**, vm-parity **101** | `./scripts/sv0 milestone-orient show M3`, `./scripts/sv0 test-guards` |
| 2026-04-09 (America/Denver) | **`continue-development`** — close **sv0c C-3** (VM parity v1 allowlist + docs) | **sv0c:** `test/vm-parity/README.md`, `lib/LAYOUT.md`, `PROGRESS.md` (**C-3**=1); **meta:** this rollup Summary + run log | sv0c checklist **2/6→3/6** (**33%→50%**); vm-parity manifest **101** paths (unchanged) | `./scripts/sv0 milestone-orient show M3`, `./scripts/sv0 test-guards` |
| 2026-04-09 (America/Denver) | **`continue-development`** — ship slice (YAML hygiene, stage0 golden EOF, commits); **push blocked** (SSH timeout to `github.com:22` from agent env) | **meta:** task `updated:` YAML spacing; **sv0c:** golden `lower_store_value_to_slot_core.c` trailing newline; local commits on `sv0c` **7b6232f** + **9e0b8e7** | *(metrics unchanged vs prior row)* | `./scripts/sv0 test-guards`, `./scripts/sv0 test` |
| 2026-04-11 (America/Denver) | **`continue-development`** — sv0c checklist **2/6** + **`storeValueToSlot`** seed | **sv0c:** `PROGRESS.md` (**C-1**, **C-2**); `lib/lower_store_value_to_slot_core.sv0`, lists + golden, `doc/transliteration-plan.md`, `lib/LAYOUT.md`, `doc/self-host-sv0-loop.md`; **meta:** M3 task + checklist, rollup Summary | sv0c checklist **0/6→2/6** (**33%**); pilots **48→49**; stage0 **58→59** | `./scripts/sv0 milestone-orient show M3`, `./scripts/sv0 test-guards`, `./scripts/sv0 self-host-check-golden`, `./scripts/sv0 test` |
| 2026-04-10 (America/Denver) | M3 / **`continue-development`** — **`ensGlue`** + **`injectEnsuresAndRetSlot`** harness | **sv0c:** `lib/lower_ens_glue_inject_core.sv0`, lists + `lib/golden/stage0/lower_ens_glue_inject_core.c`, `doc/transliteration-plan.md`, `lib/LAYOUT.md`, `doc/self-host-sv0-loop.md`, `PROGRESS.md`; **meta:** M3 task + checklist `.Rmd` | Self-host pilots **47→48**; stage0 goldens **57→58**; sv0c **0/6** unchanged | `./scripts/sv0 test-guards`, `./scripts/sv0 self-host-check-golden`, `./scripts/sv0 test` |
| 2026-04-09 (America/Denver) | `task/sv0-toolchain-milestone-3-self-host.Rmd` — **`reqInstrs`** harness slice | **sv0c:** `lib/lower_req_instrs_core.sv0`, `lib/bootstrap-sources.list`, `lib/self-host-sv0-loop.list`, `lib/golden/stage0/lower_req_instrs_core.c`, `doc/transliteration-plan.md`, `lib/LAYOUT.md`, `doc/self-host-sv0-loop.md`, `PROGRESS.md`; **meta:** M3 task + M3 checklist `.Rmd` (decomposition table, refinement log, pilot count) | Self-host pilots **46→47**; stage0 goldens **56→57**; sv0c checklist **0/6** unchanged | `./scripts/sv0 test-guards`, `./scripts/sv0 self-host-check-golden`, `./scripts/sv0 test` |
| 2026-04-09 | Integrate milestone-orient + progress ledger; META-2 closure | meta (full diff: `scripts/sv0`, guards, CI, `AGENTS.md`, `task/milestone-orientation.json`, `.cursor/`, `task/sv0-toolchain-workspace.Rmd`); progress rollup + workspace doc sync | Meta checklist **80%→100%** (META-2 verified via `01-verify-roadmap-artifacts.sh`) | `bash task/sv0-toolchain-roadmap-full/01-verify-roadmap-artifacts.sh`, `./scripts/sv0 test-guards` |
| 2026-04-09 | Reverse workspace↔JSON milestone index | `scripts/verify_workspace_milestone_table.py`, `task/milestone-orientation.json`, `task/sv0-toolchain-workspace.Rmd` frontmatter | Table paths must ⊆ JSON `primary_tasks`; optional `workspace_milestone_table_allowlist` | `python3 scripts/verify_workspace_milestone_table.py --root .`, `./scripts/sv0 test-guards` |
| 2026-04-09 | CI: explicit full `sv0 test` + workspace↔JSON drift guard | meta (`.github/workflows/ci.yml`, `scripts/verify_workspace_milestone_table.py`, `task/sv0-toolchain-workspace.Rmd` YAML) | Meta 75%→80% (META-5); forward inclusion JSON→workspace | `python3 scripts/verify_workspace_milestone_table.py --root .`, `./scripts/sv0 test-guards` |
| 2026-04-09 | Milestone orientation guard + Make + CI | meta (`Makefile`, `.github/workflows/ci.yml`, `scripts/verify_milestone_orientation.py`, `task/sv0-toolchain-workspace.Rmd`) | Meta checklist 67%→75% (META-4); `test-guards` includes JSON/path check | `python3 scripts/verify_milestone_orientation.py --root .`, `./scripts/sv0 test-guards` |
| 2026-04-09 | Milestone-driven LLM workflow | meta (`task/`, `scripts/`, `.cursor/`, `AGENTS.md`, `README.md`) | Meta checklist 50%→67% (META-3 done); orient loop documented | `./scripts/sv0 milestone-orient list`, `./scripts/sv0 milestone-orient show M3`, `./scripts/sv0 test-guards` |
| 2026-04-09 (seed) | Introduce hybrid progress ledger | meta, sv0doc, sv0c, sv0vm, sv0-mcp | Meta rollup 0%→50% (META-1); submodule checklists seeded at 0% | *(none — documentation-only seed)* |
| 2026-04-13 (America/Denver) | **`continue-development`** — **Deep-function transliteration: env.sv0 + lexer.sv0.** `env.sv0`: all 6 remaining SML `env.sml` gaps filled (fnArity, tyAlias, registerValueAlias, registerTypeAlias, lookupType, resolveCanonicalTy) — **env.sml parity reached**. `lexer.sv0`: full `tokenize` pipeline (scan_string_end, scan_char_end, scan_number_full with dec/hex/bin/oct + float detection, scan_op_full with 3-char ops, tokenize loop with parallel Vec<i32> output) — **lexer.sml pipeline parity reached**. 9 new test functions total. | **sv0c:** `lib/env.sv0` (+8 fns, +4 tests), `lib/lexer.sv0` (+5 fns, +5 tests), `lib/golden/stage0/env.c` + `lexer.c` (regen), `test/vm-parity/golden/sml/env.sv0b` + `lexer.sv0b` (refreshed), `PROGRESS.md`, `doc/transliteration-plan.md`; **meta:** `README.md` sv0c pin, M3 task (refinement log), this rollup | Metrics unchanged: bootstrap 97, self-host 97, stage0 21, vm-parity 84. **Qualitative:** 2 of 20 real modules reach SML parity (env, lexer); previously 4 were at parity (contract_analyzer, unify, codegen, ast/token/ir/types/span — type definitions). | `./scripts/sv0 compile-run lib/env.sv0`, `./scripts/sv0 compile-run lib/lexer.sv0`, `./scripts/sv0 test-guards`, `./scripts/sv0 test` (all green: 97-file self-host loop, 84 vm-parity, 21 stage0 goldens) |
| 2026-04-13 (America/Denver) | **`continue-development`** — **Deep-function transliteration: resolver.sv0 + parser.sv0 (continued).** `resolver.sv0`: `path_join_vec` (Vec-based generalized path→string), `register_all_intrinsics` (bulk env registration for 17 builtins), import alias management (add/lookup/count). `parser.sv0`: `parse_generic_params` (extracts type param names from `<T, U: Bound>`). Goldens refreshed for both. | **sv0c:** `lib/resolver.sv0` (+7 fns, +3 tests), `lib/parser.sv0` (+1 fn, +1 test), C + VM parity goldens (resolver, parser), `PROGRESS.md`, `doc/transliteration-plan.md`; **meta:** M3 task (refinement log), this rollup | Metrics unchanged: bootstrap 97, self-host 97, stage0 21, vm-parity 84. **Qualitative:** resolver.sv0 now covers generalized path join + env registration + import aliases; parser.sv0 adds real generic param extraction. | `./scripts/sv0 compile-run lib/resolver.sv0`, `./scripts/sv0 compile-run lib/parser.sv0`, `./scripts/sv0 test-guards`, `./scripts/sv0 test` (all green; pre-existing `bytecode_file_layout_core` failure unrelated) |
| 2026-04-13 (America/Denver) | **`continue-development`** — **Deep-function transliteration: checker.sv0 + lowering.sv0 + vm_codegen.sv0.** `checker.sv0`: `path_key_vec` (Vec-based n-segment join), `is_contract_param`, struct/enum name extraction. `lowering.sv0`: `old_slot_name` (full string), complete 6-variant unop table, `param_name_tag`, `uniq_old_names` (Vec dedup). `vm_codegen.sv0`: **fixed `width_of_cty`** (proper layout lookup), enum field helpers, `layout_lookup`. | **sv0c:** `lib/checker.sv0` (+8 fns, +3 tests), `lib/lowering.sv0` (+6 fns, +2 tests, updated unop test), `lib/vm_codegen.sv0` (+7 fns, +3 tests, fixed width_of_cty), C + VM parity goldens (3 modules), `PROGRESS.md`, `doc/transliteration-plan.md`; **meta:** M3 task (refinement log), this rollup | Metrics unchanged: bootstrap 97, self-host 97, stage0 21, vm-parity 84. **Qualitative:** 3 modules deepened; vm_codegen.sv0 width_of_cty **bug fixed** (was non-functional); lowering.sv0 unop table now covers all 6 AST variants. | `./scripts/sv0 compile-run lib/checker.sv0`, `./scripts/sv0 compile-run lib/lowering.sv0`, `./scripts/sv0 compile-run lib/vm_codegen.sv0`, `./scripts/sv0 test-guards`, `./scripts/sv0 test` (all green; pre-existing `bytecode_file_layout_core` failure unrelated) |
| 2026-04-14 (America/Denver) | **`continue-development`** — **Deep-function transliteration batch 6: link.sv0, resolver.sv0, lowering.sv0, checker.sv0.** `link.sv0`: `map_path_segs_needs_mangle`/`map_path_segs_mangle_first` (SML `mapPathSegs`). `resolver.sv0`: `variant_stem`, `enum_variant_reg_path`/`arity`. `lowering.sv0`: import alias infra + `resolve_fn_callee` + `resolve_enum_ctor_path`. `checker.sv0`: `enter_loop`/`exit_loop`. | **sv0c:** `lib/link.sv0` (+2 fns, +1 test), `lib/resolver.sv0` (+3 fns, +2 tests), `lib/lowering.sv0` (+7 fns, +3 tests), `lib/checker.sv0` (+2 fns, updated test), C + VM parity goldens (4 modules), `PROGRESS.md`, `doc/transliteration-plan.md`; **meta:** M3 task (refinement log), this rollup | Metrics unchanged: bootstrap 97, self-host 97, stage0 21, vm-parity 97. **Qualitative:** 4 modules deepened; lowering.sv0 gains import alias resolution (SML `resolveFnCallee`/`resolveEnumCtorPath`); link.sv0 gains path mangling dispatch; resolver.sv0 gains variant registration helpers; checker.sv0 gains loop depth management. | `./scripts/sv0 compile-run lib/link.sv0`, `./scripts/sv0 compile-run lib/resolver.sv0`, `./scripts/sv0 compile-run lib/lowering.sv0`, `./scripts/sv0 compile-run lib/checker.sv0`, `./scripts/sv0 test-guards`, `./scripts/sv0 test` (all green: 97 vm-parity, 21 stage0 goldens, 97-file self-host loop) |
| 2026-04-14 (America/Denver) | **`continue-development`** — **Deep-function transliteration batch 5: checker.sv0, lowering.sv0, vm_codegen.sv0.** `checker.sv0`: `expect` (type unification gate), `fields_of_struct` (struct-def index lookup), `variant_shape_of` (enum+variant search), `assign_lhs_bad` (error sentinel). `lowering.sv0`: `fresh_tmp_str` (string `_sv0t<N>` — SML `freshTmp`), `lower_lit_to_ir_tag` (SML `lowerLit` dispatch), `fn_ret_cty` (SML `fnRetCty`). `vm_codegen.sv0`: `lookup_slot`/`slot_base`/`slot_width` (SML `lookupSlot`), `alloc_local` (SML `allocLocal`), `replace_loop_exit_jump` (SML `replaceLoopExitJump`). 10 new test groups total. | **sv0c:** `lib/checker.sv0` (+4 fns, +4 tests), `lib/lowering.sv0` (+5 fns, +3 tests), `lib/vm_codegen.sv0` (+6 fns, +3 tests), C + VM parity goldens (3 modules), `PROGRESS.md`, `doc/transliteration-plan.md`; **meta:** M3 task (refinement log), this rollup | Metrics unchanged: bootstrap 97, self-host 97, stage0 21, vm-parity 97. **Qualitative:** 3 modules deepened; checker.sv0 gains central `expect`+struct/enum lookup; lowering.sv0 gets string `freshTmp` and literal IR dispatch; vm_codegen.sv0 gains slot env and loop patch utilities. | `./scripts/sv0 compile-run lib/checker.sv0`, `./scripts/sv0 compile-run lib/lowering.sv0`, `./scripts/sv0 compile-run lib/vm_codegen.sv0`, `./scripts/sv0 test-guards`, `./scripts/sv0 test` (all green: 97 vm-parity, 21 stage0 goldens, 97-file self-host loop) |
| 2026-04-12 (America/Denver) | **`continue-development`** — **Deep-function transliteration batch 4: unify.sv0, span.sv0, env.sv0, main.sv0 + VM parity manifest expansion.** `unify.sv0`: `unify_list` (pairwise Vec unification — fixes TyTuple/TyFn/TyNamed from length-only to element-wise). `span.sv0`: `span_merge` + `pos_to_string` + `span_to_string` (SML parity reached for `span.sml`). `env.sv0`: `type_is_registered` (combined prelude ∪ modTys — SML `typeIsRegistered`). `main.sv0`: `base_name_sv0` (strip dir + `.sv0` ext — SML `baseNameSv0`). VM parity manifest: added 12 entries (codegen, link, lexer, resolver, bytecode, vm_codegen, checker, lowering, parser, main, + 3 integration tests). | **sv0c:** `lib/unify.sv0`, `lib/span.sv0`, `lib/env.sv0`, `lib/main.sv0`, C + VM parity goldens (4 modules + 9 manifest-aligned modules), `test/vm-parity/manifest.txt`, `PROGRESS.md`, `doc/transliteration-plan.md`; **meta:** M3 task (refinement log), this rollup | vm-parity **84→97** (+13 files); bootstrap 103, self-host 103, stage0 21. **Qualitative:** span.sml parity reached; unify.sv0 list-wise unification bug fixed; 3 more SML utility gaps closed (type_is_registered, base_name_sv0). | `./scripts/sv0 compile-run lib/unify.sv0`, `./scripts/sv0 compile-run lib/span.sv0`, `./scripts/sv0 compile-run lib/env.sv0`, `./scripts/sv0 compile-run lib/main.sv0`, `./scripts/sv0 test-guards`, `./scripts/sv0 test` (all green: 97 vm-parity, 21 stage0 goldens, 103-file bootstrap) |
| 2026-04-14 (America/Denver) | **`continue-development`** — **Deep-function transliteration batch 7: vm_codegen.sv0, codegen.sv0, diagnostic.sv0, parser.sv0.** `vm_codegen.sv0`: `enum_field_names` (SML `enumFieldNames`), `value_width` (SML `valueWidth`), `bind_param` (SML `bindParam`). `codegen.sv0`: `c_escape_one` control-char parity. `diagnostic.sv0`: `format_header_with_code`, `format_gutter`, `format_snippet_numbered`, `format_related`, `format_help`. `parser.sv0`: `parse_use_item` (SML `parseUseItem`), `parse_module_decl` (SML `parseModuleDecl`). | **sv0c:** `lib/vm_codegen.sv0` (+3 fns, +3 tests), `lib/codegen.sv0` (+1 branch, updated test), `lib/diagnostic.sv0` (+5 fns, +4 tests), `lib/parser.sv0` (+2 fns, +2 tests), C + VM parity goldens (4 modules), `PROGRESS.md`, `doc/transliteration-plan.md`; **meta:** M3 task (refinement log), this rollup | Metrics unchanged: bootstrap 97, self-host 97, stage0 21, vm-parity 97. **Qualitative:** 4 modules deepened; diagnostic.sv0 now covers all SML `format` sub-expressions (codeStr, gutter, related, help); parser.sv0 gains first item-level parse functions; vm_codegen.sv0 gains full `bindParam`/`valueWidth` utilities; codegen.sv0 achieves full `cEscapeString` parity. | `./scripts/sv0 compile-run lib/vm_codegen.sv0`, `./scripts/sv0 compile-run lib/codegen.sv0`, `./scripts/sv0 compile-run lib/diagnostic.sv0`, `./scripts/sv0 compile-run lib/parser.sv0`, `./scripts/sv0 test-guards`, `./scripts/sv0 test` (all green: 97 vm-parity, 21 stage0 goldens, 97-file self-host loop) |
| 2026-04-15 (America/Denver) | **`continue-development`** — **Deep-function transliteration batch 8: bytecode.sv0, checker.sv0, resolver.sv0, lowering.sv0.** `bytecode.sv0`: `encode_strings` (SML `encodeStrings` — string pool byte codec), `encode_string_literals` (handle-list variant), `decode_strings` (SML `decodeStrings` — reverse parse). `checker.sv0`: `infer_lit` (SML `synth` literal arms — literal tag → type tag). `resolver.sv0`: `pat_binds` (SML `patBinds` — collect bound var handles), `bind_pattern_locals` (SML `bindPatternLocals`). `lowering.sv0`: `param_name` (SML `paramName`), `lower_field_index` (SML local `fieldIndex`). | **sv0c:** `lib/bytecode.sv0` (+3 fns, +2 tests), `lib/checker.sv0` (+1 fn, +1 test), `lib/resolver.sv0` (+2 fns, +2 tests), `lib/lowering.sv0` (+2 fns, +2 tests), C + VM parity goldens (4 modules), `PROGRESS.md`, `doc/transliteration-plan.md`; **meta:** M3 task (refinement log), this rollup | Metrics unchanged: bootstrap 97, self-host 97, stage0 21, vm-parity 97. **Qualitative:** 4 modules deepened; bytecode.sv0 gains string-pool encode/decode (first non-trivial wire-format codec); checker.sv0 gains first inference sub-step; resolver.sv0 gains actual variable collection from patterns; lowering.sv0 gains struct-field lookup used in match arms. | `./scripts/sv0 compile-run lib/bytecode.sv0`, `./scripts/sv0 compile-run lib/checker.sv0`, `./scripts/sv0 compile-run lib/resolver.sv0`, `./scripts/sv0 compile-run lib/lowering.sv0`, `./scripts/sv0 test-guards`, `./scripts/sv0 bootstrap-build`, `./scripts/sv0 test` (all green: 97 vm-parity, 21 stage0 goldens, 97-file self-host loop; pre-existing `bytecode_file_layout_core` golden failure is runtime-header issue, not our changes) |
| 2026-04-12 (America/Denver) | **`continue-development`** — **Deep-function transliteration batch 9 + CI fix: link.sv0, checker.sv0, parser.sv0, codegen.sv0.** Fixed CI red (batches 7-8): removed stale `bytecode_file_layout_core` golden test; fixed README sv0c SHA to 40-hex format. Added CI verification invariant to `40-validation-and-proof.mdc`. `link.sv0`: `collect_top_names` (SML `collectTops`), `map_path_segs_rewrite_1`/`_2` (SML `mapPathSegs` rewriting). `checker.sv0`: `ctor_ty_arity` (SML `ctorTy` param count), `struct_field_ty` (SML `tyOfStructField`). `parser.sv0`: `is_assign_tok` (SML `isAssignTok`), `path_of_ty_valid` (SML `pathOfTy`). `codegen.sv0`: `split_main_index` (SML `splitMain`). | **sv0c:** `lib/link.sv0` (+3 fns, +2 tests), `lib/checker.sv0` (+2 fns, +2 tests), `lib/parser.sv0` (+2 fns, +2 tests), `lib/codegen.sv0` (+1 fn, +1 test), C + VM parity goldens (4 modules), `test/test_runner.sml` (removed stale test), `PROGRESS.md`, `doc/transliteration-plan.md`; **meta:** README (40-hex SHA), `.cursor/rules/40-validation-and-proof.mdc` (CI invariant), M3 task (refinement log), this rollup | Metrics unchanged: bootstrap 97, self-host 97, stage0 21, vm-parity 97. **Qualitative:** CI now green; 4 modules deepened; link.sv0 gains full `collectTops` + path rewriting; checker.sv0 gains ctor arity + field type lookup; parser.sv0 gains assignment token predicate; codegen.sv0 gains `splitMain` equivalent. | `./scripts/sv0 compile-run` (4 modules), `./scripts/sv0 test-guards`, `./scripts/sv0 bootstrap-build`, `./scripts/sv0 test` (all green: 308/308 unit tests, 97 vm-parity, 21 stage0 goldens, 97-file self-host loop), `gh run list` + `gh run view` for CI verification |
| 2026-04-15 (America/Denver) | **`continue-development`** — **Deep-function transliteration batch 10: checker.sv0, lowering.sv0, lexer.sv0, vm_codegen.sv0.** `checker.sv0`: `dup_variant_names` (SML `dupVariants`), `struct_enum_name_clash` (SML `initTypes` clash), `named_only_ty` (SML `namedOnlyTy`). `lowering.sv0`: `ast_ty_to_c_string_with_user` (SML `astTyToCString` struct/enum branch). `lexer.sv0`: `scan_escape` (SML `scanEscape` — full decode incl. `\xNN`). `vm_codegen.sv0`: `insn_vec_stride` + `patch_continue_jumps` (SML `patchContinueJumpsFrom`). | **sv0c:** `lib/checker.sv0` (+3 fns, +3 tests), `lib/lowering.sv0` (+1 fn, +1 test), `lib/lexer.sv0` (+1 fn, +1 test), `lib/vm_codegen.sv0` (+2 fns, +1 test), C + VM parity goldens (4 modules), `PROGRESS.md`, `doc/transliteration-plan.md`; **meta:** README sv0c pin, M3 task (refinement log), this rollup | Metrics unchanged: bootstrap 97, self-host 97, stage0 21, vm-parity 97. **Qualitative:** 4 modules deepened; checker.sv0 gains initTypes building blocks (dup/clash/namedOnlyTy); lexer.sv0 adds escape decode function; vm_codegen.sv0 gains continue-jump patching. | `./scripts/sv0 vm-compile` (4 modules), `./scripts/sv0 test-guards`, `./scripts/sv0 test` (all green: 97 vm-parity, 21 stage0 goldens, 97-file self-host loop) |
| 2026-04-17 (America/Denver) | **`continue-development`** — **Deep-function transliteration batch 36: lowering.sv0 lowerFn prereqs + bootstrap workarounds.** `lowering.sv0`: `expr_init_cty` (SML `exprInitCty` — type handle from init expr shape: ExprStruct/ExprPath 2-seg/ExprCall/ExprTuple(1), returns -1 for "int"), `scan_lets` (SML `scanLets` — LetStmt walk collecting name+type handle pairs). Bootstrap workarounds: `scripts/sv0` `ulimit -s` raised to hard limit; `sv0c/doc/bootstrap-compiler-workarounds.md` created; `.cursor/rules/41-sml-process-and-vm-hang-prevention.mdc` updated. | **sv0c:** `lib/lowering.sv0` (+2 fns, +2 tests), `doc/bootstrap-compiler-workarounds.md` (new), `doc/transliteration-plan.md`, `PROGRESS.md`, C + VM parity goldens; **meta:** `scripts/sv0`, `.cursor/rules/41-*`, M3 task (refinement log), this rollup | Metrics unchanged: bootstrap 97, self-host 97, stage0 21, vm-parity 97. lowering.sv0 function count 105→109. **Qualitative:** lowerFn prerequisite AST helpers complete; ensGlue/injectEnsuresAndRetSlot deferred to core IR cluster; bootstrap workarounds now documented and `ulimit` guard active. | `./scripts/sv0 compile-run lib/lowering.sv0`, `./scripts/sv0 vm-compile lib/lowering.sv0`, `./scripts/sv0 test-guards`, `./scripts/sv0 test` (all green: 97 vm-parity, 21 stage0 goldens, 97-file self-host loop) |
| 2026-04-15 (America/Denver) | **`continue-development`** — **Deep-function transliteration batch 11: checker.sv0, lowering.sv0, vm_codegen.sv0.** `checker.sv0`: `fields_of_struct_count` (SML `fieldsOfStruct` field count). `lowering.sv0`: `find_fn_index`/`callee_fn_index` (SML `calleeRetCty` decomposed), `scrut_local_lookup`/`param_ty_lookup` (SML `matchScrutCty` helpers). `vm_codegen.sv0`: `struct_layouts_build` (SML `structLayouts`), `enum_layouts_build` (SML `enumLayouts`). | **sv0c:** `lib/checker.sv0` (+1 fn, +1 test), `lib/lowering.sv0` (+4 fns, +4 tests), `lib/vm_codegen.sv0` (+2 fns, +2 tests), C + VM parity goldens (3 modules), `PROGRESS.md`, `doc/transliteration-plan.md`; **meta:** README sv0c pin, M3 task (refinement log), this rollup | Metrics unchanged: bootstrap 97, self-host 97, stage0 21, vm-parity 97. **Qualitative:** 3 modules deepened; lowering.sv0 gains calleeRetCty/matchScrutCty building blocks; vm_codegen.sv0 gains layout builders from program items. | `./scripts/sv0 vm-compile` (3 modules), `./scripts/sv0 test-guards`, `./scripts/sv0 test` (all green: 97 vm-parity, 21 stage0 goldens, 97-file self-host loop) |
| 2026-04-17 (America/Denver) | **`continue-development`** — **Deep-function transliteration batch 37: checker.sv0, lowering.sv0, diagnostic.sv0.** `checker.sv0`: `ast_ty_to_types_tag` (SML `astTyToTy` primitive dispatch — maps type name strings to `types.sv0` Ty tags). `lowering.sv0`: `try_variant_names_lookup` (SML `tryVariantNames` — full arena lookup with string-based Ok/Err/Some/None detection), `find_variant_in_items` (SML `findVariantAst` — variant flat index within enum's item arena). `diagnostic.sv0`: `format_diagnostic_full` (SML `format` — combined header+location+numbered snippet). | **sv0c:** `lib/checker.sv0` (+1 fn, +1 test), `lib/lowering.sv0` (+2 fns, +2 tests), `lib/diagnostic.sv0` (+1 fn, +1 test), C + VM parity goldens (3 modules), `PROGRESS.md`, `doc/transliteration-plan.md`; **meta:** M3 task (refinement log), this rollup | Metrics unchanged: bootstrap 97, self-host 97, stage0 21, vm-parity 97. **Qualitative:** 3 modules deepened; checker.sv0 gains first `astTyToTy` building block (primitive type name dispatch); lowering.sv0 gains full enum variant name resolution helpers (Result/Option detection + item arena search); diagnostic.sv0 gains complete `format` equivalent combining all sub-formatters. | `./scripts/sv0 compile-run lib/checker.sv0`, `./scripts/sv0 compile-run lib/lowering.sv0`, `./scripts/sv0 compile-run lib/diagnostic.sv0`, `./scripts/sv0 vm-compile lib/checker.sv0`, `./scripts/sv0 vm-compile lib/lowering.sv0`, `./scripts/sv0 vm-compile lib/diagnostic.sv0`, `./scripts/sv0 test-guards`, `./scripts/sv0 test` (all green: 97 vm-parity, 21 stage0 goldens, 97-file self-host loop) |
| 2026-04-17 (America/Denver) | **`continue-development`** — **Deep-function transliteration batch 38: link.sv0, checker.sv0.** `link.sv0`: `strip_link_directives` (SML `stripLinkDirectives` — filtered index Vec), `split_module_name`/`split_module_body_start` (SML `splitModule`). `checker.sv0`: `type_param_push`/`type_param_lookup` (SML `withTypeParams` decomposed — logical-length restore model), `resolve_field_ty_tag` (SML `resolveFieldTy` simplified — parser type arena node to Ty tag with struct/enum name tables + type param scope). | **sv0c:** `lib/link.sv0` (+3 fns, +2 tests), `lib/checker.sv0` (+3 fns, +2 tests), C + VM parity goldens (2 modules), `PROGRESS.md`, `doc/transliteration-plan.md`; **meta:** README sv0c pin, M3 task (refinement log), this rollup | Metrics unchanged: bootstrap 97, self-host 97, stage0 21, vm-parity 97. **Qualitative:** link.sv0 gains program-level operations (directive stripping, module splitting); checker.sv0 gains initTypes prerequisites (type param scope management + field type resolution from parser type arena). | `./scripts/sv0 compile-run lib/link.sv0`, `./scripts/sv0 compile-run lib/checker.sv0`, `./scripts/sv0 vm-compile lib/link.sv0`, `./scripts/sv0 vm-compile lib/checker.sv0`, `./scripts/sv0 test-guards`, `./scripts/sv0 test` (all green: 97 vm-parity, 21 stage0 goldens, 97-file self-host loop) |
| 2026-04-12 (America/Denver) | **`continue-development`** — **Deep-function transliteration batch 39: checker.sv0, lowering.sv0.** `checker.sv0`: `contract_expr_new`/`enter`/`exit`/`active` (SML `withContractExpr` decomposed — scoped "in contract expr" flag via Vec tail), `ty_import_alias_push`/`alias_lookup_bounded` (SML `addTyImportAlias`/`clearTyImportAliases` — scoped push with logical-length restore), `ctor_ty_tag` (SML `ctorTy` — constructor type tag from shape). `lowering.sv0`: `scrut_locals_push`/`scrut_local_lookup_bounded` (SML `withScrutLocals` — push extra scrut bindings with logical-length restore), `lower_alias_replace` (SML `setImportAliases` — bulk alias table fill from parallel Vecs). | **sv0c:** `lib/checker.sv0` (+7 fns, +3 tests), `lib/lowering.sv0` (+3 fns, +2 tests), C + VM parity goldens (2 modules), `PROGRESS.md`, `doc/transliteration-plan.md`; **meta:** README sv0c pin, this rollup | Metrics unchanged: bootstrap 97, self-host 97, stage0 21, vm-parity 97. **Qualitative:** checker.sv0 gains withContractExpr decomposition (scoped flag for contract checking), scoped import alias management, and constructor type classification; lowering.sv0 gains scoped scrut-locals push (for match arm lowering) and bulk alias replacement (for link boundary resets). | `./scripts/sv0 compile-run lib/checker.sv0`, `./scripts/sv0 compile-run lib/lowering.sv0`, `./scripts/sv0 vm-compile lib/checker.sv0`, `./scripts/sv0 vm-compile lib/lowering.sv0`, `./scripts/sv0 test-guards`, `./scripts/sv0 test` (all green: 97 vm-parity, 21 stage0 goldens, 97-file self-host loop) |
| 2026-04-12 (America/Denver) | **`continue-development`** — **Deep-function transliteration batch 40: checker.sv0, lowering.sv0.** `checker.sv0`: `try_success_validate` (SML `trySuccessPayloadTy` — validates enum is valid `?` target: Result Ok/Err or Option Some/None with single-field tuple success variant; returns 1=Result/2=Option/0=not valid/-1=shape mismatch). `lowering.sv0`: `match_scrut_cty` (SML `matchScrutCty` — type handle for match scrutinee via scrut locals, fn params, enum ctor path, or callee return), `variant_shape_in_items` (enum variant shape+field count from item arena flat arrays). | **sv0c:** `lib/checker.sv0` (+1 fn, +1 test), `lib/lowering.sv0` (+2 fns, +2 tests), C + VM parity goldens (2 modules), `PROGRESS.md`, `doc/transliteration-plan.md`; **meta:** README sv0c pin, this rollup | Metrics unchanged: bootstrap 97, self-host 97, stage0 21, vm-parity 97. **Qualitative:** checker.sv0 gains full `?` operator validation (the sv0 equivalent of trySuccessPayloadTy); lowering.sv0 gains match scrutinee type resolution (the sv0 matchScrutCty with scrut locals + fn params probing) and variant shape/field-count lookup from item arena. | `./scripts/sv0 compile-run lib/checker.sv0`, `./scripts/sv0 compile-run lib/lowering.sv0`, `./scripts/sv0 vm-compile lib/checker.sv0`, `./scripts/sv0 vm-compile lib/lowering.sv0`, `./scripts/sv0 test-guards`, `./scripts/sv0 test` (all green: 97 vm-parity, 21 stage0 goldens, 97-file self-host loop) |
| 2026-04-12 (America/Denver) | **`continue-development`** — **Deep-function transliteration batch 41: checker.sv0.** `checker.sv0`: `ast_ty_to_ty_payload` (SML `astTyToTy` full dispatch — type tag + payload output: width for int/uint/float, name handle for struct/enum, type param index for TyVar), `enum_ctor_count` (variant count for an enum by name handle), `item_fn_ret_ty_tag` (function return type resolution from arena index), `binop_result_ty_tag` (Arith→TY_INT, Logic/Cmp→TY_BOOL), `unop_result_ty_tag` (Neg/BitNot→TY_INT, Not→TY_BOOL). | **sv0c:** `lib/checker.sv0` (+5 fns, +4 tests), C + VM parity goldens (1 module), `PROGRESS.md`, `doc/transliteration-plan.md`; **meta:** README sv0c pin, this rollup | Metrics unchanged: bootstrap 97, self-host 97, stage0 21, vm-parity 97. **Qualitative:** checker.sv0 gains full astTyToTy equivalent with payload data (extends resolve_field_ty_tag for itemFnTy/modEnvFromProg prerequisites), enum variant counting, function return type resolution, and synth-level operator result type helpers. | `./scripts/sv0 compile-run lib/checker.sv0`, `./scripts/sv0 vm-compile lib/checker.sv0`, `./scripts/sv0 test-guards`, `./scripts/sv0 test` (all green: 97 vm-parity, 21 stage0 goldens, 97-file self-host loop) |
| 2026-04-12 (America/Denver) | **`continue-development`** — **Deep-function transliteration batch 42: checker.sv0.** `checker.sv0`: `env_entry_index` (SML `envEntry` index-returning lookup — returns Vec index for name or -1), function type table infrastructure (`fn_table_add`/`fn_table_lookup`/`fn_table_ret_type`/`fn_table_param_count`/`fn_table_param_type` — parallel Vec<i32>s storing function signatures: name handles, param counts, return type tags, param type tags flat array with offsets; needed by `modEnvFromProg` and `synth ExprCall`). | **sv0c:** `lib/checker.sv0` (+6 fns, +2 tests), C + VM parity goldens (1 module), `PROGRESS.md`, `doc/transliteration-plan.md`; **meta:** README sv0c pin, this rollup | Metrics unchanged: bootstrap 97, self-host 97, stage0 21, vm-parity 97. **Qualitative:** checker.sv0 gains function type table infrastructure — the key data structure for `modEnvFromProg` function registration and `synth ExprCall` type checking; env_entry_index provides index-based env lookup enabling callers to retrieve both type and mut. | `./scripts/sv0 compile-run lib/checker.sv0`, `./scripts/sv0 vm-compile lib/checker.sv0`, `./scripts/sv0 test-guards`, `./scripts/sv0 test` (all green: 97 vm-parity, 21 stage0 goldens, 97-file self-host loop) |
| 2026-04-12 (America/Denver) | **`continue-development`** — **Deep-function transliteration batches 42–43: checker.sv0.** Batch 42: `env_entry_index` (SML `envEntry` index-returning lookup), function type table infrastructure (`fn_table_add`/`fn_table_lookup`/`fn_table_ret_type`/`fn_table_param_count`/`fn_table_param_type`). Batch 43: `scan_struct_field_names` (token re-scan for initTypes — struct field name extraction from token stream), `scan_enum_variant_names` (enum variant name extraction — handles unit/tuple/struct shapes). | **sv0c:** `lib/checker.sv0` (+8 fns, +4 tests), C + VM parity goldens (1 module), `PROGRESS.md`, `doc/transliteration-plan.md`; **meta:** README sv0c pin, this rollup | Metrics unchanged: bootstrap 97, self-host 97, stage0 21, vm-parity 97. **Qualitative:** checker.sv0 gains function type table (modEnvFromProg prerequisite) + token re-scan infrastructure for struct/enum field name extraction (bypasses parser arena enrichment blocker for initTypes). | `./scripts/sv0 compile-run lib/checker.sv0`, `./scripts/sv0 vm-compile lib/checker.sv0`, `./scripts/sv0 test-guards`, `./scripts/sv0 test` (all green: 97 vm-parity, 21 stage0 goldens, 97-file self-host loop) |
| 2026-04-12 (America/Denver) | **`continue-development`** — **Deep-function transliteration batch 44: checker.sv0.** `checker.sv0`: `type_param_lookup_str` (string-based type param lookup for cross-position name comparison), `scan_type_tag_at` (token-stream type resolver — resolves type annotation at token position to checker type tag; handles primitives, struct/enum via string comparison, type params, IDENT<...> generics, unit `()`, tuples, `&T`/`&mut T`), `scan_struct_field_type_tags` (struct field type tag extraction combining scan_struct_field_names positions with scan_type_tag_at), `scan_fn_param_type_tags` (function parameter type tag extraction — walks fn signature tokens past optional generics through param list; initTypes/modEnvFromProg prerequisite). | **sv0c:** `lib/checker.sv0` (+4 fns, +3 tests), C + VM parity goldens (1 module), `PROGRESS.md`, `doc/transliteration-plan.md`; **meta:** README sv0c pin, this rollup | Metrics unchanged: bootstrap 97, self-host 97, stage0 21, vm-parity 97. **Qualitative:** checker.sv0 gains complete token-stream type resolution infrastructure — scan_type_tag_at is the universal type resolver for the re-scan strategy, enabling both struct field type extraction and function param type extraction without parser modifications; directly unlocks initTypes struct field type tables and modEnvFromProg via itemFnTy param types. | `./scripts/sv0 compile-run lib/checker.sv0`, `./scripts/sv0 vm-compile lib/checker.sv0`, `./scripts/sv0 test-guards`, `./scripts/sv0 test` (all green: 97 vm-parity, 21 stage0 goldens, 97-file self-host loop) |
| 2026-04-12 (America/Denver) | **`continue-development`** — **Deep-function transliteration batch 45: checker.sv0.** `checker.sv0`: `fn_table_lookup_str` (string-based fn table lookup for cross-scope resolution), `scan_fn_ret_type_tag` (token re-scan for function return type — walks past generics and param list), `item_fn_ty_to_table` (SML `itemFnTy` — extract fn type from item arena, register in fn table), `register_all_item_fns` (first part of SML `modEnvFromProg` — walk all ItemFn items, populate fn type table). | **sv0c:** `lib/checker.sv0` (+4 fns, +3 tests), C + VM parity goldens (1 module), `PROGRESS.md`, `doc/transliteration-plan.md`; **meta:** README sv0c pin, this rollup | Metrics unchanged: bootstrap 97, self-host 97, stage0 21, vm-parity 97. **Qualitative:** checker.sv0 gains the modEnvFromProg function registration pipeline — register_all_item_fns walks the item arena, uses scan_fn_param_type_tags + scan_fn_ret_type_tag to extract full function signatures, and populates the fn type table. fn_table_lookup_str enables cross-scope lookups by string. This completes the "ItemFn → fn table" path of modEnvFromProg; builtin registration and enum ctor registration remain. | `./scripts/sv0 compile-run lib/checker.sv0`, `./scripts/sv0 vm-compile lib/checker.sv0`, `./scripts/sv0 test-guards`, `./scripts/sv0 test` (all green: 97 vm-parity, 21 stage0 goldens, 97-file self-host loop) |
| 2026-04-17 (America/Denver) | **`continue-development`** — **Deep-function transliteration batch 46: checker.sv0.** `checker.sv0`: `scan_enum_variant_shapes` (token re-scan — classify variant as VSUnit/VSTuple/VSStruct from next-token), `init_struct_defs` (SML `initTypes` struct half — walks ItemStruct items, populates struct def table with field names+types via token re-scan), `init_enum_defs` (SML `initTypes` enum half — walks ItemEnum items, populates enum def table with variant names+shapes), `struct_def_field_ty_str` (string-based field type lookup in struct def table), `enum_def_variant_shape_str` (string-based variant shape lookup in enum def table); **bugfix**: `scan_enum_variant_names` now handles `(...)` tuple variant payloads (tag 6/7). | **sv0c:** `lib/checker.sv0` (+5 fns, +3 tests, +1 bugfix), C + VM parity goldens (1 module), `PROGRESS.md`, `doc/transliteration-plan.md`; **meta:** README sv0c pin, this rollup | Metrics unchanged: bootstrap 97, self-host 97, stage0 21, vm-parity 97. **Qualitative:** checker.sv0 gains the core of SML `initTypes` — struct and enum definition tables populated from the item arena using token re-scan, with string-based field/variant lookup; scan_enum_variant_names bugfix enables proper handling of tuple variant `(...)` payloads; struct_def_field_ty_str and enum_def_variant_shape_str provide runtime lookup into the def tables. Next: builtin registration, enum ctor registration, synth ExprCall. | `./scripts/sv0 compile-run lib/checker.sv0`, `./scripts/sv0 vm-compile lib/checker.sv0`, `./scripts/sv0 test-guards`, `./scripts/sv0 test` (all green: 97 vm-parity, 21 stage0 goldens, 97-file self-host loop) |
| 2026-04-17 (America/Denver) | **`continue-development`** — **Deep-function transliteration batch 47: checker.sv0.** `checker.sv0`: `builtin_fn_lookup` (maps 13 builtin name strings to integer IDs — println, string_len/eq/concat/char_at/substr, vec_new/push/len/get/set, box_new/deref), `builtin_fn_ret_type`/`builtin_fn_param_count`/`builtin_fn_param_type` (builtin type signature accessors; TY_VAR for polymorphic positions like vec_get return), `BUILTIN_COUNT` (constant 13), `resolve_fn_call` (unified user fn + builtin lookup — checks fn_table_lookup_str first, falls through to builtin_fn_lookup; outputs is_builtin flag). | **sv0c:** `lib/checker.sv0` (+6 fns, +2 tests), C + VM parity goldens (1 module), `PROGRESS.md`, `doc/transliteration-plan.md`; **meta:** README sv0c pin, this rollup | Metrics unchanged: bootstrap 97, self-host 97, stage0 21, vm-parity 97. **Qualitative:** checker.sv0 gains the builtin function registration from modEnvFromProg — all 13 SML builtins mapped to integer IDs with full type signature accessors; resolve_fn_call provides unified function lookup for synth ExprCall. Next: enum constructor registration, then synth ExprCall. | `./scripts/sv0 compile-run lib/checker.sv0`, `./scripts/sv0 vm-compile lib/checker.sv0`, `./scripts/sv0 test-guards`, `./scripts/sv0 test` (all green: 97 vm-parity, 21 stage0 goldens, 97-file self-host loop) |
| 2026-04-17 (America/Denver) | **`continue-development`** — **Deep-function transliteration batch 48: checker.sv0.** `checker.sv0`: `edef_name_lookup_str` (string-based enum def table name lookup — returns edef index or -1), `ctor_type_from_edef` (enum ctor type from def table — variant shape→TY_ENUM for VSUnit, TY_FN for VSTuple/VSStruct), `resolve_ctor_path_ty` (2-segment path → ctor type tag — extracts enum+variant name strings from token positions, resolves via edef table — synth ExprPath building block for enum constructor references). | **sv0c:** `lib/checker.sv0` (+3 fns, +1 test), C + VM parity goldens (1 module), `PROGRESS.md`, `doc/transliteration-plan.md`; **meta:** README sv0c pin, this rollup | Metrics unchanged: bootstrap 97, self-host 97, stage0 21, vm-parity 97. **Qualitative:** checker.sv0 gains enum constructor type resolution — edef_name_lookup_str provides string-based enum lookup, ctor_type_from_edef combines edef table shape lookup with ctor type tag classification, resolve_ctor_path_ty provides the complete 2-segment path resolution needed by synth ExprPath. Combined with batch 47's resolve_fn_call, function and constructor resolution for synth ExprCall/ExprPath are now largely in place. Next: ItemUse processing (oneUse), then synth ExprCall/ExprPath. | `./scripts/sv0 compile-run lib/checker.sv0`, `./scripts/sv0 vm-compile lib/checker.sv0`, `./scripts/sv0 test-guards`, `./scripts/sv0 test` (all green: 97 vm-parity, 21 stage0 goldens, 97-file self-host loop) |
| 2026-04-17 (America/Denver) | **`continue-development`** — **Deep-function transliteration batch 49: checker.sv0.** `checker.sv0`: `sdef_name_lookup_str` (string-based struct def table name lookup — mirrors edef_name_lookup_str; returns sdef index or -1), `env_lookup_str` (string-based env variable lookup — walks env_names backward, extracts string at each token position via string_substr, compares with target string; needed for cross-scope name resolution in synth ExprPath/ExprCall), `synth_call_ret_type` (function call return type — resolves callee name string via fn_table/builtins using resolve_fn_call, returns return type tag; also fixed is_builtin_out read to use last element). | **sv0c:** `lib/checker.sv0` (+3 fns, +1 test), C + VM parity goldens (1 module), `PROGRESS.md`, `doc/transliteration-plan.md`; **meta:** README sv0c pin, this rollup | Metrics unchanged: bootstrap 97, self-host 97, stage0 21, vm-parity 97. **Qualitative:** checker.sv0 gains three synth expression dispatch building blocks — sdef_name_lookup_str enables struct literal type checking, env_lookup_str enables cross-scope variable resolution, synth_call_ret_type provides the return type for any resolvable function call. Combined with previous batches, the checker now has full function/builtin/ctor resolution infrastructure. Next: synth expression dispatch skeleton, checkStmt. | `./scripts/sv0 compile-run lib/checker.sv0`, `./scripts/sv0 vm-compile lib/checker.sv0`, `./scripts/sv0 test-guards`, `./scripts/sv0 test` (all green: 97 vm-parity, 21 stage0 goldens, 97-file self-host loop) |
| 2026-04-17 (America/Denver) | **`continue-development`** — **Deep-function transliteration batch 50: checker.sv0.** `checker.sv0`: `synth_expr` (SML `synth` expression dispatch — recursive type synthesis over expr arena; handles ExprLit via infer_lit, ExprPath 1-seg via env_lookup_str, ExprPath 2-seg via resolve_ctor_path_ty for enum constructors, ExprUnop via unop_result_ty_tag, ExprBinop via binop_result_ty_tag; returns -1 for unimplemented forms). **Bugfix**: `infer_lit` now returns checker TY_* constants (TY_INT=0, TY_BOOL=3, TY_UNIT=2, TY_STRING=5, TY_CHAR=4) instead of types.sv0 enum ordinals (BoolLit→2=TY_UNIT wrong, UnitLit→4=TY_CHAR wrong, CharLit→1=TY_UINT wrong); latent inconsistency uncovered by synth integration. | **sv0c:** `lib/checker.sv0` (+1 fn, +1 test, +1 bugfix), C + VM parity goldens (1 module), `PROGRESS.md`, `doc/transliteration-plan.md`; **meta:** README sv0c pin, this rollup | Metrics unchanged: bootstrap 97, self-host 97, stage0 21, vm-parity 97. **Qualitative:** checker.sv0 gains the first synth_expr dispatch — the core expression type synthesis function that recursively processes the expr arena. Currently handles 4 of ~27 expression tags (Lit, Path, Unop, Binop). The infer_lit bugfix ensures type tag consistency across the checker. Next: extend synth_expr with ExprIf, ExprCall, ExprBlock. | `./scripts/sv0 compile-run lib/checker.sv0`, `./scripts/sv0 vm-compile lib/checker.sv0`, `./scripts/sv0 test-guards`, `./scripts/sv0 test` (all green: 97 vm-parity, 21 stage0 goldens, 97-file self-host loop) |
| 2026-04-17 (America/Denver) | **`continue-development`** — **Deep-function transliteration batches 48–51: checker.sv0.** Batch 48: `edef_name_lookup_str`, `ctor_type_from_edef`, `resolve_ctor_path_ty` (enum ctor path → type tag). Batch 49: `sdef_name_lookup_str`, `env_lookup_str`, `synth_call_ret_type` (synth building blocks). Batch 50: `synth_expr` skeleton (ExprLit/Path/Unop/Binop — 4 tags) + **bugfix** `infer_lit` now returns checker TY_* constants. Batch 51: extend `synth_expr` with ExprIf/ExprReturn/ExprBreak/ExprContinue (8 of ~27 tags), adding `ret_ty` + `loop_depth` parameters. | **sv0c:** `lib/checker.sv0` (+10 fns, +4 tests, +2 bugfixes), C + VM parity goldens, `PROGRESS.md`, `doc/transliteration-plan.md`; **meta:** README sv0c pin (4 updates), this rollup | Metrics unchanged: bootstrap 97, self-host 97, stage0 21, vm-parity 97. **Qualitative:** checker.sv0 gains full enum ctor type resolution, synth building blocks, and the first `synth_expr` dispatch handling 8 expression forms. The infer_lit bugfix resolves a latent type tag inconsistency. Next: extend synth_expr with ExprWhile/ExprFor/ExprLoop (loops), ExprCall, ExprField, ExprStruct. | `./scripts/sv0 compile-run lib/checker.sv0`, `./scripts/sv0 vm-compile lib/checker.sv0`, `./scripts/sv0 test-guards`, `./scripts/sv0 test` (all green: 97 vm-parity, 21 stage0 goldens, 97-file self-host loop) |
| 2026-04-17 (America/Denver) | **`continue-development`** — **Deep-function transliteration batches 52–53: checker.sv0.** Batch 52: `synth_expr` extended with ExprWhile (loop_depth+1), ExprLoop, ExprAssign, ExprAssignOp (12 tags). Batch 53: `synth_expr_call_simple` (SML `synthExprCall` simplified — 1-seg path fn_table/builtin or 2-seg enum ctor; synthesises args; returns ret type), `synth_expr` extended with ExprTuple (1-elem unwrap) and ExprAssert (14 tags + call helper). | **sv0c:** `lib/checker.sv0` (+1 fn, +4 synth_expr arms, +7 test assertions), C + VM parity goldens, `PROGRESS.md`, `doc/transliteration-plan.md`; **meta:** README sv0c pin, this rollup | Metrics unchanged: bootstrap 97, self-host 97, stage0 21, vm-parity 97. **Qualitative:** synth_expr now handles 14 of ~27 expression tags (Lit, Path, Unop, Binop, If, Return, Break, Continue, While, Loop, Assign, AssignOp, Assert, Tuple) plus a dedicated call helper. Remaining tags needing type payload tracking: ExprField, ExprStruct, ExprMatch, ExprFor, ExprBlock, ExprCall (full), ExprCast, ExprTry, ExprMethodCall, ExprIndex. | `./scripts/sv0 compile-run lib/checker.sv0`, `./scripts/sv0 vm-compile lib/checker.sv0`, `./scripts/sv0 test-guards`, `./scripts/sv0 test` (all green: 97 vm-parity, 21 stage0 goldens, 97-file self-host loop) |
| 2026-04-12 (America/Denver) | **`continue-development`** — **Deep-function transliteration batch 54: checker.sv0.** `checker.sv0`: `check_stmt_in_block` (SML `checkStmt` — processes LetStmt: synth init expression + extend env with name/type/mut; SemiStmt: synth expression + discard type). `synth_expr` extended with ExprBlock (tag 9: iterate stmts via check_stmt_in_block, synth tail or return TY_UNIT). Added `env_muts` and `ed4` parameters to `synth_expr`/`synth_expr_call_simple` for proper let binding mutability tracking and LetStmt d4 access. synth_expr now handles 15 of ~27 expression tags. | **sv0c:** `lib/checker.sv0` (+1 fn, +1 synth_expr arm, +2 test assertions, +2 params threaded through 20 call sites), C + VM parity goldens (1 module), `PROGRESS.md`, `doc/transliteration-plan.md`; **meta:** README sv0c pin, this rollup | Metrics unchanged: bootstrap 97, self-host 97, stage0 21, vm-parity 97. **Qualitative:** checker.sv0 gains ExprBlock processing — the most common compound expression form — via check_stmt_in_block (LetStmt extends env, SemiStmt discards type). env_muts+ed4 parameter threading enables proper mutability and stmt d4 access across the synth recursion tree. | `./scripts/sv0 compile-run lib/checker.sv0`, `./scripts/sv0 vm-compile lib/checker.sv0`, `./scripts/sv0 test-guards`, `./scripts/sv0 test` (all green: 97 vm-parity, 21 stage0 goldens, 97-file self-host loop) |
| 2026-04-12 (America/Denver) | **`continue-development`** — **Deep-function transliteration batch 55: checker.sv0.** `synth_expr` extended with ExprFor (tag 13: synth range lo/hi, extend env with loop variable as TY_INT immutable, synth body with loop_depth+1, return TY_UNIT), ExprCast (tag 20: synth source, return TY_INT simplified), ExprTry (tag 22: synth operand, return TY_INT simplified). synth_expr now handles 18 of ~27 expression tags. | **sv0c:** `lib/checker.sv0` (+3 synth_expr arms, +3 test assertions), C + VM parity goldens (1 module), `PROGRESS.md`, `doc/transliteration-plan.md`; **meta:** README sv0c pin, this rollup | Metrics unchanged: bootstrap 97, self-host 97, stage0 21, vm-parity 97. **Qualitative:** three more synth_expr tags implemented. ExprFor is the first tag that combines env extension with loop-depth tracking. ExprCast and ExprTry use simplified return types (TY_INT placeholder) until type arena / enum variant payload infrastructure is threaded into synth_expr. Remaining ~9 tags: ExprCall, ExprMethodCall, ExprField, ExprTupleField, ExprIndex, ExprMatch, ExprRange, ExprStruct, ExprArray. | `./scripts/sv0 compile-run lib/checker.sv0`, `./scripts/sv0 vm-compile lib/checker.sv0`, `./scripts/sv0 test-guards`, `./scripts/sv0 test` (all green: 97 vm-parity, 21 stage0 goldens, 97-file self-host loop) |
| 2026-04-12 (America/Denver) | **`continue-development`** — **Deep-function transliteration batch 56: checker.sv0.** `synth_expr` extended with ExprCall (tag 4: delegate to synth_expr_call_simple), ExprIndex (tag 8: synth obj+index, return TY_INT simplified), ExprRange (tag 21: synth lo/hi, return TY_INT). Added `fn_names`+`fn_ret_types` params to `synth_expr`/`check_stmt_in_block` — threaded through ~27 recursive call sites. synth_expr now handles 21 of ~27 expression tags. | **sv0c:** `lib/checker.sv0` (+3 synth_expr arms, +2 new params on synth_expr/check_stmt_in_block, +6 test assertions), C + VM parity goldens (1 module), `PROGRESS.md`, `doc/transliteration-plan.md`; **meta:** README sv0c pin, this rollup | Metrics unchanged: bootstrap 97, self-host 97, stage0 21, vm-parity 97. **Qualitative:** the single most impactful tag — ExprCall — is now integrated, meaning synth_expr can fully recurse through expressions containing function calls. fn_names/fn_ret_types threading is the largest parameter-passing change so far (~27 call sites). Remaining ~6 tags: ExprMethodCall, ExprField, ExprTupleField, ExprMatch, ExprStruct, ExprArray. | `./scripts/sv0 compile-run lib/checker.sv0`, `./scripts/sv0 vm-compile lib/checker.sv0`, `./scripts/sv0 test-guards`, `./scripts/sv0 test` (all green: 97 vm-parity, 21 stage0 goldens, 97-file self-host loop) |
| 2026-04-12 (America/Denver) | **`continue-development`** — **Deep-function transliteration batch 57: checker.sv0.** `synth_expr` extended with ExprMethodCall (tag 5: synth obj + args, return TY_INT simplified), ExprField (tag 6: synth obj, return TY_INT), ExprTupleField (tag 7: synth obj, return TY_INT). synth_expr now handles 24 of ~27 expression tags. | **sv0c:** `lib/checker.sv0` (+3 synth_expr arms, +3 test assertions), C + VM parity goldens (1 module), `PROGRESS.md`, `doc/transliteration-plan.md`; **meta:** README sv0c pin, this rollup | Metrics unchanged: bootstrap 97, self-host 97, stage0 21, vm-parity 97. **Qualitative:** synth_expr near-complete — 24 of 27 tags handled. Remaining 3 (ExprMatch, ExprStruct, ExprArray) require pattern binding infrastructure or non-contiguous arena traversal. All simplified tags return TY_INT; full field/method type resolution deferred until sdef_* tables are threaded. | `./scripts/sv0 compile-run lib/checker.sv0`, `./scripts/sv0 vm-compile lib/checker.sv0`, `./scripts/sv0 test-guards`, `./scripts/sv0 test` (all green: 97 vm-parity, 21 stage0 goldens, 97-file self-host loop) |
| 2026-04-12 (America/Denver) | **`continue-development`** — **Deep-function transliteration batch 58: checker.sv0.** `synth_expr` **complete** — all 27 expression tags now handled. ExprMatch (tag 11: synth scrutinee + arm guards/bodies, return first arm type), ExprStruct (tag 24: return TY_INT simplified), ExprArray (tag 26: return TY_INT simplified). **Milestone:** synth_expr is the core expression type inference engine; completing all tags means the checker can traverse arbitrary expression trees. | **sv0c:** `lib/checker.sv0` (+3 synth_expr arms, +3 test assertions, +6 lines in doc comment), C + VM parity goldens (1 module), `PROGRESS.md`, `doc/transliteration-plan.md`; **meta:** README sv0c pin, this rollup | Metrics unchanged: bootstrap 97, self-host 97, stage0 21, vm-parity 97. **Qualitative:** synth_expr feature-complete. Next work: `checkFn` (function-level checking), `initTypes` (struct/enum registration into type env), `modEnvFromProg` (program-level orchestration). These compose synth_expr with the infrastructure already built (fn_table, struct/enum def tables, env management) to achieve end-to-end type checking. | `./scripts/sv0 compile-run lib/checker.sv0`, `./scripts/sv0 vm-compile lib/checker.sv0`, `./scripts/sv0 test-guards`, `./scripts/sv0 test` (all green: 97 vm-parity, 21 stage0 goldens, 97-file self-host loop) |
| 2026-04-12 (America/Denver) | **`continue-development`** — **Deep-function transliteration batches 59–61: checker.sv0.** Batch 59: `scan_fn_param_names` (fn param name extraction), `check_fn_body` (SML `checkFn` core — function-level type checking). Batch 60: `process_item_use` (SML `oneUse` — ItemUse env import). Batch 61: `build_mod_env` (SML `modEnvFromProg` — register all ItemFn as TY_FN + process all ItemUse via `process_item_use`). | **sv0c:** `lib/checker.sv0` (+4 fns, +4 tests), C + VM parity goldens (1 module), `PROGRESS.md`, `doc/transliteration-plan.md`; **meta:** README sv0c pin (3 updates), this rollup | Metrics unchanged: bootstrap 97, self-host 97, stage0 21, vm-parity 97. **Qualitative:** checker.sv0 gains the full `modEnvFromProg` equivalent — `build_mod_env` orchestrates function registration and ItemUse processing, building the module environment for function-level checking. Combined with `check_fn_body`, `synth_expr` (all 27 tags), fn_table, builtin infrastructure, struct/enum def tables, and `init_struct_defs`/`init_enum_defs`, the checker now has all components for end-to-end type checking except the per-fn body re-parse loop. Next: shift to other modules (lowering.sv0 deep functions) or implement the cross-module check loop. | `./scripts/sv0 compile-run lib/checker.sv0`, `./scripts/sv0 vm-compile lib/checker.sv0`, `./scripts/sv0 test-guards`, `./scripts/sv0 test` (all green: 97 vm-parity, 21 stage0 goldens, 97-file self-host loop) |
| 2026-04-18 (America/Denver) | **`continue-development`** — **Deep-function transliteration batches 62–64: lowering.sv0.** Batch 62: local IR enum types + tag accessors + `ctr_fresh` + `classify_assign_lhs_arena`. Batch 63: `lower_lit_to_ir_value` + `lower_expr_to_value` first 6 cases. Batch 64: `lower_expr_to_value` extended to 17 of ~27 AST forms — ExprReturn (ReturnVal/ReturnVoid), ExprBlock (inline LetStmt/SemiStmt + tail), ExprIf (with/without else), ExprCall (1-seg general), ExprAssign, ExprAssignOp, Borrow/BorrowMut→VAddrOf, While/For/Loop/Break/Continue stubs. | **sv0c:** `lib/lowering.sv0` (+11 fns batch 62–64, +8 tests), C + VM parity goldens (1 module), `PROGRESS.md`, `doc/transliteration-plan.md`; **meta:** README sv0c pin (3 updates), this rollup | Metrics unchanged: bootstrap 97, self-host 97, stage0 21, vm-parity 97. **Qualitative:** `lower_expr_to_value` now handles 17 of 27 AST expression forms. Remaining ~10: ExprCall builtins/2-seg ctor, ExprMatch, ExprStruct, ExprCast, ExprTry, ExprMethodCall. Then `lowerExprWithInstrs`/`lowerStmt`/`lowerFn`/`lower`. | `./scripts/sv0 compile-run lib/lowering.sv0`, `./scripts/sv0 vm-compile lib/lowering.sv0`, `./scripts/sv0 test-guards`, `./scripts/sv0 test` (all green: 97 vm-parity, 21 stage0 goldens, 97-file self-host loop) |
| *(add the next row above this line)* | | | | |

## Progress contract (for agents)

A session **improves tracking** if at least one holds:

1. **Measurable:** `Done` increases for some item, or `Total` changes with an explicit rationale (smaller scope or newly discovered work).
2. **Explainable:** a new **run log** row records a **blocker**, **recon-only** outcome, or **decomposition** update with the next concrete step.

Cosmetic rewording without a run log entry does **not** satisfy the contract.

## Pointers

- LLM milestone loop + anti-patterns: `AGENTS.md`, `.cursor/rules/37-llm-milestone-driven-workflow.mdc`, `task/milestone-orientation.json`, `./scripts/sv0 milestone-orient`
- Milestone bar and decomposition: `task/sv0-toolchain-milestone-3-self-host.Rmd`, `task/sv0-toolchain-milestone-3-checklist.Rmd`
- Roadmap index: `task/sv0-toolchain-roadmap-full.Rmd`
- Cursor commands: `.cursor/commands/continue-development.md`, `.cursor/commands/thoroughly-execute-document.md`
