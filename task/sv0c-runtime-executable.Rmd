---
created: "2026-08-23T00:00:00-06:00"
updated: "2026-08-23T00:00:00-06:00"
id: task-sv0c-runtime-executable
key: sv0c-runtime-executable
state: active
tags:
  - sv0
  - sv0c
  - native-executable
  - c-backend
  - host-driver
  - planning
title: "sv0c native runtime executable output: delivery plan (F0 → R1)"
type: task
---

# sv0c native runtime executable output: delivery plan

## purpose

Plan — **not yet implement** — the missing production step after the sv0 C
backend: a thin host driver that turns the C translation unit `sv0c` already
emits into a durable, runnable, host-native executable, governed by the
specification at `~/Documents/project-specs/sv0c-runtime-executable/SPEC.md`
(source-audit date 2026-08-23; audited revisions: `sv0-toolchain@f46144f`,
`sv0c@0705d68`, `sv0doc@f4dabf6`, `sv0vm@e9698f6`, `sv0-mcp@6f38d20`). This
file is the **hub**: scope, architecture, release ladder, phase sequencing,
gates, risks, and open decisions. The trackable **`NEX-###`** slice backlog
lives in **`task/sv0c-runtime-executable-checklist.Rmd`**, using the exact
slice IDs already assigned by the spec's own implementation backlog (§31), so
citing an ID in a PR is directly traceable back to the governing document.

This feature is **owned by `sv0c`** (the C backend, entry ABI, and runtime
bundle) with a thin `sv0-toolchain` driver adapter — it is **not** part of the
active M5 milestone (`task/sv0-toolchain-milestone-5-llvm-crypto.Rmd`). The
spec is explicit that the LLVM path is separate future work and **must not**
silently redefine this C-backend contract (spec §4.5, ownership table §3.3,
decision register note in §33.1). No `roadmap_parent` is set for the same
reason the VS Code extension hub omits one — this is adjacent to, not nested
under, the numbered design-milestone roadmap.

## where we start (2026-08-23)

A source audit (spec §4) found real capability but no production feature:

- **What already works:** the self-hosted compiler emits one C translation
  unit beginning `#include "sv0_runtime.h"`; `sv0_runtime.h`/`sv0_runtime.c`
  cover panic/assert/contract/string/vector/box/filesystem support; single-file
  and recursive `--project` composition both work; contract modes
  `runtime`/`verified`/`disabled` all lower correctly on the C path; a repeated
  working link recipe (`cc -std=c99 -O0 -I<runtime> program.c sv0_runtime.c -o
  program`) is proven by `make e2e test-contract-runtime` at the pinned
  revision on macOS arm64 with Apple Clang (42 / precondition-1 confirmed).
- **What does not exist:** `./scripts/sv0 compile-run` (`scripts/sv0:1088`) is
  the *only* thing resembling this feature today, and it is explicitly a test
  convenience, not the spec'd driver — it accepts only `sv0c/`-relative paths,
  writes into a temp dir, **deletes the binary after running it**, treats any
  nonzero program exit as a failed *compile* test, suppresses emitter stderr,
  hard-codes `-O0`, and has no stable output path, profile, or provenance
  contract (spec §4.2). There is no `--emit=exe`, no atomic publication, no
  entry-point ABI validation, no compiler-relative runtime resolution, no
  argv-safe hostile-path handling, and no structured exit-class taxonomy.
- **The concurrency blocker:** the current native compiler wrapper depends on
  a single global mutable file, `/tmp/.sv0_drv_path` (also called out in
  `[[feedback_drv_path_reset]]` — it must be reset to empty before any
  gate/push, and build scripts must never run concurrently with a pre-push
  `sv0 test` for exactly this reason). The spec makes retiring this dependency
  from the *stable* path a hard release blocker (AE-008, REL-004) — F0/R0 MAY
  serialize legacy invocations behind a lock as a migration shim, but that
  lock is not R0 completion.
- **C portability is honest, not strict.** `sv0_runtime.h` uses GCC/Clang
  extensions (`__typeof__`, statement expressions); `sv0_runtime.c` uses POSIX
  directory/file APIs. R0 is therefore a hosted POSIX, GCC/Clang-compatible
  build — it explicitly does not claim strict C99 or native Windows support
  merely because the generated source resembles C99 (spec §4.3, §20.3).

## scope decisions (locked by the spec, §10 + §33.1)

1. **Host-driver phase after C emission (AE-001).** The IR emitter never
   learns shell syntax, process spawning, or platform packaging; a thin driver
   invokes the core compiler and the host C compiler and owns artifacts.
2. **One generated `program.c` + the compiler-matched `sv0_runtime.c`, linked
   in a single host-compiler invocation through R1 (AE-002).** Separate
   sv0-module compilation, a runtime archive, LTO, and foreign libraries are
   deferred.
3. **Existing C-stdout and `--target=vm` behavior stays compatible; new
   executable output is opt-in via `--emit=exe`** (or the workspace adapter
   `./scripts/sv0 native-compile` during development) (AE-003).
4. **Explicit executable-entry adapter (AE-004).** The C backend must not emit
   a nonconforming `void`/aggregate/parameterized `main` directly as the
   hosted C entry, and must not fabricate a successful `main` for a source
   library that has none.
5. **Compiler-relative runtime discovery only (AE-005).** The driver resolves
   `sv0_runtime.h`/`.c` from the compiler installation, never from cwd,
   `CPATH`, or the source project — a malicious project-local
   `sv0_runtime.h` must never win.
6. **Atomic final publication (AE-006).** The host compiler writes to a
   private temporary target; only a validated, successfully linked regular
   executable is renamed over the requested final path. A failed rebuild
   leaves the prior executable byte-for-byte unchanged.
7. **Absolute bytecode isolation (AE-007).** `--target=vm`, `.sv0b` input, or
   VM-emission requests combined with `--emit=exe` are usage errors; `sv0vm`
   is never invoked by this path.
8. **No production global control file (AE-008).** The stable path passes
   inputs through ordinary argv or a unique per-invocation private channel;
   `/tmp/.sv0_drv_path` may remain inside legacy bootstrap tests only.
9. **Host-native only (AE-009).** Target triples, sysroots, cross-linkers, and
   remote builders are rejected or deferred rather than guessed.
10. **Development profile first; `--profile=release` is blocked until R1**
    optimized/sanitizer/UB-parity gates pass. No arbitrary `CFLAGS`/`LDFLAGS`
    shell strings in R0 (avoids quoting ambiguity and irreproducibility).

## explicit non-goals through R1 (spec §7.5)

`.sv0b`-to-executable conversion, bundling `sv0vm` into a launcher, LLVM/Wasm
executable construction, cross-compilation, universal/fat binaries, static
linking as a supported guarantee, shared-library/object/header SDK output,
incremental compilation or caching, a package registry, foreign-library
discovery (`pkg-config`, C FFI manifests), user `fn main` arguments,
source-level native debugging, code signing/notarization/installers, and
native Windows support before the runtime and driver are ported and gated.
These exclusions do not block a later spec — they keep untested behavior out
of this release implicitly.

## release ladder

| release | meaning | gate (spec §29) |
|---|---|---|
| **F0** | Feasibility + contract-locking slice; CLI grammar, phase/artifact state machines, executable-entry validation, discovery contracts, an argv-safe fake-tool harness, one minimal `-O0` proof; no public stability claim. | §29.1 |
| **R0** | Preview: durable dev-profile executables for single-file and `--project` inputs on the macOS-arm64/Linux-x86_64 matrix, atomic publication, human diagnostics with stable exit classes, behavior/project-corpus coverage. | §29.2 |
| **R0.1** | Hardened preview: `--emit=c`/`--keep-c` retention, `sv0.toml` config, `--message-format=json`, build-record JSON, installed-layout tests. | §29.3 |
| **R1** | Stable: `--profile=release` after optimized/sanitizer parity, ABI version checks, concurrent-build certification, reproducibility evidence, packaged-driver discovery, deprecation completion. | §29.4 |

## phases → checklist bands

Full per-slice detail (deliverable + red test) is in the checklist, using the
spec's own `NEX-###` numbering verbatim (§31) so the mapping never drifts.

| band | scope | release | slice range |
|---|---|---|---|
| **F0 — contract, harness, state machine** | requirement/traceability IDs, CLI parse for `--emit=exe`/`--project`/`-o`/`--`, normalized build request + phase enum, fake emitter + fake C-compiler executables, error-phase→exit-class map, atomic-publication abstraction, private scratch lifecycle, argv subprocess abstraction, VM/exe rejection | F0 | NEX-001…010 |
| **F0/R0 — core compiler + entry ABI** | reentrant core-compiler request interface (retires `/tmp/.sv0_drv_path` from the stable path), stdout/stderr/exit protocol normalization, no-`main`/duplicate-`main` rejection, entry signature/return-type validation, `sv0_user_main` + hosted C adapter + runtime init, reserved-symbol mangling, staging-C hash validation | F0/R0 | NEX-011…018 |
| **R0 — runtime + host toolchain** | compiler-relative runtime resolver, ABI manifest + hash verification, compiler-selection precedence (no fallback), capability probe, central dev-profile argv builder, sanitized child env + trusted include order, host-failure diagnostics, default/explicit path + permission/symlink rules, human quiet/verbose/success output | R0 | NEX-019…027 |
| **R0 — corpus, project, reliability** | native-executable manifest from the behavior corpus, runtime-feature suite (println/string/Vec/Box/contracts), project-mode build via compiler composition, contract-mode matrix, per-failure-phase preservation tests, symlink/scratch/race hardening, cancellation, parallel distinct-output builds, hostile-path corpus, Linux GCC/Clang + macOS Apple Clang jobs, user docs | R0 | NEX-028…038 |
| **R0.1 — inspection, config, provenance** | `--emit=c` atomic output, `--keep-c` retention, JSON event encoder/schema/timing, atomic build-record JSON with hashes, `sv0.toml` schema/discovery/precedence, compiler-identity probe cache, installed runtime manifest/licenses, installed-layout E2E, benchmark harness | R0.1 | NEX-039…047 |
| **R1 — optimization, reproducibility, stability** | UB/strict-aliasing audit, warning policy (drop blanket `-w`), ASan/UBSan matrix, `--profile=release` enablement, same-output lock/publication protocol, pinned double-build reproducibility, stable entry/runtime ABI manifest, regression-budget + concurrent perf certification, signed release-candidate evidence, stable published docs, duplicated manual `cc` recipe removal | R1 | NEX-048…058 |

`Future backlog, not scheduled by this specification` (native Windows,
cross-compilation, static/shared libraries, prebuilt runtime archive, program
arguments, C FFI manifests, LTO, signing/notarization, source-level debug
mapping, cache/incremental builds, and eventual convergence with a
backend-neutral CLI once LLVM ships) stays out of the checklist until
separately accepted through change control (spec §31.7).

## completion criteria per gate (condensed from spec §29)

- **F0:** AE-001…009 accepted; CLI/phase state-machine tests pass; missing or
  invalid `main` is rejected before host invocation; one minimal executable
  is produced through the canonical driver; fake-process injection, failure,
  and atomic-preservation tests pass; VM/executable combinations are rejected;
  no public stability claim is made.
- **R0:** every R0 requirement has passing traceability; single-file and
  project builds pass on the R0 host matrix; every supported behavior-manifest
  row passes through the canonical driver; runtime/verified/disabled contract
  modes are sound; runtime-bundle integrity checks pass; prior-output
  preservation, hostile paths, symlinks, cancellation, and distinct-output
  concurrency all pass; global-control-file dependence is absent from the R0
  path; existing full toolchain tests remain green; docs are complete.
- **R0.1:** retained-C identity + atomicity, configuration discovery/
  precedence, JSON message + build-record schema tests, installed-layout
  tests, a benchmark baseline report, and no unresolved R0 correctness or
  output-preservation defect.
- **R1:** all active R1 requirements pass; release profile passes behavior
  parity on every supported host tuple; ASan/UBSan + accepted warnings policy
  pass; compiler/runtime ABI is versioned and documented; same-output
  concurrency is certified; reproducibility scope is tested and documented;
  package contents include manifest/licenses/checksums/support policy; all
  waivers are time-bounded, owned, and noncritical; no known defect can
  corrupt, mispublish, or silently miscompile an executable.

## risk register (top items, full table at spec §32)

| risk | mitigation / descope trigger |
|---|---|
| Global `/tmp/.sv0_drv_path` race under concurrent builds | Reentrant argv/private request interface (NEX-011); static guard that the string never appears on the stable path; parallel-fingerprint tests (NEX-035). |
| Runtime header/source shadowing by a malicious project file | Private staging dir, trusted include order before inherited paths, content hashes, malicious-shadow fixture (NEX-019, NEX-024, AC-008/AC-016). |
| Nonconforming C `main` (void/aggregate/parameterized, or a fabricated no-op) | Entry validation + hosted adapter before host compile, never after (NEX-013…017). |
| Failed link truncates/replaces a known-good binary | Compile to a private temp leaf, validate, atomic rename only after success (NEX-007, NEX-026). |
| `-O2` exposes UB in raw box/vector representation | R0 stays `-O0`; UB audit, sanitizers, and dev/release parity are R1 gates before `--profile=release` unblocks (NEX-048…051). |
| Overstating "C99"/Windows portability | Runtime uses GCC/Clang extensions + POSIX APIs; docs claim only the honest GNU/Clang-compatible hosted matrix; Windows stays deferred (spec §20.3, DOC-003). |
| Shell injection via source/output/flag paths | Direct argv only, no shell-string flags, metacharacter corpus (NEX-009, NEX-036, AC-015). |
| Same-output concurrent publication mixes artifacts | Unique scratch + validated atomic publish now; an output lock/token protocol is an explicit R1 slice (NEX-052, AC-021). |
| Verified-mode false proof (missing/unknown Z3 misreported) | Requested-vs-effective contract mode always distinguished; only *proven* checks are ever stripped (NEX-031, PIPE-009, AC-017). |
| Test driver diverges from the user-facing driver | One canonical host-link implementation shared by tests and commands (GOV-008); duplication guard lands after parity (NEX-058). |

## open decisions (resolve before the named slice, spec §33.2)

| ID | decision | due | default recommendation |
|---|---|---|---|
| OD-001 | Implementation language/module for the host driver | before NEX-003 | **resolved 2026-08-23** — Python, alongside existing `scripts/verify_*.py`; see decisions record above. |
| OD-002 | Direct argv/request mechanism for the self-hosted core compiler | before NEX-011 | **partially resolved 2026-08-23** — investigated; the real ABI needs a new sv0 language/runtime argv host function (out of scope for this driver work, own future slice). NEX-011 shipped as the spec-sanctioned lock-based migration shim instead; see decisions record below. |
| OD-003 | Exact new `E####` codes for entry errors | before NEX-013 | Allocate from compiler diagnostic governance; keep missing/duplicate/params/return-type cases distinguishable. |
| OD-004 | Runtime ABI manifest schema + version numbering | before NEX-020 | **resolved 2026-08-27** — schema/ABI version 1, SHA-256 file hashes; see `sv0c/runtime/runtime-manifest.json` and `scripts/native_exe_runtime_manifest.py`. |
| OD-005 | `-std=gnu99` vs. documented `-std=c99` with accepted extensions | before NEX-023 | **resolved 2026-08-27** — `gnu99`; see `scripts/native_exe_argv_builder.py`. |
| OD-006 | Exact sanitized child environment allowlist on macOS/Linux | before NEX-024 | **resolved 2026-08-27** — see `scripts/native_exe_env.py`'s `ALLOWED_ENV_KEYS`/`BLOCKED_INJECTION_KEYS`. |
| OD-007 | Same-output publication locking primitive across macOS/Linux | before NEX-052 | Filesystem atomic create/rename + ownership token; do not rely on `flock` alone. |
| OD-008 | Build-record digest algorithm | before NEX-042 | SHA-256. |
| OD-009 | Stable installed-command transition from the workspace adapter | before R1 | `sv0c --emit=exe` canonical; `./scripts/sv0 native-compile` forwards, deprecates later. |
| OD-010 | Release-profile overflow/aliasing flags | before NEX-051 | Decide only from language semantics + UB/sanitizer evidence; no `-ffast-math`. |

These are controlled gates, not permission to improvise (mirrors the VS Code
extension hub's own framing) — do not silently default them inside an
implementation PR; record the resolution here when made.

## decisions record

Resolved 2026-08-23, in order:

- **OD-001.** Host driver implementation: **Python**, alongside the existing
  `scripts/verify_*.py` guard/test infrastructure this repo already trusts for
  exactly this kind of testable, argv-safe, atomic-filesystem code. `./scripts/sv0
  native-compile` becomes a thin bash→Python forward. `sv0c --emit=exe` is
  documented as the canonical spelling and delegates to this same Python
  driver until sv0's runtime gains process-spawn/exec host functions (tracked
  as a prerequisite for the self-hosted-driver alternative, not scheduled by
  this spec). Unblocks NEX-003 and the whole F0 band (NEX-001…010).
- **OD-002.** Investigated 2026-08-23 while implementing NEX-011: sv0 (the
  language) has no argv/CLI-args host function today — `sv0_runtime.h`/`.c`
  expose no such symbol, and the native mega-TU compiler's own CLI entry
  point is synthesized at build time by `scripts/build-sv0-megatu-native.sh`
  string-injecting a `/tmp/.sv0_drv_path` read into generated sv0 source,
  because `read_file(path)` is the only external-input channel a self-hosted
  sv0 program has. A real reentrant ABI needs a new host function threaded
  through the resolver/checker/C-lowering/runtime — and `sv0doc`, which owns
  language capabilities per §3.3's ownership table — a compiler-feature
  addition, not a driver change. **Decision: land the spec-sanctioned lock
  migration shim now (§22.2), scope the real argv builtin as its own future
  slice, not undertaken here.** NEX-011 shipped on this basis.

## progress dashboard (sv0-track)

<!-- sv0-track:begin {"id":"nex.plan.session","title":"sv0c native runtime executable — session hooks","kind":"checklist-region","ingest":"gfm","milestone":"NEX"} -->
- [ ] Open decisions (OD-001…010) resolved before the slice that depends on them starts
- [ ] Checklist (`task/sv0c-runtime-executable-checklist.Rmd`) slice status flipped + one-line result recorded when a `NEX-###` lands
- [ ] Requirement/acceptance ID cited in every implementation PR (spec Appendix D traceability rule)
- [ ] `/tmp/.sv0_drv_path` reset to empty (`printf '' > /tmp/.sv0_drv_path`) before any gate/push touching this area — see `[[feedback_drv_path_reset]]`
- [ ] `./scripts/sv0 test-guards` after `task/` edits; `./scripts/sv0 test` when driver/compiler code is integrated
<!-- sv0-track:end {"id":"nex.plan.session"} -->

## dependencies

/require: bash

## configuration

/env: SV0TOOLCHAIN_ROOT=.
/env: SV0C_ROOT=sv0c
/env: SV0DOC_ROOT=sv0doc
/env: SPEC_ROOT=~/Documents/project-specs/sv0c-runtime-executable

## prerequisites

- A self-hosted compiler that emits one C translation unit per program —
  **met** (existing `--target=c` default path, native mega-TU compiler).
- Repeated working `cc` link recipes proving the emitted C runs — **met**
  (`make e2e test-contract-runtime`, C integration fixtures).
- Contract-mode lowering (`runtime`/`verified`/`disabled`) on the C path —
  **met**; this feature reuses it unchanged (PIPE-008/009).
- external: a supported GCC/Clang-compatible host `cc` on the R0 matrix
  (Ubuntu 22.04 default `cc` + a supported Clang; macOS Apple Clang arm64).

## refinement log

- 2026-08-23: initial scaffold from `~/Documents/project-specs/sv0c-runtime-executable/SPEC.md`
  (draft governing specification, source-audit date 2026-08-23). Hub created
  with scope decisions (AE-001…009), release ladder, phase→checklist-band
  mapping onto the spec's own `NEX-###` numbering, condensed completion
  criteria, risk register, and open decisions. Confirmed by direct repo audit
  (`scripts/sv0:1088` `compile-run`) that no part of the spec'd feature exists
  yet — every checklist slice starts `todo`. State `draft`; flip to `active`
  when work begins on NEX-001.
- 2026-08-23: **OD-001 resolved** (Python host driver, alongside existing
  `scripts/verify_*.py`) — see decisions record above. State `draft → active`.
  **NEX-001 landed**: `scripts/native_exe_requirements.json` (140-id catalog)
  + `scripts/verify_native_exe_traceability.py`, wired into
  `sv0 test-guards`. The lint surfaced 4 real F0 citation gaps in the
  checklist as-drafted (ERR-002, PIPE-002, SEC-002, TEST-001) before it went
  green — fixed by adding the missing ids to their correct slices rather than
  loosening the check. F0 is now 0/40 blocking gaps; 27 higher-release ids are
  reported informationally and become blocking only when `--active-release`
  advances past F0. **NEX-002 landed**: `scripts/native_exe_cli.py`
  (`parse_args`/`ParsedArgs`/`UsageError`), 15-case table-driven parser
  corpus, wired into `sv0 test-guards`. Confirmed load-bearing by mutation
  (removing the `-o -` guard fails exactly that case). No production entry
  point yet — this is CLI-001…006 argv parsing only; wiring into
  `./scripts/sv0 native-compile` follows once NEX-003's `NativeBuildRequest`
  type exists. **NEX-003 landed**: `scripts/native_exe_phases.py`
  (`Phase`/`PhaseSequencer`, spec §12.5 atomic-publication state machine) and
  `scripts/native_exe_request.py` (`NativeBuildRequest`/`normalize_request`,
  Appendix A shape). Phase machine confirmed load-bearing by mutation
  (loosening the linear-order check fails the skip-ahead case). Path
  resolution is CLI-007 only (relative-to-invocation-cwd); default output
  naming (§12.1) and full path validation are still NEX-026. **NEX-004
  landed**: `scripts/native_exe_fake_emitter.py` — a real subprocess-invocable
  fake core compiler with all 7 spec §26.2 modes, mode selected via env var
  (not argv) so it stays indistinguishable from a real compiler call to
  driver code under test. Confirmed load-bearing by mutation (dropping the
  `diag` mode's stderr write fails that case). **NEX-005 landed**:
  `scripts/native_exe_fake_cc.py` — the host-compiler counterpart, all 7 spec
  §26.3 modes plus a `--version` probe responder, argv-recording proves
  hostile paths pass through literally (never shell-interpreted). Confirmed
  load-bearing by mutation (`zero-no-output` writing the file anyway fails
  that case). F0's harness pair (NEX-004/005) is now complete — every later
  PIPE/TOOL/ART/SEC slice can drive fully-fake builds before any real
  compiler is involved. **NEX-006 landed**: `scripts/native_exe_errors.py` —
  `DiagnosticPhase`/`exit_code_for`/`BuildError` centralizing the §18.2
  phase→exit-code table (2/3/4/5/6/7/8/70/130) so no driver code hand-rolls a
  `sys.exit` number. Confirmed load-bearing by mutation (misassigning
  `host-compile`'s exit code fails exactly that phase). **NEX-007 landed**:
  `scripts/native_exe_publish.py` — `validate_temp_output`/`publish_atomically`,
  enforcing ART-002…004 (validate-before-touch, byte-for-byte preservation on
  failure, correct `HOST_LINK` vs. `PUBLISH` phase classification). ART-004
  preservation exercised through `native_exe_fake_cc.py`'s real failure
  modes, not synthetic stand-ins. Confirmed load-bearing: removing the
  pre-publish validation call reproduces the exact bug this module exists to
  prevent (final-output corruption on a failed rebuild). **NEX-008 landed**:
  `scripts/native_exe_scratch.py` — `ScratchDir`, a context manager whose
  cleanup only ever removes the exact path `mkdtemp` handed back, never a
  prefix/glob scan (SEC-005). Confirmed load-bearing: swapping exact-path
  cleanup for a prefix-glob (the precise bug class this module exists to
  prevent) fails the neighbor-survival case. **NEX-009 landed**:
  `scripts/native_exe_subprocess.py` — `run_argv`, which structurally cannot
  invoke a shell (`shell` isn't a parameter; a bare string argument is a
  `TypeError`). Confirmed load-bearing by mutation (joining argv into a
  string + `shell=True` fails 4 of 6 cases immediately). **NEX-010 landed**:
  extended `native_exe_cli.parse_args` to reject `--target=vm` (any position)
  and `.sv0b` input with a distinct "bytecode isolation" message, exit 2,
  before `ParsedArgs` is ever built. GOV-003 (`sv0vm` never invoked) holds by
  construction — no subprocess-invoking code exists in this module yet; a
  real call-log assertion is deferred to when driver orchestration exists.
  Mutation testing caught a real gap on the first pass (the pre-existing
  generic "unknown option" fallback already raised *a* `UsageError`, so the
  dedicated guard wasn't actually proven load-bearing until the corpus was
  strengthened to assert on message content) — fixed before landing.

  **§31.1 F0 backlog (NEX-001…010) is now fully landed, 10/10, all green in
  `sv0 test-guards`, all mutation-verified.** This is not the same as the F0
  *release gate* (§29.1): that additionally needs the F0-labeled entry-ABI
  requirements (ENTRY-001…004) from §31.2's NEX-013…017, plus real
  end-to-end orchestration (NEX-011/012) — those are next.

  **NEX-011 landed** (as a documented migration shim, not the stable ABI —
  see OD-002 above): `scripts/native_exe_core_compiler.py` —
  `CoreCompilerClient`, a POSIX-`flock`-serialized wrapper around the legacy
  control-file binary. 4-case selftest incl. a genuine "before/after" proof:
  a fake compiler with a deliberate read-then-sleep-then-echo widens the race
  window so the unlocked path reliably cross-contaminates, while 6 concurrent
  requests through the real locked `invoke()` are always correctly isolated
  (stable across 15 repeated runs). Confirmed load-bearing by mutation:
  removing the `flock` call reproduces cross-contamination in 10/10 runs.
  Documented limitation: the lock only protects calls made through this
  module, not unrelated `scripts/sv0`/self-host-loop touches of the same
  file — matches existing `[[feedback_drv_path_reset]]` guidance.

  **NEX-012 landed**: `scripts/native_exe_emit.py` — `classify_emission`
  (PIPE-003 nonzero-exit-governs-first ordering, PIPE-004 empty-stdout
  detection, PIPE-005 stream separation preserved even on success, ERR-002
  verbatim diagnostic passthrough). 5-case selftest driven through the real
  fake emitter. Confirmed load-bearing by mutation: checking stdout
  emptiness before exit code — the exact ordering bug PIPE-003 exists to
  prevent — fails the `partial` case.

  **NEX-013 landed**: `scripts/native_exe_entry_scan.py` —
  `validate_entry_exists`, a comment/string-aware source scan for `fn main`.
  Investigated the C backend first: it currently *fabricates*
  `int main(void) { return 0; }` for a missing `main`, byte-identical in the
  emitted C to a real trivial one — confirming detection must happen on
  source text, not compiler output. Deliberately scoped as a pre-filter, not
  a reimplementation of project composition or semantic entry validation
  (§13.3). 8-case selftest incl. main mentioned only in a `//`/`/* */`
  comment, a string literal, or a hidden directory — none of which count.
  Confirmed load-bearing by mutation (dropping comment-masking fails 3
  cases; inverting the core check fails 5). **NEX-014 landed**: extended the
  same module — `count_main_declarations` + updated `validate_entry_exists`
  reject >1 `fn main` occurrence, whether within one file or spread across
  separate project files (10 cases total). Confirmed load-bearing by
  mutation: relaxing either the file- or project-mode uniqueness check fails
  exactly its corresponding case.

  **NEX-015 landed**: `scripts/native_exe_entry_signature.py` —
  `extract_main_signatures` (balanced-paren param extraction + return-type
  text scan, reusing NEX-013's comment-masking) + `validate_entry_signature`.
  11-case selftest: 3 accepted shapes + 7 rejected shapes (params, bool,
  wrong int width, struct, enum-shaped, reference, string) + 1 project-mode
  case. Confirmed load-bearing by mutation: disabling either the params or
  return-type check fails exactly its corresponding cases.

  **NEX-016 landed** (first real compiler change, planned via `EnterPlanMode`
  given the scope shift — see `/Users/sasank/.claude/plans/starry-discovering-quokka.md`).
  Investigation found 3 different C-backend `main`-emission sites in `sv0c`;
  only `sv0c/lib/megaTU-main.sv0`'s `megatu_emit_program` is on the path any
  currently-invoked binary (`build/sv0-megatu-native`, behind `sv0 compile`)
  actually runs — `codegen.sv0`'s `emit_program` is compiled but dead for
  output, `driver.sv0` only matters for native-parity testing. Modified only
  `megaTU-main.sv0` (the recognized-shape branch: zero params, `i32`/unit
  return → `sv0_user_main` + hosted `int main(argc, argv)` + `sv0_runtime_init`
  call; every other shape keeps the old plain emission unchanged) and
  `sv0c/runtime/sv0_runtime.h` (new no-op `sv0_runtime_init`, ABI slot
  reserved). Verified: 3 manual smoke fixtures (AC-001/002/003) plus the
  existing 112-program native behavior corpus
  (`verify_behavior_corpus_native.py`, part of `./scripts/sv0 test`) passing
  unmodified through the new shape — stronger evidence than adding synthetic
  fixtures, so none were added. Two `./scripts/sv0 test` failures observed
  (an SML-side checker self-test, and `self-host-sv0-loop` exit 232 for
  `lib/checker.sv0`) were confirmed **pre-existing and unrelated**: the SML
  one runs via `make -C sv0c test`, entirely independent of the two files
  touched here; the 232 one was reproduced byte-for-byte by `git stash`ing
  this change, rebuilding, and re-running the same check on the clean tree
  before restoring. Neither blocks this slice. `codegen.sv0`/`driver.sv0`
  deliberately untouched per the plan's documented scope.

  **NEX-017 landed** (planned via `EnterPlanMode` again, lighter-weight than
  NEX-016 — pure Python, no compiler changes): `scripts/native_exe_entry_reserved.py`
  — `validate_no_reserved_collisions`, rejecting a user-declared top-level
  `fn sv0_user_main`/`fn sv0_runtime_init` at the driver layer, before the
  host compiler ever runs. Verified first that today a colliding fixture
  produces a raw `cc` redefinition/conflicting-types error (host-compile,
  exit 6) rather than a clean early ENTRY diagnostic — chose driver-side
  rejection over compiler-side mangling specifically to avoid needing to
  rewrite call sites for a renamed function, and left the host compiler's
  own error as a defense-in-depth backstop. Deliberately scoped to the 2
  symbols NEX-016 introduced, not the ~20 other `sv0_`-prefixed runtime
  symbols. 5-case selftest, confirmed load-bearing by mutation (disabling
  the check fails all 3 collision cases).

  **NEX-018 landed**: `scripts/native_exe_staging.py` —
  `validate_staging_c` (PIPE-011: rejects staging C missing the runtime ABI
  include marker) + `hash_staging_c` (SHA-256). PIPE-006 (byte-identical C
  between `--emit=c` and `--emit=exe`) holds by construction — one emission
  call site, no per-mode divergence — proved with two independent fake-
  emitter invocations compared byte-for-byte and by hash. Confirmed
  load-bearing by mutation (disabling the marker check fails that case).

  **F0/R0 core-compiler + entry-ABI band (NEX-011…018) is now fully closed,
  8/8, all green in `sv0 test-guards`, all mutation-verified.** Combined
  with the fully-closed §31.1 F0 band, every NEX-### slice through NEX-018
  is done. Next band is R0 runtime + host toolchain (NEX-019…027).

  **NEX-019 landed** (first slice of the R0 runtime+toolchain band, planned
  via `EnterPlanMode` — see `/Users/sasank/.claude/plans/starry-discovering-quokka.md`):
  `scripts/native_exe_runtime.py` — `resolve_runtime_dir`/`RuntimeLocation`,
  derived from this script's own toolchain-root location, never cwd/env/
  project-relative. Confirmed load-bearing by mutation (disabling the
  missing-file check fails the fail-closed case).

  **NEX-020 landed** (touches `sv0c` — a plain JSON metadata file, not
  compiler logic, same risk class as the README SHA-pin bumps):
  `scripts/native_exe_runtime_manifest.py` — `load_manifest`/
  `verify_manifest`/`write_manifest`, plus the new checked-in
  `sv0c/runtime/runtime-manifest.json` (schema/ABI version 1, SHA-256
  hashes, OD-004 resolved). Confirmed load-bearing by mutation (disabling
  the header-hash comparison fails the corrupted-header case).

  **NEX-021 landed**: `scripts/native_exe_cc_select.py` — `select_cc`, the
  R0-relevant precedence tiers (`--cc` → `CC` env → `PATH` `cc`). Confirmed
  load-bearing by mutation: letting an invalid explicit `--cc` silently fall
  through to `CC`/`PATH` (the exact bug TOOL-002 exists to prevent) fails
  both explicit-rejection selftest cases.

  **NEX-022 landed**: `scripts/native_exe_cc_probe.py` — `probe_compiler`,
  `--version` + family classification followed by an actual compile+link+run
  of a minimal hosted program (catches a compiler that answers `--version`
  but can't produce a working binary, which a bare version check would
  miss). Confirmed load-bearing by mutation (disabling the compile-failure
  check still fails, one stage later, at the run step).

  **NEX-023 landed**: `scripts/native_exe_argv_builder.py` —
  `build_dev_profile_argv`, the one canonical Appendix B argv builder;
  OD-005 resolved (`-std=gnu99`, never `c99`). Confirmed load-bearing by
  mutation (swapping to `-std=c99` fails both the argv snapshot and the
  dedicated dialect check).

  **NEX-024 landed**: `scripts/native_exe_env.py` — `sanitized_child_env`,
  an explicit allowlist (OD-006 resolved) rather than a denylist, so a
  future injection variable can't slip through unnoticed. Confirmed
  load-bearing by mutation: reimplementing it as a copy-then-strip denylist
  immediately lets an unrelated var leak through.

  **NEX-025 landed**: `scripts/native_exe_host_compile.py` —
  `run_host_compile`, composing `native_exe_subprocess.run_argv` +
  `native_exe_publish.validate_temp_output` rather than reimplementing
  either. Confirmed load-bearing by mutation: disabling the nonzero-exit
  check misclassifies the phase (`HOST_LINK` instead of `HOST_COMPILE`) and
  loses the original stderr — caught immediately.

  **NEX-026 landed**: `scripts/native_exe_output_path.py` —
  `default_output_path`/`validate_output_path`/`ensure_output_parent_dir`.
  Reuses `native_exe_publish`'s lexists/islink symlink-safety pattern,
  applied pre-flight on the *destination* rather than post-flight on the
  temp source. Confirmed load-bearing by mutation (removing the
  symlink-leaf check fails that case).

  **NEX-027 landed**: `scripts/native_exe_human_output.py` —
  `format_success_message`/`maybe_print_success`/`render_argv_for_display`.
  Confirmed load-bearing by mutation (dropping `shlex.quote` fails the
  hostile-argv round-trip case).

  **R0 runtime + host toolchain band (NEX-019…027) is now fully closed,
  9/9, all green in `sv0 test-guards`, all mutation-verified.** OD-004,
  OD-005, and OD-006 all resolved along the way. Combined with the
  fully-closed F0 and F0/R0 bands, every NEX-### slice through NEX-027 is
  done (25 of 58). Next band is R0 corpus/project/reliability (NEX-028…038).

  **Re-scoping pass on NEX-028…038** (planned via `EnterPlanMode`): a direct
  codebase look found none of NEX-001…027's pieces had been wired together
  into a real build yet, and several of the stock slice descriptions
  (029/032/033/036) overlapped heavily with what NEX-007/008/019/021/023/024/026
  already mutation-tested at the unit level. Re-sliced around **NEX-028
  absorbing the actual assembly function** (`build_native_executable`) —
  every later slice in the band reuses it instead of deriving its own
  mini-pipeline. Also found: `sv0c/test/behavior/manifest.txt` already has
  112 rows that all pass through the native path today; real fixtures
  already exist for Box (`test/integration/box_expr/`) and project mode
  (`test/integration/modules/`); Z3 4.16.0 is installed in this dev
  environment, so the verified-contract-mode leg is directly testable, not
  just documented. Full re-slicing recorded in
  `/Users/sasank/.claude/plans/starry-discovering-quokka.md`.

  **NEX-028 landed** (re-scoped, see above): `scripts/native_exe_build.py`
  — `build_native_executable`, the first real wiring of the whole pipeline,
  and `scripts/native_exe_behavior_corpus.py`, proving all 112 behavior-manifest
  rows build+run correctly through it (not a hand-rolled separate recipe).
  Mutation testing caught a real selftest gap on the first pass — the
  fixture set was positive-only, so removing entry validation went
  unnoticed until a no-main negative case was added — fixed before landing.

  **NEX-029 landed**: `scripts/native_exe_runtime_feature_smoke.py` — Box
  (`test/integration/box_expr/box_expr.sv0`, real existing fixture) +
  stdout/stderr channel separation on a real compiled `requires`-violation
  binary. Found and corrected a stale assumption along the way: a doc
  comment in `test/integration/contract_violation/README.md` claims native
  contract checks are dropped — verified directly against the current
  compiler that this is no longer true (the `requires` check fires
  correctly, diagnostic on stderr, `println` on stdout). Filesystem I/O
  left undone, documented, since no fixture exists yet. Confirmed
  genuinely discriminating (not just "passes trivially") by swapping the
  stdout/stderr assertions and by asserting a deliberately wrong Box exit
  code — both fail as expected against the real binary.

  **NEX-030 landed**: `scripts/native_exe_project_mode.py` — reuses
  `build_native_executable` unchanged against the real, already-existing
  `sv0c/test/integration/modules/` project fixture (AC-006: exit 42 =
  `bump(41)`). Confirmed genuinely discriminating (a deliberately wrong
  expected exit fails against the real build).

  **NEX-031 landed**: `scripts/native_exe_contract_mode.py` —
  `compute_proof_lines`, reusing `scripts/sv0`'s own `run_emit_verified`
  pass-1 machinery (`build/sv0-megatu-verify` + `scripts/sv0-z3.sh`) rather
  than reimplementing Z3 orchestration. Tested against the real, installed
  Z3 4.16.0, not just documented. Confirmed load-bearing by mutation: making
  the z3-absent path falsely claim a proof (the exact bug PIPE-009 exists to
  prevent) is caught immediately; a wrong disabled-mode exit assertion fails
  too, confirming the check-stripping behavior is real.

  **NEX-032 landed**: `scripts/native_exe_preservation.py` — drives the
  real `build_native_executable` through all 6 directly-injectable failure
  phases, confirming a seeded known-good output survives byte-for-byte and
  mode-for-mode every time. Added a `runtime_override` seam to
  `build_native_executable` to make the RUNTIME phase injectable
  end-to-end. Mid-implementation discovery: `sanitized_child_env`
  correctly (by design) strips the env var used to steer the fake host
  compiler in tests — fixed by baking the mode into a tiny wrapper script.
  Confirmed genuinely load-bearing (not just individually-already-proven
  lower layers) by mutating `build_native_executable`'s entry-validation
  call and watching this test catch it too.

  **NEX-033 landed**: `scripts/native_exe_hardening_smoke.py` —
  consolidation over NEX-008/026, not new logic. Added a `scratch_base_dir`
  test seam to `build_native_executable` so the neighbor-scratch check is
  genuinely adjacency-sensitive (the neighbor lives under the same parent
  the real build's own scratch dir uses), not just "some unrelated
  directory elsewhere." Confirmed genuinely load-bearing: mutating the real
  symlink-rejection check and mutating scratch cleanup to a prefix-glob
  (SEC-005's exact bug class) are both caught end-to-end here, not just by
  their own originating modules' isolated tests.

  **NEX-034 landed** — the most genuinely new capability in this band:
  extended `native_exe_subprocess.py` with `run_argv_cancellable`, launching
  the child as a process-group leader (`start_new_session=True`) so
  cancellation can terminate the whole group, not just the immediate child.
  **Caught a real gap in my own test, not the production code**: the first
  mutation check (targeting only the leader PID) silently passed, because
  the test's timing let the background-grandchild marker-file check get
  skipped before the marker was ever created — fixed the test's timing and
  made "marker never created" a hard failure instead of a silent skip, then
  re-confirmed the mutation is genuinely caught. A useful reminder that "the
  mutation check passed" isn't itself proof of anything until you've
  confirmed *why* it passed.

  **NEX-035 landed**: `scripts/native_exe_parallel_builds.py` — 6 real
  concurrent `build_native_executable` calls under `threading`, each
  program's exit code its own fingerprint. Confirmed dramatically
  load-bearing: removing `native_exe_core_compiler.py`'s `flock` call
  reliably reproduces real cross-contamination and outright `read_file`
  panics across all 6 builds, on every one of 3 separate runs.

  **NEX-036 landed** — another real bug caught in the test, not the
  production code: `scripts/native_exe_hostile_paths.py`'s first version
  embedded a full sentinel *path* (with slashes) inside a hostile output
  filename, so `ensure_output_parent_dir` correctly rejected it as a
  malformed path before the test could exercise anything real — every
  "hostile" build was silently caught as an unrelated, legitimate
  `BuildError`, and mutating the exit-code assertion had zero observable
  effect because that code was never reached. Root-caused with debug
  tracing (not guesswork), fixed by using a bare sentinel *name* with no
  embedded separators. Confirmed genuinely load-bearing afterward: 6/6
  hostile shapes now build+run+exit correctly end to end, and a wrong
  expected-exit mutation fails all 6. The "no shell injection" check itself
  holds by construction here (hostile paths never become subprocess argv —
  they're laundered through fixed scratch-internal names first), documented
  as a defensive guard rather than an active mutation-proof.

  **NEX-037 landed** (documentation, not a CI change): recorded the required
  R0 host-matrix job configuration in `sv0c/doc/native-executable.md`
  against current CI reality (one Ubuntu job, no Clang leg, no macOS job at
  all) — the macOS arm64/Apple Clang leg has been continuously proven on
  this dev machine throughout the whole session; the Linux legs aren't
  locally reproducible here. Adding the actual CI jobs is a separate change
  with its own review (billed minutes, supported-platform claim), correctly
  left undone in this doc-only slice.

  **NEX-038 landed**: `sv0c/doc/native-executable.md` — user docs
  (DOC-001…003), stating current status honestly (engine real and tested,
  public CLI not wired up yet). Every example was verified against the real
  engine before being written down — the direct-Python-call example was
  wrong twice on the first attempts (missing `PYTHONPATH`; an explicit
  output path that doesn't auto-create its parent per ART-007) and was
  corrected against actual runs, not left as unverified prose.

  **R0 corpus/project/reliability band (NEX-028…038) is now fully closed,
  11/11, all green in `sv0 test-guards`, all mutation-verified (where
  applicable) or explicitly, honestly scoped as documentation-only where
  not.** Combined with the already-closed F0 (NEX-001…010) and F0/R0
  (NEX-011…018) bands and the R0 runtime+toolchain band (NEX-019…027), the
  entire R0 release is now closed: 20 slices from NEX-019 through NEX-038,
  every one of NEX-001 through NEX-038 done. Next up is R0.1 (NEX-039…047:
  `sv0.toml`, `--message-format=json`, build records, `--keep-c`,
  installed-layout tests) — a genuinely different kind of work (config
  parsing, JSON schemas) from anything in this band.

  **Re-slicing pass on NEX-039…047 for the R0.1 band** (planned via
  `EnterPlanMode`): confirmed `sv0c/LICENSE-APACHE`/`LICENSE-MIT` already
  exist (no new licensing decision needed for NEX-045) and Python 3.13 here
  has `tomllib` built in (no third-party TOML dependency for NEX-043).
  Structural decision: `--emit=c`'s "front half" duplicates a few lines of
  *sequencing* from `build_native_executable` rather than factoring out a
  shared internal helper — refactoring already-mutation-tested code carries
  more risk than five lines of repeated glue. Full re-slicing recorded in
  `/Users/sasank/.claude/plans/starry-discovering-quokka.md`.

  **NEX-039 landed**: `scripts/native_exe_emit_c.py` — `emit_c_only`, plus
  `native_exe_staging.write_text_atomically` (new, mirrors
  `native_exe_publish`'s atomic-write pattern for text). Confirmed load-bearing
  by mutation, on the second attempt: the first mutation (calling the fake
  cc with `--version`) silently passed because that mode never reaches the
  fake tool's own call-recording logic — root-caused and fixed by invoking
  the fake tool in a mode that actually records, which then caught the
  regression immediately.

  **NEX-040 landed**: `scripts/native_exe_keep_c.py` + a new `keep_c_path`
  parameter on `build_native_executable` — writes the staging C atomically
  right after `validate_staging_c` succeeds, unconditionally, before host
  compile ever runs, so it survives a later host-compile/link failure.
  Selftest needed a wrapper-script indirection (baking
  `SV0_FAKE_CC_MODE=fail` into an executable, the same trick
  `native_exe_preservation.py` uses) rather than a plain env-var injection,
  since `sanitized_child_env` correctly strips that var by design. Confirmed
  load-bearing by mutation: moving the write to after a successful host
  compile correctly fails the forced-failure retention case.

  **NEX-041 landed**: `scripts/native_exe_json_output.py` — `PhaseTimer` +
  `build_event`/`encode_event` matching §18.6's exact JSON schema. `total`
  is computed as the sum of already-rounded per-phase values (not a
  separately rounded raw sum) so it's exactly consistent with its parts by
  construction. Confirmed load-bearing by mutation: removing the
  nonnegative clamp is caught immediately.

  **NEX-042 landed**: `scripts/native_exe_build_record.py` — `build_record`
  matches Appendix C's exact shape, computing the two hash-bearing pieces
  itself (`hashlib.sha256` over the real published bytes and each input
  source file) rather than trusting caller-supplied digests, since the
  whole point of a build record is independent verifiability. Confirmed
  load-bearing by mutation: hardcoding the artifact hash is caught
  immediately by the digest-mismatch check.

  **NEX-043 landed**: `scripts/native_exe_config.py` — `discover_config`/
  `load_config`/`resolve_precedence`, using Python's built-in `tomllib`
  (no third-party dependency). Confirmed load-bearing by two independent
  mutations: dropping the key/type validation, and reordering the
  precedence tiers -- both caught immediately by their respective cases.

  **NEX-044 landed**: `native_exe_cc_probe.py` gained `probe_compiler_cached`
  (in-process, keyed by `os.path.realpath`). Its selftest had to wrap the
  *real* host `cc` rather than the fake tool, since `probe_compiler` runs
  the produced binary and a fake placeholder file isn't a real executable.
  Confirmed load-bearing by mutation: bypassing the cache lookup is caught
  immediately by the invocation-count checks.

  **NEX-045 landed**: `sv0c` gained `runtime/abi-version.txt` and
  `runtime/licenses/{LICENSE-APACHE,LICENSE-MIT}` (copies of the
  already-existing top-level license files -- no new licensing decision).
  New `scripts/native_exe_runtime_bundle.py` verifies the full §15.1 bundle
  shape. Confirmed load-bearing by mutation: checking only the first
  required member is caught immediately by the per-member missing-member
  loop.

  **NEX-046 landed**: `scripts/native_exe_installed_layout.py` copies the
  full installed layout (`sv0c/runtime/*` + `build/sv0-megatu-native`) into
  a fresh temp dir with a space in its path and no `sv0c` tree beside it,
  then drives a real build off the copy via the existing
  `runtime_override`/`compiler_path` seams -- no new seams needed.
  Confirmed load-bearing by mutation: corrupting the copied header (to
  prove the pipeline reads from the copy, not a cached/real location) is
  caught immediately.

  **NEX-047 landed**: `scripts/native_exe_benchmark.py` — `run_benchmark`
  wraps `build_native_executable` with `PhaseTimer` (NEX-041) across 4
  representative fixtures (minimal, median-size, largest, and the modules
  project fixture). A real report (`--run`) confirmed all 4 present with
  60–120ms timings. Confirmed load-bearing by mutation: dropping the
  `total` key from a fixture's timings is caught immediately by the schema
  check. **R0.1 — inspection, configuration, and provenance band
  (NEX-039…047) is now fully closed, 9/9.**

  **R1 scoped and re-sliced** (planned via
  `/Users/sasank/.claude/plans/starry-discovering-quokka.md`, `EnterPlanMode`/
  `ExitPlanMode`): the spec's own §31.6 lists R1 as 11 slices, NEX-048…058,
  but several are too large for one TDD slice each (sanitizer
  certification, release-profile enablement, ABI versioning,
  reproducibility evidence, a perf regression harness, a signed-release
  workflow, a legacy-recipe dedup sweep). Re-sliced into **34 bite-sized
  sub-slices** using the same letter-suffix convention as the `PC-2b/2c/2d`
  M3 Phase C epics (`NEX-048a`, `NEX-048b`, …) — the spec's `NEX-04x`
  number stays the requirement-traceability anchor, the letter is the real
  unit of work. Full breakdown in
  `task/sv0c-runtime-executable-checklist.Rmd`'s R1 section.

  Investigation done while scoping (grounding the plan, not assumptions):
  **REL-004** ("global `/tmp/.sv0_drv_path` SHALL not appear in the stable
  executable path") is currently violated by design — `CoreCompilerClient`
  (NEX-011) is an explicitly-documented migration shim built on that exact
  global file plus an `flock`; retiring it needs a genuinely new sv0c-side
  reentrant core-compiler entry point that doesn't exist yet. Flagged as
  its own sub-slice, **NEX-055c, the single highest-risk item in this
  band** — it touches sv0c compiler internals rather than only the Python
  driver/CI layer, and gets its own dedicated `EnterPlanMode` investigation
  pass when R1 work reaches it (the same treatment NEX-016/017 got for
  comparably-sized sv0c changes), rather than being scoped in fine detail
  now. **GOV-008** is already R0-closed for the `native_exe_*.py` driver
  itself (`build_dev_profile_argv` is the one canonical argv site), but
  three legacy shell scripts still hand-roll `cc -std=c99 -O0 -w ...`
  recipes with blanket `-w` (forbidden at the stable gate by §26.6):
  `scripts/pc3b6-native-project-acceptance.sh`,
  `scripts/sv0-megatu-native-parity.sh`,
  `scripts/sv0-megatu-corpus-parity.sh` — NEX-058/049c's real content.
  **ENTRY-010** has no code today: only the runtime *bundle* is
  ABI-versioned (`native_exe_runtime_manifest.ABI_VERSION`), not the entry
  contract itself (reserved symbols, `sv0_user_main`/hosted-adapter shape)
  — NEX-054a adds a genuinely new, parallel `ENTRY_ABI_VERSION`. CI today
  (`.github/workflows/ci.yml`) is a single Ubuntu job with no Clang,
  macOS, sanitizer, or release-profile leg — every CI-job sub-slice in
  this band (NEX-050c, NEX-051d) is genuinely new work, not a doc-only
  restatement of NEX-037's documented-but-unapplied jobs.

  Sequencing follows the spec's own NEX-048…058 order (051's release
  profile needs 048's audit and 050's sanitizer clearance; 057's docs need
  nearly everything else written first; 058 is independent).

  **NEX-048a landed**: `sv0c/doc/native-executable-ub-audit.md`. Reading
  the actual source turned up two real, concrete sites beyond the generic
  "raw box representation" risk register entry: `sv0__box_deref_raw`'s
  pointer-cast-and-deref (write via `memcpy`, read via cast — an
  asymmetric, genuine strict-aliasing violation), and a second, more
  fragile instance of the same root cause already deliberately documented
  in the bootstrap compiler itself — `lowering.sv0::Value` and
  `codegen.sv0::Value` are nominally-distinct C struct types the megaTU
  emitter cross-reinterprets via the same box-deref macro, relying on a
  documented but compiler-unenforced "prefix-compatible layout" convention.
  Both need `-fno-strict-aliasing`, one flag covering both — feeding
  directly into NEX-051a's release-profile argv. A third finding (signed
  arithmetic overflow at type-minimum values) has no documented sv0 policy
  and is deferred to NEX-048b for fixture-driven resolution rather than
  guessed at here.

  **NEX-048b landed**: 5 sanitizer fixtures registered in
  `sv0c/doc/native-executable-sanitizer-fixtures.md`. The two safe stress
  fixtures (`box_deref_chain_stress.sv0`, `vec_growth_stress.sv0`) joined
  the ordinary 114-program behavior-corpus PASS manifest with observed
  (not hand-computed) exit codes. The three UB-only fixtures for Site 4
  were deliberately kept out of the manifest and instead directly
  *observed*: on this dev machine's arm64/Apple Clang at `-O0`, none crash
  today — ARM's `SDIV`/negate silently wrap on signed overflow rather than
  trapping the way x86's `IDIV` does. That's real, platform-specific
  evidence (not an assumption) that a program relying on this UB could
  behave differently across NEX-051d's eventual host matrix.

  **NEX-049a/b/c landed**: `scripts/native_exe_warning_policy.py` +
  `native_exe_warning_report.py` + `native_exe_no_blanket_suppression.py`.
  Grounding the policy in a real full-corpus run under `-Wall -Wextra`
  surfaced a genuine finding, not just noise: every enum `match` lowers to
  an if/else-if chain with an empty final else branch, so the result temp
  is provably uninitialized on any tag the C compiler can't prove
  exhaustive (`-Wsometimes-uninitialized`, 16 occurrences across the
  corpus) — confirmed real via generated-C inspection
  (`enum_match_payload.sv0` and others), not dismissed as a false
  positive. Rather than either silently suppressing it (dishonest) or
  rewriting match codegen inline in a warning-policy slice (out of
  proportion), added a new `TRACKED_GAPS` concept distinct from
  `SUPPRESSED_WARNINGS` — a real gap stays *visible* in every future
  report rather than hidden, with the actual fix filed as a spawned
  follow-up task (`task_69df0050`, "Add defensive panic branch to match
  codegen"). The other three observed warning categories
  (`-Wparentheses-equality`, `-Wunused-variable`,
  `-Wunused-but-set-variable`) plus `-Winteger-overflow` (which turned out
  to be the pre-existing `overflow_wrap_mask.sv0` fixture already
  deliberately exercising Site 4's exact overflow-wraps assumption — more
  direct evidence for that audit finding) were genuinely narrow and
  suppressed with grounded rationales. Final real run: 114 fixtures, 56
  suppressed, 16 tracked, **0 unclassified**. Confirmed load-bearing by
  mutation on all three modules.

  **NEX-050a/b landed**: new `extra_cc_args` seam threaded through
  `build_dev_profile_argv` → `build_native_executable` (omitted stays
  byte-identical). `native_exe_sanitizer_build.py` confirmed, by direct
  observation, that UBSan's default mode is print-and-continue (not
  fatal) — the deliberately-UB fixture from NEX-048b still exits 0, but
  prints a real runtime-error diagnostic to stderr, which is what the
  check actually asserts on. `sv0c/runtime/lsan-suppressions.txt`
  documents the `Vec<T>` table's intentional never-freed allocation, and
  honestly records that LeakSanitizer doesn't run on macOS at all (this
  dev machine), so the file is inert-but-present here and load-bearing
  only on NEX-050c's future Linux CI job. `native_exe_sanitizer_matrix.py`
  ran every NEX-048b fixture at both `-O0` and `-O2`, confirming both GCC/
  Clang honor the last-specified `-O` flag rather than assuming it.

  **NEX-050c landed**: `scripts/native_exe_sanitizer_corpus.py` ran the
  FULL 114-fixture behavior corpus under ASan+UBSan for real, not just a
  sample — **114 programs, 1 known/expected finding
  (`overflow_wrap_mask.sv0`, already-known Site 4 evidence), 0 unexpected
  findings**. Landed as a new CI *step* inside the existing `verify` job
  rather than a wholly separate job (avoids re-paying the SML/NJ bootstrap
  cost the job already pays) — a pragmatic, honestly-noted deviation from
  the plan's literal "new job" wording; the substance (a real, wired
  sanitizer check) is unchanged. Caught a genuine bug in the selftest
  itself before landing: the first version derived its fast-sample rows
  from `KNOWN_SANITIZER_FINDINGS`, so a mutation emptying that dict
  silently shrank the sample instead of failing — the exact
  self-defeating-test pattern NEX-034/036 caught earlier this project;
  fixed by hardcoding the known-finding fixture's name independently of
  the data under test.

  **NEX-051a/b landed**: `build_release_profile_argv` (`-O2
  -fno-strict-aliasing`, explicitly none of §16.5's prohibited flags) plus
  `native_exe_release_parity.py`'s full-corpus dev-vs-release diff — real
  run: **114 programs, exactly 1 known-divergent
  (`overflow_wrap_mask.sv0`), 0 unexpected**. Since the real corpus never
  diverges, mutation-testing the comparison itself required extracting it
  as a pure function (`observations_diverge`) and unit-testing it with
  synthetic tuples — an end-to-end-only test could never prove this
  load-bearing, the same lesson as NEX-034/036/050c's caught
  self-defeating tests, applied proactively this time instead of after
  the fact.

  **NEX-051c landed**: new `profile` parameter on `build_native_executable`,
  validated at phase 0, before entry validation — matching NEX-028 case5's
  own "usage errors first" precedent, but only after the FIRST version
  validated it too late (deep after the core compiler ran) and got caught
  by its own ordering mutation test. That mutation test's proof (a
  nonexistent input path) surfaced a real, separate, pre-existing gap:
  `validate_entry_exists` has no clean handling for a missing file at all
  — an unhandled `FileNotFoundError`, not a `BuildError`. Filed as its own
  follow-up task (`task_8b10596d`) rather than fixed inline; the
  ordering proof still uses a nonexistent path deliberately (profile
  validation now intercepts before ever reaching the broken code, so the
  correct implementation never triggers it — only a regression would).

  **NEX-051d landed partial**: a real `native-exe-clang` CI job (`CC=clang`,
  scoped to the native-exe checks) — verified locally first (114/114
  programs across all 3 checks), with an honest caveat recorded: this dev
  machine's `cc`/`clang` are the same Apple Clang binary, so the local
  proof is of the mechanism, not an actual GCC-vs-Clang divergence. The
  macOS Apple Clang arm64 leg stays deliberately deferred — needs its own
  unverified-runner-type SML/NJ bootstrap with no meaningful way to
  dry-run it locally, so it's documented (updated in
  `sv0c/doc/native-executable.md`) rather than applied speculatively.
  PORT-005 is therefore **partial**, not fully closed — recorded honestly
  as such rather than claimed done.

  **NEX-052a/b landed**: `OutputLock` + wiring into
  `native_exe_publish.publish_atomically` + a 6-way same-output stress
  test. Caught a genuine bug in the lock's own first implementation: a
  rename-based claim scheme, which isn't actually mutually exclusive
  (POSIX `rename` silently overwrites an existing destination) — the
  6-thread concurrent-holder test observed 6 simultaneous holders where
  exactly 1 was expected, root-caused, and fixed with a real `O_EXCL`
  create. Documented honestly that `os.replace` was already atomic before
  this slice, so AC-021's core correctness bar was already met by
  construction — the lock's actual contribution is coordination, not new
  safety.

  **NEX-053a landed**: a genuine, unassumed finding — two builds of the
  identical source from different scratch dirs are never byte-identical
  on this dev machine, root-caused via `dwarfdump --uuid` to Mach-O's
  `LC_UUID` load command (`ld64` regenerates it randomly on every link,
  no known suppression flag) — exactly §21.3's anticipated
  platform-prevents-byte-identity case. `native_exe_repro_harness.py`
  classifies each comparison `byte-identical`/`semantic-only`/`divergent`,
  confirming the divergence is fully explained by the UUID region plus
  its derived trailing signature bytes, not just present alongside a
  separate real bug. A second, independent nondeterminism source
  (debug-info-embedded scratch paths under `-g`) was found and
  deliberately left undocumented-as-solved rather than silently masked —
  flagged as its own gap. Confirmed load-bearing by mutation, after the
  first mutation-test attempt was itself insufficient (only checked the
  platform-level classification, which a real divergence would also
  satisfy) — added a direct classifier unit test on two genuinely
  different builds before the mutation actually caught anything.

  **NEX-053b landed**: `build_record` gained an optional `reproducibility`
  field, recorded verbatim from the caller (never recomputed or
  reinterpreted) — an explicit `None` when unchecked, never an omitted
  key. "CI job running 053a" turned out to already be satisfied:
  `native_exe_repro_harness.py --selftest` is already wired into `sv0
  test-guards`, which the existing `verify` job's `./scripts/sv0 test`
  step runs — no separate job needed. Confirmed load-bearing by mutation:
  hardcoding the field to `None` is caught immediately.

  **NEX-054a landed**: `native_exe_entry_abi.py` — `ENTRY_ABI_VERSION = 1`,
  a genuinely new concept parallel to (not reusing) the runtime bundle's
  own `ABI_VERSION`. Both hosted-adapter shape strings are copied
  byte-for-byte from `megaTU-main.sv0`'s actual emission code, not
  paraphrased. A version-number difference is treated as the allowed
  escape hatch (not an error) — only a hash mismatch AT the same version
  is a real drift. Confirmed load-bearing by mutation: changing an adapter
  shape string without a version bump is caught immediately.

  **NEX-054b landed**: `verify_entry_abi_compat` — the RUNTIME-facing half
  of ENTRY-010, distinct from 054a's repo-hygiene drift check — wired
  directly into `build_native_executable`'s pipeline, plus
  `entry-abi-manifest.json` added to `native_exe_runtime_bundle.py`'s
  required-members list. `native_exe_entry_abi_install_mismatch.py`
  reuses NEX-046's installed-layout copy machinery to corrupt a copied
  manifest's version and confirm the build fails closed. Confirmed
  load-bearing by two separate mutations: the compat function itself, and
  its wiring into the real pipeline.

  **NEX-054c landed**: `sv0c/doc/entry-abi.md` — every claim checked
  against real, already-landed code, not aspirational. **NEX-054 (entry/
  runtime ABI) is now fully closed, 3/3.**

  **NEX-055a landed**: another real, directly-observed finding — a single
  benchmark capture showed a 506ms cold-start outlier on the minimal
  fixture, vs. a stable ~78-80ms median across 3 repeats.
  `capture_baseline` takes the median of `n_runs` for exactly this reason,
  rather than trusting one sample. Real baseline persisted to
  `sv0c/doc/perf-baseline.json`. Confirmed load-bearing by mutation: a
  no-op regression check is caught by both the plain-regression and
  waiver-scoping selftest cases.

  **NEX-055b landed**: a real, measured finding contrary to this band's
  own a-priori worry — `native_exe_core_compiler.py`'s `flock` does
  serialize the core-compiler sub-step, but measured to NOT be a
  practical bottleneck. 2 and 4 concurrent builds consistently showed
  1.7×–6.6× real speedup over sequential across repeated runs, because
  the host compile/link step (never serialized) dominates real build
  time. PERF-006 is genuinely met today; the flock's removal (NEX-055c)
  remains warranted for REL-004's own static-guard requirement, not
  because this measurement found a perf blocker. Confirmed load-bearing
  by mutation: forcing full serialization drops the measured speedup to
  1.02x, correctly failing the bar.

  **NEX-055c scoped (not implemented)**: the flagged highest-risk item got
  its own dedicated `EnterPlanMode` investigation pass, per the original
  plan. Confirmed by direct investigation: sv0 the language has no argv/
  getenv/stdin host builtin today (only `read_file`/`write_file`/
  `read_dir`) — the actual root cause of the global-control-file design,
  not merely a migration-shim laziness. Design settled on a minimal
  `getenv(name) -> string` builtin (not full argv) plus a non-flag-day
  migration path, following the exact precedent the three existing host
  I/O builtins already set. Recorded as a durable design doc
  (`sv0c/doc/native-executable-reentrant-core-compiler-design.md`) and
  deliberately deferred to its own dedicated future session — comparable
  in kind to NEX-016/017 (which each got their own pass) but larger in
  scope, touching the compiler's own entry-reading logic and self-host
  bootstrap correctness. REL-004 remains honestly open.

  **NEX-056a/b landed**: `native_exe_release_evidence.py` assembles one
  evidence bundle from four already-existing sources (never recomputing
  any of them), rejecting a dirty tree outright.
  `sv0c/doc/release-signing-workflow.md` documents the actual signing
  step as policy — never a fabricated-signature script — explicitly
  rejecting CI-automated signing to keep one deliberate human control
  point. Confirmed load-bearing by mutation: a no-op dirty-tree check is
  caught immediately. **NEX-056 (release-candidate evidence) is now fully
  closed, 2/2.**

  **NEX-057 landed**: a new `sv0c/doc/native-executable-stable.md`
  (splitting off `native-executable.md`, which stays F0/R0-scoped per its
  own title) — indexes and links the runtime/entry ABI, build-record
  schema, profiles, sanitizer/warning gates, reproducibility, performance,
  and release-evidence material already landed across NEX-042/048-056,
  plus the GOV-009 compatibility policy. Every claim checked against
  real, already-landed code, not aspirational.

  **NEX-058 landed — R1 closed**: `native_exe_canonical_compile.py` shares
  the exact canonical host-compile pieces `build_native_executable` uses
  (not a new implementation, not the full pipeline either — these scripts
  emit C themselves via the core compiler's own behavior). All 3 legacy
  scripts migrated off `cc -std=c99 -O0 -w`; full before/after
  differential parity confirmed for each by git-stashing the migration and
  re-running the originals — byte-identical results, including the two
  pre-existing, unrelated failures (`lib/checker.sv0` exit 232,
  `lib/parser.sv0` exit 197) that exist with or without this change. The
  new static duplication guard's own first run surfaced 3 *more*
  pre-existing manual recipes outside this slice's scope, exempted with a
  spec §26.5-grounded rationale and flagged as a real follow-up task
  rather than silently fixed or silently left unenforced. **R1 —
  optimization, reproducibility, and stability (NEX-048…058, 34
  sub-slices) is now closed: 33/34 done, NEX-055c scoped and honestly
  deferred to its own future dedicated session.**

  **NEX-059 landed — the CLI gap is closed**: R1 built the native-executable
  *engine* end to end but left it uninvokable — no command existed that a
  user could actually type. `native_exe_request.py`'s stale
  `--profile=release` rejection (written back before the release gate was
  real) is fixed to forward it through; `native_exe_cli.py`/
  `native_exe_request.py` now actually populate the R0.1 provenance flags
  (`--keep-c`, `--message-format`, `--build-record`) they'd carried fields
  for since NEX-039…041 but never wired from real argv; and a new
  `scripts/native_exe_main.py` is the real production entry point,
  dispatched via `./scripts/sv0 native-compile`. Verified the way this
  project verifies everything real: not just unit tests but an actual
  terminal invocation (`./scripts/sv0 native-compile hello.sv0 -o hello`)
  producing a real, runnable binary. Scope is deliberately the workspace
  spelling only — the installed `sv0c --emit=exe` spelling needs the real
  compiler's own argument-parsing internals changed (comparable risk to
  NEX-055c) and is deferred to a future NEX-060. `verify_native_exe_traceability.py
  --active-release R1` also separately flags 19 requirements with no
  relation to the CLI surface at all (documentation/security/portability/
  test-plan gaps) — real, pre-existing, and out of this slice's scope;
  noted rather than silently left for later rediscovery.

  **NEX-060 landed — the installed `sv0c --emit=exe` command is real**:
  scoping (060a) investigated the installed command surface and resolved
  OD-009 ("stable installed command transition from workspace adapter",
  overdue since "before R1"), **correcting NEX-059's own prior risk
  assumption**: `sv0c/build/sv0c` is the unrelated SML bootstrap heap
  image, not an argv-parsing compiler binary — there was nothing to
  "modify the internals of." The scoping investigation showed this to be
  a packaging problem, not a compiler-internals one, and genuinely
  lower-risk than NEX-055c — so, unlike NEX-055c, it was implemented in
  the same pass rather than deferred. `native_exe_installed_layout.build_install_layout`
  (060b) assembles a real installed tree (`<root>/scripts/native_exe_*.py`,
  `<root>/sv0c/runtime/*`, `<root>/build/sv0-megatu-native`) that the
  driver's *default* path resolution already handles with zero code
  changes — proven with a real subprocess, no override seams, at a
  space-containing relocated prefix. `native_exe_install_launcher.write_sv0c_launcher`
  (060c) writes the `<install>/bin/sv0c` launcher itself: a thin shell
  script forwarding argv unmodified into the same driver
  `./scripts/sv0 native-compile` already used, proven behaviorally
  identical to it end to end. `native-executable.md`'s worked examples now
  lead with the installed spelling (060d, per spec line 472), correcting
  two stale claims found along the way (the R0.1 flags are now
  implemented; `sv0.toml` discovery, NEX-043, is still genuinely unwired —
  stated honestly rather than silently dropped). Traceability re-run (060e)
  confirms `CLI-017` is genuinely resolved — and separately documents a
  real tooling-honesty finding: the guard's uncited-count also dropped for
  9 unrelated requirements purely because NEX-059's own honest disclosure
  prose satisfied its naive "mentioned anywhere" citation check, not
  because they were actually addressed; recorded rather than let the
  guard's output imply otherwise. Also surfaced, flagged separately, not
  folded in: `--emit=c` (NEX-039) is fully built but unreachable from
  either CLI surface today.

  **Post-R1 traceability audit, step 1+2 complete**: step 1 fixed
  `verify_native_exe_traceability.py`'s citation-context blindness
  (`extract_cited_ids` now only counts a real header/`requirement(s)`-column
  citation, never a row's free-text prose). Step 2 audited every one of
  the 25 requirements the fixed guard then honestly flagged under
  `--active-release R1`. Root cause found for most of the CLI-0xx gaps:
  NEX-059's own section header used a compact `CLI-007/008/010…013/017`
  shorthand the guard's regex cannot actually parse (a bare `/008` has no
  prefix letters to attach to) — fixed by rewriting it in the
  comma-separated convention every other header already uses. 15 more
  citation-hygiene fixes added real IDs to rows/headers whose underlying
  work already covered them (`TOOL-011`, `SEC-007`, `PIPE-010`, `PORT-003`,
  `SEC-010`, `CLI-011`, `SEC-008`, `CLI-012`, `ENTRY-009`, `PORT-002`,
  `GOV-006`, `TEST-003`, `PIPE-012`, `SEC-011`, `GOV-009` — several with
  an honestly-stated completeness caveat rather than an unqualified
  claim). Two genuine gaps got real new code: `CLI-008` (new
  `scripts/native_exe_input_validation.py`, closing the file-mode
  regular/readable/`.sv0`-extension requirement AND the previously-flagged
  `validate_entry_exists` raw-crash follow-up in the same fix) and
  `CLI-013` (`--verbose` was parsed all the way into `NativeBuildRequest`
  but silently never read by `native_exe_main.py` — a real dead flag,
  now wired to print real build detail). `DOC-004` needed real new doc
  content (`native-executable.md`'s new "Diagnosing a host compile
  failure" section, sv0c commit `a3a40fe`). Six requirements remain
  genuinely, honestly open after this pass — `GOV-007`, `REPRO-001`,
  `SEC-012`, `TEST-005`, `TEST-006`, `TOOL-013` — each recorded in the
  checklist's own "Traceability audit" section with why it's still open,
  not silently dropped. `verify_native_exe_traceability.py
  --active-release R1` now reports exactly those 6, matching the audit's
  own conclusion precisely.

  **NEX-061 (a-f): all six closed, and `test-guards`' traceability gate
  bumped to R1.** Per the user's explicit choice to resolve every gap
  before bumping the gate: `TOOL-013` — a real, grounded supported
  compiler-family/version matrix, pinned into release evidence.
  `GOV-007` — a real known-conflict registry formalizing 4
  previously-only-informally-mentioned conflicts (2 pre-existing
  self-host-loop failures, the enum-match missing-else-branch gap, the
  2 strict-aliasing sites), with actual teeth: an open blocking entry
  raises and stops release-evidence assembly. `SEC-012` — a real threat
  model + mitigation table cross-referencing every actual defense this
  driver already has, and an honest security-contact statement (no
  dedicated one exists yet; a private GitHub Security Advisory is the
  documented interim channel). `REPRO-001` — a real finding:
  `sv0_read_dir`'s own `qsort` already made project-mode file ordering
  deterministic by construction; a new test proves it against a real
  reversed-creation-order perturbation. `TEST-005` — a systematic guard
  requiring every reachable error phase to have a real negative-test
  reference, with 3 phases honestly excluded by name and rationale
  (`CONFIG`: unwired `sv0.toml`; `INTERNAL`: no deliberate raise site;
  `INTERRUPTED`: exit-code-only, real cancellation is a distinct
  exception class). `TEST-006` — the test harness itself now pins
  locale/timezone/umask (`scripts/sv0`'s `run_python_guards`); temp
  root, cwd, and compiler selection already held by construction. Every
  module has its own `--selftest` and a genuine mutation test.
  `verify_native_exe_traceability.py --active-release R1` now reports
  **0 blocking gaps** — `scripts/sv0`'s own `test-guards` invocation is
  bumped from its old no-flag default (F0) to `--active-release R1`,
  making every one of these citations load-bearing in CI going forward,
  not merely informational.

  **NEX-062 landed — `sv0.toml` is genuinely wired now**, closing one of
  the two remaining known gaps surfaced when asked "is the sv0c runtime
  spec fully implemented?" `native_exe_request.normalize_request` now
  calls `discover_config`/`load_config`/`resolve_precedence`
  (`native_exe_config.py`, NEX-043 — previously tested in isolation only)
  for all six `[build]` keys, with `c-compiler`/`output-dir` resolved
  against the config file's own directory per §17.4, and CLI values still
  winning outright per §11.4. Required changing `native_exe_cli.ParsedArgs.profile`/`.contract_mode`
  from string defaults to `None`, since the old design had no way to tell
  "not given" apart from "given the default value" — a real precondition
  for correct precedence, not a cosmetic change (confirmed zero
  regressions against both modules' existing corpora). Manually verified
  end to end: a real `sv0.toml` (`profile = "release"`,
  `contract-mode = "disabled"`) drove a real build with zero CLI flags,
  producing the release argv shape and the contract-stripped exit code
  exactly as configured. `config_path` is now real and surfaced in
  `--verbose` output and build records, per §17.3. 6 separate mutations
  confirmed load-bearing. The one remaining, explicitly out-of-scope-for-
  this-slice item: the `SV0_CC` environment-variable compiler tier
  (`native_exe_cc_select.py`'s own docstring names it as a separate,
  still-unimplemented R0.1 precedence tier).

  **NEX-063 landed — `--emit=c` is wired on both CLI surfaces**, closing
  the last of the small, known wiring gaps named when asked "is the sv0c
  runtime spec fully implemented?" `native_exe_cli.py` now accepts
  `--emit=c` (`ParsedArgs.emit_value`), enforcing `emit_c_only`'s own
  documented contract at parse time: an explicit `-o` is required (no
  default-naming rule exists for `--emit=c`), and `--keep-c`/
  `--build-record`/`--message-format=json` are all rejected in
  combination (none has a coherent meaning with no executable artifact
  or compiler identity to report). `native_exe_main.py` dispatches to
  `emit_c_only` instead of `build_native_executable` on `Emit.C_ONLY`,
  honoring `--quiet`/`--verbose` the same way. Real finding along the
  way: `native_exe_emit_c.py` was missing the same CLI-008 file-input-
  shape hardening the executable path already has — fixed in the same
  slice, closing a parity gap rather than leaving it. Manually verified
  end to end on both CLI surfaces (the workspace adapter and a real
  installed `sv0c` binary): both wrote real, correct C; the `-o`-required
  and invalid-value usage errors both worked as documented. All new logic
  confirmed load-bearing by mutation.

  **NEX-064 landed — the `SV0_CC` compiler-selection tier is real**,
  closing the last known small wiring gap. `native_exe_cc_select.select_cc`
  now checks `SV0_CC` between the unified explicit (`--cc`/`sv0.toml`)
  tier and `CC`, completing the spec's own stated 5-tier order. No
  changes needed outside that one module — `native_exe_build.py` already
  passed real `os.environ` through, so `SV0_CC` was picked up
  automatically once `select_cc` itself checked for it. Manually verified
  end to end on both CLI surfaces: `SV0_CC` pointed at a real wrapper
  compiler while `CC` was deliberately broken — the build succeeded via
  `SV0_CC`, `CC` was never reached. **With this slice, the sv0c
  native-executable driver's compiler-selection precedence is fully
  implemented.** The sv0c runtime-executable spec now has exactly one
  genuinely unimplemented item left in this whole body of work:
  NEX-055c (REL-004, the reentrant core-compiler entry point) — plus the
  deliberately-deferred macOS CI leg.

  **NEX-055c deepened (still scoped, not implemented)**: user-requested
  scoping of the `getenv` builtin specifically, one level deeper than the
  original design doc. Real corrections found by reading the current
  source directly rather than trusting the earlier sketch: `write_bytes`
  (the most recently added host builtin, checker id 16/lowering id 18) —
  not `read_file` — is the current, trustworthy template, and it
  establishes a genuine existing precedent for a builtin shipping
  C/native-only, entirely absent from `sv0vm`'s interpreter, exactly the
  shape `getenv` needs; and `read_file` actually panics on a missing file
  (re-read directly) rather than the "returns empty on absence" contract
  the original doc assumed — `getenv`'s real contract must be the
  opposite (return `""` on an unset variable, never panic), since the
  core-compiler wrapper needs to detect "channel not present" and fall
  back to the legacy control file. New doc:
  `sv0c/doc/native-executable-getenv-builtin-scoping.md` (commit
  `c0138ae`) names the exact five touch points with concrete builtin ids
  (17 in `checker.sv0`, 19 in `lowering.sv0`/`megaTU-main.sv0`) and a
  verification plan anchored on the 114-program native behavior corpus
  and the self-host loop staying green. REL-004 itself remains open —
  only the builtin's own implementation shape was fully scoped at that
  point, ready for implementation.

  **NEX-055c's `getenv` builtin is now implemented** (the builtin
  itself — not the wider REL-004 migration, which stays deliberately
  deferred). Landed exactly per the scoping doc's five touch points, plus
  a real sixth found only by building and running a fixture: a third,
  independent, hardcoded string-returning-builtin name list
  (`lowering.sv0`'s `lower_init_is_string`) also needed `"getenv"` added,
  or a `let`-bound call compiled to a real C type error even though a
  direct call-as-argument worked fine — `read_file`/`read_dir` already
  had this exact triplicated-knowledge fragility, just never noticed
  before. Verified for real: rebuilt the native mega-TU compiler from
  these exact changes; 4 real fixtures compiled and run, confirming
  `getenv` reads a real environment variable and returns `""` (never
  panics) when unset; `sv0c`'s own `make test` (307/308, the one failure
  pre-existing and confirmed via a `git stash` clean-tree reproduction);
  the 114-program native behavior corpus unmodified; 3 stage0 C goldens +
  5 vm-parity bytecode goldens refreshed and diff-inspected. The
  self-host-loop's own `exit 232 for lib/checker.sv0` — and the
  `main.sv0b` vm-parity staleness it causes downstream — is the same
  pre-existing `KC-001` conflict, now root-caused precisely
  (`test_infer_lit`, an unrelated test, failing at offset 232, blocking
  the aggregator from ever reaching the new getenv assertions at offset
  580) and recorded in `native_exe_known_conflicts.py` with that exact
  root cause. REL-004 itself remains open — wiring `getenv` into
  `driver.sv0`/`megaTU-main.sv0`'s entry-reading logic, migrating
  `CoreCompilerClient`, and retiring `/tmp/.sv0_drv_path` stay
  deliberately deferred, a separate, later effort.

- **2026-08-28 — REL-004 migration step 2 of 6: `driver.sv0`/`megaTU-main.sv0`
  wired to the new channel.** `lib/driver.sv0`'s `fn main()` and
  `scripts/build-sv0-megatu-native.sh`'s injected compose-main both now
  prefer `getenv("SV0_DRV_REQUEST")` over the legacy
  `/tmp/.sv0_drv_path` control file when the env var is non-empty —
  strictly additive, so any caller that never sets it is unaffected.
  Two more real findings, again only via build-and-run, not static
  reading: the SML bootstrap compiler
  (`sv0c/sml-legacy/{name_resolution/resolver,type_checker/checker,ir/lowering}.sml`)
  is a wholly separate implementation from the self-hosted `.sv0`
  compiler already updated for `getenv` — it had never registered the
  builtin at all, since no committed `.sv0` source had ever actually
  called it until this wiring forced the first real call site through
  the SML path; and `lowering.sml`'s `exprInitCty` had no case for a
  bare local-variable identifier as an if-expression branch, mistyping
  `let y = if c { some_string_var } else { read_file(...) }`'s result
  temp as `int` — a real, pre-existing, getenv-unrelated bug, fixed by
  consulting the already-populated `scrutLocals` local-type table.
  Verified end to end: both `build/sv0-megatu-native` and
  `build/sv0-driver-native` rebuilt; byte-identical emitted C between
  the legacy-file and env-var paths for the same source, correct env-var
  precedence when both are set, and the env-path C actually compiles and
  runs. `sv0c make test` 307/308 (same pre-existing failure, git-stash
  confirmed); 114-program native behavior corpus unmodified; all stage0
  + vm-parity goldens diff-clean (no refresh needed this time);
  `./scripts/sv0 test` shows only the same pre-existing `KC-001`-class
  failures; `test-guards --active-release R1` green. sv0c commit
  `647d2a0`, parent pending. Step 3 (migrate `CoreCompilerClient` off
  its `flock`+file onto `SV0_DRV_REQUEST`) is next; REL-004 stays open
  until step 6 (legacy-path removal + static guard).

- **2026-08-28 — REL-004 migration step 3 of 6: `CoreCompilerClient`
  migrated off the `flock`, retired entirely.**
  `scripts/native_exe_core_compiler.py` rewritten: `invoke()` now passes
  `control_value` via a fresh, call-local `SV0_DRV_REQUEST` env-var dict
  straight into `subprocess.run` — no shared control file, no lock, no
  serialization of any kind. Every child process gets its own
  independent environment-block copy at exec time, so concurrent calls
  are structurally isolated, not merely lock-protected — this is the
  "unique private request channel" REL-004's own text asks for. The
  constructor dropped its now-dead `control_file`/`lock_file` params;
  both production callers (`native_exe_build.py`, `native_exe_emit_c.py`)
  needed zero changes since they only ever passed `compiler_path`
  positionally. New selftest proves isolation under real thread
  concurrency (verified load-bearing: deliberately reintroducing a
  shared-dict aliasing bug makes it fail) plus a deterministic
  env-dict-capture mutation guard and a parent-`os.environ`-non-mutation
  check. Real, not just unit-tested: a genuine `./scripts/sv0
  native-compile` build confirmed the legacy control file is never
  touched; 4 truly concurrent real OS-process builds (`&`/`wait`) each
  produced correctly isolated output. `native_exe_concurrent_perf.py`'s
  docstring updated — PERF-006's "no global control-file serialization"
  is now fully, structurally met. `./scripts/sv0
  test`/`test-guards --active-release R1` re-run clean (same two
  pre-existing failures only). Design doc sequencing updated in place.
  sv0c commit `fea0a71`, parent pending. Steps 4 (an explicit self-host-loop
  re-verification, done informally here), 5 (migrate `scripts/sv0`'s
  remaining direct control-file writers), and 6 (remove the legacy path +
  a static guard) remain — REL-004 itself is not yet closed.

- **2026-08-28 — REL-004 migration steps 4-5 done, step 6 honestly
  partial.** Step 4 formalized as its own checked step (not just an
  implicit side effect of step 3's verification): `./scripts/sv0 test`
  re-run fresh both immediately before and after step 5's changes,
  showing only the same two pre-existing failures both times — empirical
  confirmation that steps 3 and 5's pure driver-layer changes never put
  self-host-loop correctness at risk. Step 5: `scripts/sv0`'s
  `run_compile`/`run_emit_verified` migrated to `SV0_DRV_REQUEST`; two
  harmless reset-only lines touching an unrelated separate control file
  (`/tmp/.sv0_verify_path`, used by `build/sv0-megatu-verify`) removed as
  dead code. Verified via real invocations of `sv0 verify`/`sv0
  compile`/`sv0 emit-verified` against real fixtures, each leaving the
  legacy file untouched; the emitted C compiled and ran correctly.

  Step 6, stated honestly rather than overclaimed: "remove the legacy
  control-file path entirely" (the design doc's own original text) is
  NOT safe today. A full-repo scan found the legacy file still
  load-bearing in at least 11 more files beyond steps 3/5 — including
  `build-sv0-megatu-vm-native.sh`, a **separate** injected compose-main
  for the VM-bytecode emitter target that step 2's `SV0_DRV_REQUEST`
  wiring never touched at all. Removing the legacy path today would
  break every one of them. What step 6 DOES deliver: new
  `scripts/native_exe_no_new_legacy_control_file.py` (mirroring
  NEX-058's `native_exe_no_duplicate_cc_recipe.py` precedent) — a static
  guard, wired into `test-guards`, failing closed if any file outside
  its exhaustive 13-file allowlist references the legacy control file,
  stopping the channel from growing further while the real migration of
  its remaining callers stays a tracked, separate future effort.
  Verified load-bearing by mutation. `test-guards --active-release R1`
  and traceability both green throughout. sv0c commit `e0da799`, parent
  `048870b`. **REL-004 remains open** — full closure needs the 11+ files
  above migrated and this guard deleted entirely.

- **2026-08-28 — REL-004 CLOSED.** Continuing past the prior checkpoint,
  chunks 1-5 finished migrating every genuinely temporary legacy-file
  caller: 3 shell parity scripts; the VM-native compose-main (unblocking
  `sv0-vm-tier2-native-emitter.sh`); both wrapper-*generating* scripts'
  emitted wrapper bodies (internal implementation only, external
  argv/stdout contract unchanged, verified byte-identical for every
  invocation shape); `verify_behavior_corpus_native.py`'s dead reset;
  and a genuine gap the guard's own scripts-only scan had missed —
  `.github/workflows/self-host-native.yml` had a real unmigrated write,
  found by an exhaustive repo-wide check and fixed alongside extending
  the guard to scan `.github/workflows/` too. The real finding from
  finishing that pass: every temporary caller was now migrated, leaving
  only entries that existed *because* `driver.sv0`/`megaTU-main.sv0`
  still had a legacy fallback at all — meaning removing it for real was
  finally safe.

  Did exactly that: `lib/driver.sv0`'s `fn main()` and both compose-main
  templates (`build-sv0-megatu-native.sh`, `build-sv0-megatu-vm-native.sh`)
  no longer read `/tmp/.sv0_drv_path` under any circumstance —
  `getenv("SV0_DRV_REQUEST")` (never panics on unset) is the sole entry
  channel. `scripts/sv0`'s `ensure_*` guarantees, the wrapper-generator
  scripts' and CI workflow's file-init resets, and
  `assemble-sv0-megaTU.py`'s defensive reset were all removed as
  genuinely dead code in the same pass.

  **Real verification, not hypothetical**: rebuilt every affected binary
  with the legacy file deleted (not merely emptied) beforehand; confirmed
  correct behavior throughout. Compared against a true pre-change
  baseline via `git -C sv0c stash` (a parent-only `git stash` does NOT
  revert a dirty submodule's own working tree -- caught and corrected
  after an initial false-positive "identical" result that was actually
  comparing the same post-change file against itself): every migrated
  path's output is byte-identical before and after. **Incidental bonus,
  not chased further**: simplifying `driver.sv0`'s `fn main()` fixed a
  pre-existing native-C-compile failure for `lib/driver.sv0` itself in
  the two native-parity scripts (both now 97/99 instead of 96/99,
  `CCFAIL` 1→0) — a genuine improvement, root cause not investigated
  since it's strictly positive. `sv0c make test` 307/308 (same
  pre-existing failure); stage0/vm-parity goldens unaffected;
  `./scripts/sv0 test` shows only the same two pre-existing failures
  throughout, with the legacy control file never created at any point
  in the entire run. `native_exe_no_new_legacy_control_file.py` fully
  rewritten — its allowlist now holds only doc-only historical mentions,
  no more "real, unmigrated" bucket. `test-guards --active-release R1`
  and traceability both green. sv0c commit `b54077c`, parent pending.
  **REL-004 is closed** — the entire NEX-055c body of work is done. The
  only other known open item across this whole spec's F0/R0/R0.1/R1
  bands is the deliberately-deferred macOS Apple Clang CI leg
  (documented, not applied in `.github/workflows/ci.yml`).

- **2026-08-28 — KC-001 fixed for real, not just its cascading
  symptoms.** After pushing (the first push of this whole body of work),
  GitHub CI came back red on both jobs, tracing to `KC-001` -- confirmed,
  not assumed, since CI had simply never run against any of this
  session's ~120 local commits before. Root-caused precisely:
  `checker.sv0`'s test aggregator's `test_infer_lit()` asserted
  `infer_lit(1) == TY_BOOL()`, a mapping stale since `infer_lit`'s own
  lit_tag reassignment (bool moved from lit_tag 1 to lit_tag 5 once
  float literals correctly claimed tag 1 -- see `infer_lit`'s own
  BUGS.md #5 comment) -- the test was simply never updated to match. A
  second, independent test exercising the exact same stale mapping
  through a different path (`test_synth_expr()`'s `r1` case, whose
  literal push flows straight into `infer_lit()` via `synth_expr`'s
  `ExprLit` handling) was found while verifying the first fix alone
  wasn't sufficient, located precisely via a temporary debug build
  printing each aggregator check's name.

  Fixed both. Verified: `lib/checker.sv0` compiles and runs to exit 0
  standalone (was 232); stage0 + vm-parity goldens refreshed and
  confirmed deterministic; `sv0c make test` 307/308 (the one remaining
  failure is a distinct, unrelated SML test); the native behavior corpus
  unmodified; `test-guards` green. This also resolves `KC-001`'s own
  cascading symptom -- the self-host loop no longer aborts at
  `checker.sv0` early. It now reaches, and stops at, the next
  pre-existing issue in that same sequence, `KC-002` (`lib/parser.sv0`
  exits 197), a separate, already-tracked conflict this fix deliberately
  does not touch. `native_exe_known_conflicts.py`'s `KC-001` entry
  flipped from `"open"` to `"resolved"`. sv0c commit `bae2126`, parent
  `e382d86`.

- **2026-08-28 — KC-002 fixed too, in the same follow-up.** Same class
  of bug: `test_parse_assign()`'s `tags2`/`tags3` fixtures (testing
  `x.y = 1;` / `x.y += 1;`) pushed literal token tag 15 for the
  field-access `.`, but tag 15 is `COLONCOLON` (`::`) -- tag 16 is the
  real `TK_DOT`. The production fix correcting this exact confusion had
  already landed in `parse_assign_target_op_pos` (its own comment cites
  `BUGS.md #6`/sv0-mathlib), but these two test fixtures using the old
  tag were never updated, so `parse_expr` correctly rejected the
  resulting invalid `::` syntax and returned `-1` where the test
  expected `8`. Root-caused via the same debug-print technique used for
  `KC-001`. Fixed both fixtures to push tag 16.

  **With both KC-001 and KC-002 fixed, the self-host loop is fully green
  for the first time this whole project has observed it**:
  `self-host-sv0-loop: 99 file(s); emit+cc+run OK; SV0_SELF_HOST_COMPILER
  BEHAVIORAL parity with SML`. Verified: `lib/parser.sv0` compiles+runs
  to exit 0 standalone (was 197); stage0 + vm-parity goldens refreshed
  and confirmed deterministic; `sv0c make test` 307/308 (the one
  remaining failure is a distinct, unrelated SML test);
  `verify_behavior_corpus_native.py` 114/114; `test-guards` green.
  `native_exe_known_conflicts.py`'s `KC-002` entry flipped to
  `"resolved"`; `KC-001`'s own closing note updated since `KC-002` is no
  longer open. sv0c commit `da122d9`, parent `728ec8d`.

- **2026-08-28 — a THIRD, previously-invisible issue surfaced and
  fixed, filed as `KC-005`.** Pushing after the `KC-001`/`KC-002` fixes,
  GitHub CI still came back red on both jobs -- on a genuinely new root
  cause this time, confirmed by direct log inspection:
  `sv0c error: vm: unknown function 'sv0_getenv'`. The self-host loop's
  bootstrap-build step compiles every file in `bootstrap-sources.list`
  to VM bytecode, including `lib/driver.sv0` -- a step that had NEVER
  once succeeded in this project's history, since `KC-001`/`KC-002`
  always aborted the loop first. With both fixed, the loop finally
  reached `driver.sv0`'s own VM-bytecode compilation for the first time
  and failed there: NEX-055c/REL-004's `getenv("SV0_DRV_REQUEST")` call
  in `driver.sv0`'s `fn main()` has no `vm_codegen.sml` (SML VM
  bytecode emitter) support -- a touch point never in scope for
  `getenv` before, since nothing had ever needed to VM-compile a
  getenv-calling file.

  Asked the user directly rather than guessing: add VM codegen support
  for `getenv`, or exclude `driver.sv0` from VM targeting entirely. User
  chose exclusion -- `driver.sv0` is a native-only CLI entry point/test
  harness, not compiler-pipeline logic, and no VM program has any real
  use for a host-environment builtin. Removed it from all three lists
  that targeted it for VM compilation/parity (`bootstrap-sources.list`,
  `test/vm-parity/manifest.txt`, `test/vm-parity/tier2-manifest.txt`);
  deleted the now-orphaned `driver.sv0b` golden.

  Verified: `./scripts/sv0 test` now passes completely clean (exit
  0) -- bootstrap-build, vm-parity SML goldens (97/97, correctly reduced
  from 98), stage0 golden (22/22), and self-host-sv0-loop (99/99) all
  green; the optional tier-2 leg also green (17/17, previously always
  failed on `driver.sv0`). `sv0c make test` 307/308 (same one unrelated
  failure); native behavior corpus 114/114; `test-guards` green. New
  `KC-005` registry entry, immediately `"resolved"`. sv0c commit
  `0609a30`, parent pending.

  **All three self-host-loop conflicts this project has ever tracked
  (KC-001, KC-002, KC-005) are now resolved.**

## related

- `task/sv0c-runtime-executable-checklist.Rmd` — the `NEX-###` slice backlog
- `~/Documents/project-specs/sv0c-runtime-executable/SPEC.md` — governing spec
- `task/sv0-toolchain-milestone-5-llvm-crypto.Rmd` — active M5 (LLVM backend);
  explicitly a separate, non-conflicting future backend per spec §3.3/§4.5
- `task/sv0-toolchain-milestone-4-checklist.Rmd` — checklist-convention
  precedent (one-line result + commit per landed slice)
- `task/sv0-vsc-extension-plan.Rmd` / `-checklist.Rmd` — structural precedent
  for a spec-governed hub+checklist pair with no `roadmap_parent`
- `task/sv0-toolchain-roadmap-full.Rmd` — roadmap index
