---
created: "2026-08-23T00:00:00-06:00"
updated: "2026-08-23T00:00:00-06:00"
id: task-sv0c-runtime-executable
key: sv0c-runtime-executable
state: active
tags:
  - sv0
  - sv0c
  - native-executable
  - c-backend
  - host-driver
  - planning
title: "sv0c native runtime executable output: delivery plan (F0 → R1)"
type: task
---

# sv0c native runtime executable output: delivery plan

## purpose

Plan — **not yet implement** — the missing production step after the sv0 C
backend: a thin host driver that turns the C translation unit `sv0c` already
emits into a durable, runnable, host-native executable, governed by the
specification at `~/Documents/project-specs/sv0c-runtime-executable/SPEC.md`
(source-audit date 2026-08-23; audited revisions: `sv0-toolchain@f46144f`,
`sv0c@0705d68`, `sv0doc@f4dabf6`, `sv0vm@e9698f6`, `sv0-mcp@6f38d20`). This
file is the **hub**: scope, architecture, release ladder, phase sequencing,
gates, risks, and open decisions. The trackable **`NEX-###`** slice backlog
lives in **`task/sv0c-runtime-executable-checklist.Rmd`**, using the exact
slice IDs already assigned by the spec's own implementation backlog (§31), so
citing an ID in a PR is directly traceable back to the governing document.

This feature is **owned by `sv0c`** (the C backend, entry ABI, and runtime
bundle) with a thin `sv0-toolchain` driver adapter — it is **not** part of the
active M5 milestone (`task/sv0-toolchain-milestone-5-llvm-crypto.Rmd`). The
spec is explicit that the LLVM path is separate future work and **must not**
silently redefine this C-backend contract (spec §4.5, ownership table §3.3,
decision register note in §33.1). No `roadmap_parent` is set for the same
reason the VS Code extension hub omits one — this is adjacent to, not nested
under, the numbered design-milestone roadmap.

## where we start (2026-08-23)

A source audit (spec §4) found real capability but no production feature:

- **What already works:** the self-hosted compiler emits one C translation
  unit beginning `#include "sv0_runtime.h"`; `sv0_runtime.h`/`sv0_runtime.c`
  cover panic/assert/contract/string/vector/box/filesystem support; single-file
  and recursive `--project` composition both work; contract modes
  `runtime`/`verified`/`disabled` all lower correctly on the C path; a repeated
  working link recipe (`cc -std=c99 -O0 -I<runtime> program.c sv0_runtime.c -o
  program`) is proven by `make e2e test-contract-runtime` at the pinned
  revision on macOS arm64 with Apple Clang (42 / precondition-1 confirmed).
- **What does not exist:** `./scripts/sv0 compile-run` (`scripts/sv0:1088`) is
  the *only* thing resembling this feature today, and it is explicitly a test
  convenience, not the spec'd driver — it accepts only `sv0c/`-relative paths,
  writes into a temp dir, **deletes the binary after running it**, treats any
  nonzero program exit as a failed *compile* test, suppresses emitter stderr,
  hard-codes `-O0`, and has no stable output path, profile, or provenance
  contract (spec §4.2). There is no `--emit=exe`, no atomic publication, no
  entry-point ABI validation, no compiler-relative runtime resolution, no
  argv-safe hostile-path handling, and no structured exit-class taxonomy.
- **The concurrency blocker:** the current native compiler wrapper depends on
  a single global mutable file, `/tmp/.sv0_drv_path` (also called out in
  `[[feedback_drv_path_reset]]` — it must be reset to empty before any
  gate/push, and build scripts must never run concurrently with a pre-push
  `sv0 test` for exactly this reason). The spec makes retiring this dependency
  from the *stable* path a hard release blocker (AE-008, REL-004) — F0/R0 MAY
  serialize legacy invocations behind a lock as a migration shim, but that
  lock is not R0 completion.
- **C portability is honest, not strict.** `sv0_runtime.h` uses GCC/Clang
  extensions (`__typeof__`, statement expressions); `sv0_runtime.c` uses POSIX
  directory/file APIs. R0 is therefore a hosted POSIX, GCC/Clang-compatible
  build — it explicitly does not claim strict C99 or native Windows support
  merely because the generated source resembles C99 (spec §4.3, §20.3).

## scope decisions (locked by the spec, §10 + §33.1)

1. **Host-driver phase after C emission (AE-001).** The IR emitter never
   learns shell syntax, process spawning, or platform packaging; a thin driver
   invokes the core compiler and the host C compiler and owns artifacts.
2. **One generated `program.c` + the compiler-matched `sv0_runtime.c`, linked
   in a single host-compiler invocation through R1 (AE-002).** Separate
   sv0-module compilation, a runtime archive, LTO, and foreign libraries are
   deferred.
3. **Existing C-stdout and `--target=vm` behavior stays compatible; new
   executable output is opt-in via `--emit=exe`** (or the workspace adapter
   `./scripts/sv0 native-compile` during development) (AE-003).
4. **Explicit executable-entry adapter (AE-004).** The C backend must not emit
   a nonconforming `void`/aggregate/parameterized `main` directly as the
   hosted C entry, and must not fabricate a successful `main` for a source
   library that has none.
5. **Compiler-relative runtime discovery only (AE-005).** The driver resolves
   `sv0_runtime.h`/`.c` from the compiler installation, never from cwd,
   `CPATH`, or the source project — a malicious project-local
   `sv0_runtime.h` must never win.
6. **Atomic final publication (AE-006).** The host compiler writes to a
   private temporary target; only a validated, successfully linked regular
   executable is renamed over the requested final path. A failed rebuild
   leaves the prior executable byte-for-byte unchanged.
7. **Absolute bytecode isolation (AE-007).** `--target=vm`, `.sv0b` input, or
   VM-emission requests combined with `--emit=exe` are usage errors; `sv0vm`
   is never invoked by this path.
8. **No production global control file (AE-008).** The stable path passes
   inputs through ordinary argv or a unique per-invocation private channel;
   `/tmp/.sv0_drv_path` may remain inside legacy bootstrap tests only.
9. **Host-native only (AE-009).** Target triples, sysroots, cross-linkers, and
   remote builders are rejected or deferred rather than guessed.
10. **Development profile first; `--profile=release` is blocked until R1**
    optimized/sanitizer/UB-parity gates pass. No arbitrary `CFLAGS`/`LDFLAGS`
    shell strings in R0 (avoids quoting ambiguity and irreproducibility).

## explicit non-goals through R1 (spec §7.5)

`.sv0b`-to-executable conversion, bundling `sv0vm` into a launcher, LLVM/Wasm
executable construction, cross-compilation, universal/fat binaries, static
linking as a supported guarantee, shared-library/object/header SDK output,
incremental compilation or caching, a package registry, foreign-library
discovery (`pkg-config`, C FFI manifests), user `fn main` arguments,
source-level native debugging, code signing/notarization/installers, and
native Windows support before the runtime and driver are ported and gated.
These exclusions do not block a later spec — they keep untested behavior out
of this release implicitly.

## release ladder

| release | meaning | gate (spec §29) |
|---|---|---|
| **F0** | Feasibility + contract-locking slice; CLI grammar, phase/artifact state machines, executable-entry validation, discovery contracts, an argv-safe fake-tool harness, one minimal `-O0` proof; no public stability claim. | §29.1 |
| **R0** | Preview: durable dev-profile executables for single-file and `--project` inputs on the macOS-arm64/Linux-x86_64 matrix, atomic publication, human diagnostics with stable exit classes, behavior/project-corpus coverage. | §29.2 |
| **R0.1** | Hardened preview: `--emit=c`/`--keep-c` retention, `sv0.toml` config, `--message-format=json`, build-record JSON, installed-layout tests. | §29.3 |
| **R1** | Stable: `--profile=release` after optimized/sanitizer parity, ABI version checks, concurrent-build certification, reproducibility evidence, packaged-driver discovery, deprecation completion. | §29.4 |

## phases → checklist bands

Full per-slice detail (deliverable + red test) is in the checklist, using the
spec's own `NEX-###` numbering verbatim (§31) so the mapping never drifts.

| band | scope | release | slice range |
|---|---|---|---|
| **F0 — contract, harness, state machine** | requirement/traceability IDs, CLI parse for `--emit=exe`/`--project`/`-o`/`--`, normalized build request + phase enum, fake emitter + fake C-compiler executables, error-phase→exit-class map, atomic-publication abstraction, private scratch lifecycle, argv subprocess abstraction, VM/exe rejection | F0 | NEX-001…010 |
| **F0/R0 — core compiler + entry ABI** | reentrant core-compiler request interface (retires `/tmp/.sv0_drv_path` from the stable path), stdout/stderr/exit protocol normalization, no-`main`/duplicate-`main` rejection, entry signature/return-type validation, `sv0_user_main` + hosted C adapter + runtime init, reserved-symbol mangling, staging-C hash validation | F0/R0 | NEX-011…018 |
| **R0 — runtime + host toolchain** | compiler-relative runtime resolver, ABI manifest + hash verification, compiler-selection precedence (no fallback), capability probe, central dev-profile argv builder, sanitized child env + trusted include order, host-failure diagnostics, default/explicit path + permission/symlink rules, human quiet/verbose/success output | R0 | NEX-019…027 |
| **R0 — corpus, project, reliability** | native-executable manifest from the behavior corpus, runtime-feature suite (println/string/Vec/Box/contracts), project-mode build via compiler composition, contract-mode matrix, per-failure-phase preservation tests, symlink/scratch/race hardening, cancellation, parallel distinct-output builds, hostile-path corpus, Linux GCC/Clang + macOS Apple Clang jobs, user docs | R0 | NEX-028…038 |
| **R0.1 — inspection, config, provenance** | `--emit=c` atomic output, `--keep-c` retention, JSON event encoder/schema/timing, atomic build-record JSON with hashes, `sv0.toml` schema/discovery/precedence, compiler-identity probe cache, installed runtime manifest/licenses, installed-layout E2E, benchmark harness | R0.1 | NEX-039…047 |
| **R1 — optimization, reproducibility, stability** | UB/strict-aliasing audit, warning policy (drop blanket `-w`), ASan/UBSan matrix, `--profile=release` enablement, same-output lock/publication protocol, pinned double-build reproducibility, stable entry/runtime ABI manifest, regression-budget + concurrent perf certification, signed release-candidate evidence, stable published docs, duplicated manual `cc` recipe removal | R1 | NEX-048…058 |

`Future backlog, not scheduled by this specification` (native Windows,
cross-compilation, static/shared libraries, prebuilt runtime archive, program
arguments, C FFI manifests, LTO, signing/notarization, source-level debug
mapping, cache/incremental builds, and eventual convergence with a
backend-neutral CLI once LLVM ships) stays out of the checklist until
separately accepted through change control (spec §31.7).

## completion criteria per gate (condensed from spec §29)

- **F0:** AE-001…009 accepted; CLI/phase state-machine tests pass; missing or
  invalid `main` is rejected before host invocation; one minimal executable
  is produced through the canonical driver; fake-process injection, failure,
  and atomic-preservation tests pass; VM/executable combinations are rejected;
  no public stability claim is made.
- **R0:** every R0 requirement has passing traceability; single-file and
  project builds pass on the R0 host matrix; every supported behavior-manifest
  row passes through the canonical driver; runtime/verified/disabled contract
  modes are sound; runtime-bundle integrity checks pass; prior-output
  preservation, hostile paths, symlinks, cancellation, and distinct-output
  concurrency all pass; global-control-file dependence is absent from the R0
  path; existing full toolchain tests remain green; docs are complete.
- **R0.1:** retained-C identity + atomicity, configuration discovery/
  precedence, JSON message + build-record schema tests, installed-layout
  tests, a benchmark baseline report, and no unresolved R0 correctness or
  output-preservation defect.
- **R1:** all active R1 requirements pass; release profile passes behavior
  parity on every supported host tuple; ASan/UBSan + accepted warnings policy
  pass; compiler/runtime ABI is versioned and documented; same-output
  concurrency is certified; reproducibility scope is tested and documented;
  package contents include manifest/licenses/checksums/support policy; all
  waivers are time-bounded, owned, and noncritical; no known defect can
  corrupt, mispublish, or silently miscompile an executable.

## risk register (top items, full table at spec §32)

| risk | mitigation / descope trigger |
|---|---|
| Global `/tmp/.sv0_drv_path` race under concurrent builds | Reentrant argv/private request interface (NEX-011); static guard that the string never appears on the stable path; parallel-fingerprint tests (NEX-035). |
| Runtime header/source shadowing by a malicious project file | Private staging dir, trusted include order before inherited paths, content hashes, malicious-shadow fixture (NEX-019, NEX-024, AC-008/AC-016). |
| Nonconforming C `main` (void/aggregate/parameterized, or a fabricated no-op) | Entry validation + hosted adapter before host compile, never after (NEX-013…017). |
| Failed link truncates/replaces a known-good binary | Compile to a private temp leaf, validate, atomic rename only after success (NEX-007, NEX-026). |
| `-O2` exposes UB in raw box/vector representation | R0 stays `-O0`; UB audit, sanitizers, and dev/release parity are R1 gates before `--profile=release` unblocks (NEX-048…051). |
| Overstating "C99"/Windows portability | Runtime uses GCC/Clang extensions + POSIX APIs; docs claim only the honest GNU/Clang-compatible hosted matrix; Windows stays deferred (spec §20.3, DOC-003). |
| Shell injection via source/output/flag paths | Direct argv only, no shell-string flags, metacharacter corpus (NEX-009, NEX-036, AC-015). |
| Same-output concurrent publication mixes artifacts | Unique scratch + validated atomic publish now; an output lock/token protocol is an explicit R1 slice (NEX-052, AC-021). |
| Verified-mode false proof (missing/unknown Z3 misreported) | Requested-vs-effective contract mode always distinguished; only *proven* checks are ever stripped (NEX-031, PIPE-009, AC-017). |
| Test driver diverges from the user-facing driver | One canonical host-link implementation shared by tests and commands (GOV-008); duplication guard lands after parity (NEX-058). |

## open decisions (resolve before the named slice, spec §33.2)

| ID | decision | due | default recommendation |
|---|---|---|---|
| OD-001 | Implementation language/module for the host driver | before NEX-003 | **resolved 2026-08-23** — Python, alongside existing `scripts/verify_*.py`; see decisions record above. |
| OD-002 | Direct argv/request mechanism for the self-hosted core compiler | before NEX-011 | **partially resolved 2026-08-23** — investigated; the real ABI needs a new sv0 language/runtime argv host function (out of scope for this driver work, own future slice). NEX-011 shipped as the spec-sanctioned lock-based migration shim instead; see decisions record below. |
| OD-003 | Exact new `E####` codes for entry errors | before NEX-013 | Allocate from compiler diagnostic governance; keep missing/duplicate/params/return-type cases distinguishable. |
| OD-004 | Runtime ABI manifest schema + version numbering | before NEX-020 | **resolved 2026-08-27** — schema/ABI version 1, SHA-256 file hashes; see `sv0c/runtime/runtime-manifest.json` and `scripts/native_exe_runtime_manifest.py`. |
| OD-005 | `-std=gnu99` vs. documented `-std=c99` with accepted extensions | before NEX-023 | **resolved 2026-08-27** — `gnu99`; see `scripts/native_exe_argv_builder.py`. |
| OD-006 | Exact sanitized child environment allowlist on macOS/Linux | before NEX-024 | **resolved 2026-08-27** — see `scripts/native_exe_env.py`'s `ALLOWED_ENV_KEYS`/`BLOCKED_INJECTION_KEYS`. |
| OD-007 | Same-output publication locking primitive across macOS/Linux | before NEX-052 | Filesystem atomic create/rename + ownership token; do not rely on `flock` alone. |
| OD-008 | Build-record digest algorithm | before NEX-042 | SHA-256. |
| OD-009 | Stable installed-command transition from the workspace adapter | before R1 | `sv0c --emit=exe` canonical; `./scripts/sv0 native-compile` forwards, deprecates later. |
| OD-010 | Release-profile overflow/aliasing flags | before NEX-051 | Decide only from language semantics + UB/sanitizer evidence; no `-ffast-math`. |

These are controlled gates, not permission to improvise (mirrors the VS Code
extension hub's own framing) — do not silently default them inside an
implementation PR; record the resolution here when made.

## decisions record

Resolved 2026-08-23, in order:

- **OD-001.** Host driver implementation: **Python**, alongside the existing
  `scripts/verify_*.py` guard/test infrastructure this repo already trusts for
  exactly this kind of testable, argv-safe, atomic-filesystem code. `./scripts/sv0
  native-compile` becomes a thin bash→Python forward. `sv0c --emit=exe` is
  documented as the canonical spelling and delegates to this same Python
  driver until sv0's runtime gains process-spawn/exec host functions (tracked
  as a prerequisite for the self-hosted-driver alternative, not scheduled by
  this spec). Unblocks NEX-003 and the whole F0 band (NEX-001…010).
- **OD-002.** Investigated 2026-08-23 while implementing NEX-011: sv0 (the
  language) has no argv/CLI-args host function today — `sv0_runtime.h`/`.c`
  expose no such symbol, and the native mega-TU compiler's own CLI entry
  point is synthesized at build time by `scripts/build-sv0-megatu-native.sh`
  string-injecting a `/tmp/.sv0_drv_path` read into generated sv0 source,
  because `read_file(path)` is the only external-input channel a self-hosted
  sv0 program has. A real reentrant ABI needs a new host function threaded
  through the resolver/checker/C-lowering/runtime — and `sv0doc`, which owns
  language capabilities per §3.3's ownership table — a compiler-feature
  addition, not a driver change. **Decision: land the spec-sanctioned lock
  migration shim now (§22.2), scope the real argv builtin as its own future
  slice, not undertaken here.** NEX-011 shipped on this basis.

## progress dashboard (sv0-track)

<!-- sv0-track:begin {"id":"nex.plan.session","title":"sv0c native runtime executable — session hooks","kind":"checklist-region","ingest":"gfm","milestone":"NEX"} -->
- [ ] Open decisions (OD-001…010) resolved before the slice that depends on them starts
- [ ] Checklist (`task/sv0c-runtime-executable-checklist.Rmd`) slice status flipped + one-line result recorded when a `NEX-###` lands
- [ ] Requirement/acceptance ID cited in every implementation PR (spec Appendix D traceability rule)
- [ ] `/tmp/.sv0_drv_path` reset to empty (`printf '' > /tmp/.sv0_drv_path`) before any gate/push touching this area — see `[[feedback_drv_path_reset]]`
- [ ] `./scripts/sv0 test-guards` after `task/` edits; `./scripts/sv0 test` when driver/compiler code is integrated
<!-- sv0-track:end {"id":"nex.plan.session"} -->

## dependencies

/require: bash

## configuration

/env: SV0TOOLCHAIN_ROOT=.
/env: SV0C_ROOT=sv0c
/env: SV0DOC_ROOT=sv0doc
/env: SPEC_ROOT=~/Documents/project-specs/sv0c-runtime-executable

## prerequisites

- A self-hosted compiler that emits one C translation unit per program —
  **met** (existing `--target=c` default path, native mega-TU compiler).
- Repeated working `cc` link recipes proving the emitted C runs — **met**
  (`make e2e test-contract-runtime`, C integration fixtures).
- Contract-mode lowering (`runtime`/`verified`/`disabled`) on the C path —
  **met**; this feature reuses it unchanged (PIPE-008/009).
- external: a supported GCC/Clang-compatible host `cc` on the R0 matrix
  (Ubuntu 22.04 default `cc` + a supported Clang; macOS Apple Clang arm64).

## refinement log

- 2026-08-23: initial scaffold from `~/Documents/project-specs/sv0c-runtime-executable/SPEC.md`
  (draft governing specification, source-audit date 2026-08-23). Hub created
  with scope decisions (AE-001…009), release ladder, phase→checklist-band
  mapping onto the spec's own `NEX-###` numbering, condensed completion
  criteria, risk register, and open decisions. Confirmed by direct repo audit
  (`scripts/sv0:1088` `compile-run`) that no part of the spec'd feature exists
  yet — every checklist slice starts `todo`. State `draft`; flip to `active`
  when work begins on NEX-001.
- 2026-08-23: **OD-001 resolved** (Python host driver, alongside existing
  `scripts/verify_*.py`) — see decisions record above. State `draft → active`.
  **NEX-001 landed**: `scripts/native_exe_requirements.json` (140-id catalog)
  + `scripts/verify_native_exe_traceability.py`, wired into
  `sv0 test-guards`. The lint surfaced 4 real F0 citation gaps in the
  checklist as-drafted (ERR-002, PIPE-002, SEC-002, TEST-001) before it went
  green — fixed by adding the missing ids to their correct slices rather than
  loosening the check. F0 is now 0/40 blocking gaps; 27 higher-release ids are
  reported informationally and become blocking only when `--active-release`
  advances past F0. **NEX-002 landed**: `scripts/native_exe_cli.py`
  (`parse_args`/`ParsedArgs`/`UsageError`), 15-case table-driven parser
  corpus, wired into `sv0 test-guards`. Confirmed load-bearing by mutation
  (removing the `-o -` guard fails exactly that case). No production entry
  point yet — this is CLI-001…006 argv parsing only; wiring into
  `./scripts/sv0 native-compile` follows once NEX-003's `NativeBuildRequest`
  type exists. **NEX-003 landed**: `scripts/native_exe_phases.py`
  (`Phase`/`PhaseSequencer`, spec §12.5 atomic-publication state machine) and
  `scripts/native_exe_request.py` (`NativeBuildRequest`/`normalize_request`,
  Appendix A shape). Phase machine confirmed load-bearing by mutation
  (loosening the linear-order check fails the skip-ahead case). Path
  resolution is CLI-007 only (relative-to-invocation-cwd); default output
  naming (§12.1) and full path validation are still NEX-026. **NEX-004
  landed**: `scripts/native_exe_fake_emitter.py` — a real subprocess-invocable
  fake core compiler with all 7 spec §26.2 modes, mode selected via env var
  (not argv) so it stays indistinguishable from a real compiler call to
  driver code under test. Confirmed load-bearing by mutation (dropping the
  `diag` mode's stderr write fails that case). **NEX-005 landed**:
  `scripts/native_exe_fake_cc.py` — the host-compiler counterpart, all 7 spec
  §26.3 modes plus a `--version` probe responder, argv-recording proves
  hostile paths pass through literally (never shell-interpreted). Confirmed
  load-bearing by mutation (`zero-no-output` writing the file anyway fails
  that case). F0's harness pair (NEX-004/005) is now complete — every later
  PIPE/TOOL/ART/SEC slice can drive fully-fake builds before any real
  compiler is involved. **NEX-006 landed**: `scripts/native_exe_errors.py` —
  `DiagnosticPhase`/`exit_code_for`/`BuildError` centralizing the §18.2
  phase→exit-code table (2/3/4/5/6/7/8/70/130) so no driver code hand-rolls a
  `sys.exit` number. Confirmed load-bearing by mutation (misassigning
  `host-compile`'s exit code fails exactly that phase). **NEX-007 landed**:
  `scripts/native_exe_publish.py` — `validate_temp_output`/`publish_atomically`,
  enforcing ART-002…004 (validate-before-touch, byte-for-byte preservation on
  failure, correct `HOST_LINK` vs. `PUBLISH` phase classification). ART-004
  preservation exercised through `native_exe_fake_cc.py`'s real failure
  modes, not synthetic stand-ins. Confirmed load-bearing: removing the
  pre-publish validation call reproduces the exact bug this module exists to
  prevent (final-output corruption on a failed rebuild). **NEX-008 landed**:
  `scripts/native_exe_scratch.py` — `ScratchDir`, a context manager whose
  cleanup only ever removes the exact path `mkdtemp` handed back, never a
  prefix/glob scan (SEC-005). Confirmed load-bearing: swapping exact-path
  cleanup for a prefix-glob (the precise bug class this module exists to
  prevent) fails the neighbor-survival case. **NEX-009 landed**:
  `scripts/native_exe_subprocess.py` — `run_argv`, which structurally cannot
  invoke a shell (`shell` isn't a parameter; a bare string argument is a
  `TypeError`). Confirmed load-bearing by mutation (joining argv into a
  string + `shell=True` fails 4 of 6 cases immediately). **NEX-010 landed**:
  extended `native_exe_cli.parse_args` to reject `--target=vm` (any position)
  and `.sv0b` input with a distinct "bytecode isolation" message, exit 2,
  before `ParsedArgs` is ever built. GOV-003 (`sv0vm` never invoked) holds by
  construction — no subprocess-invoking code exists in this module yet; a
  real call-log assertion is deferred to when driver orchestration exists.
  Mutation testing caught a real gap on the first pass (the pre-existing
  generic "unknown option" fallback already raised *a* `UsageError`, so the
  dedicated guard wasn't actually proven load-bearing until the corpus was
  strengthened to assert on message content) — fixed before landing.

  **§31.1 F0 backlog (NEX-001…010) is now fully landed, 10/10, all green in
  `sv0 test-guards`, all mutation-verified.** This is not the same as the F0
  *release gate* (§29.1): that additionally needs the F0-labeled entry-ABI
  requirements (ENTRY-001…004) from §31.2's NEX-013…017, plus real
  end-to-end orchestration (NEX-011/012) — those are next.

  **NEX-011 landed** (as a documented migration shim, not the stable ABI —
  see OD-002 above): `scripts/native_exe_core_compiler.py` —
  `CoreCompilerClient`, a POSIX-`flock`-serialized wrapper around the legacy
  control-file binary. 4-case selftest incl. a genuine "before/after" proof:
  a fake compiler with a deliberate read-then-sleep-then-echo widens the race
  window so the unlocked path reliably cross-contaminates, while 6 concurrent
  requests through the real locked `invoke()` are always correctly isolated
  (stable across 15 repeated runs). Confirmed load-bearing by mutation:
  removing the `flock` call reproduces cross-contamination in 10/10 runs.
  Documented limitation: the lock only protects calls made through this
  module, not unrelated `scripts/sv0`/self-host-loop touches of the same
  file — matches existing `[[feedback_drv_path_reset]]` guidance.

  **NEX-012 landed**: `scripts/native_exe_emit.py` — `classify_emission`
  (PIPE-003 nonzero-exit-governs-first ordering, PIPE-004 empty-stdout
  detection, PIPE-005 stream separation preserved even on success, ERR-002
  verbatim diagnostic passthrough). 5-case selftest driven through the real
  fake emitter. Confirmed load-bearing by mutation: checking stdout
  emptiness before exit code — the exact ordering bug PIPE-003 exists to
  prevent — fails the `partial` case.

  **NEX-013 landed**: `scripts/native_exe_entry_scan.py` —
  `validate_entry_exists`, a comment/string-aware source scan for `fn main`.
  Investigated the C backend first: it currently *fabricates*
  `int main(void) { return 0; }` for a missing `main`, byte-identical in the
  emitted C to a real trivial one — confirming detection must happen on
  source text, not compiler output. Deliberately scoped as a pre-filter, not
  a reimplementation of project composition or semantic entry validation
  (§13.3). 8-case selftest incl. main mentioned only in a `//`/`/* */`
  comment, a string literal, or a hidden directory — none of which count.
  Confirmed load-bearing by mutation (dropping comment-masking fails 3
  cases; inverting the core check fails 5). **NEX-014 landed**: extended the
  same module — `count_main_declarations` + updated `validate_entry_exists`
  reject >1 `fn main` occurrence, whether within one file or spread across
  separate project files (10 cases total). Confirmed load-bearing by
  mutation: relaxing either the file- or project-mode uniqueness check fails
  exactly its corresponding case.

  **NEX-015 landed**: `scripts/native_exe_entry_signature.py` —
  `extract_main_signatures` (balanced-paren param extraction + return-type
  text scan, reusing NEX-013's comment-masking) + `validate_entry_signature`.
  11-case selftest: 3 accepted shapes + 7 rejected shapes (params, bool,
  wrong int width, struct, enum-shaped, reference, string) + 1 project-mode
  case. Confirmed load-bearing by mutation: disabling either the params or
  return-type check fails exactly its corresponding cases.

  **NEX-016 landed** (first real compiler change, planned via `EnterPlanMode`
  given the scope shift — see `/Users/sasank/.claude/plans/starry-discovering-quokka.md`).
  Investigation found 3 different C-backend `main`-emission sites in `sv0c`;
  only `sv0c/lib/megaTU-main.sv0`'s `megatu_emit_program` is on the path any
  currently-invoked binary (`build/sv0-megatu-native`, behind `sv0 compile`)
  actually runs — `codegen.sv0`'s `emit_program` is compiled but dead for
  output, `driver.sv0` only matters for native-parity testing. Modified only
  `megaTU-main.sv0` (the recognized-shape branch: zero params, `i32`/unit
  return → `sv0_user_main` + hosted `int main(argc, argv)` + `sv0_runtime_init`
  call; every other shape keeps the old plain emission unchanged) and
  `sv0c/runtime/sv0_runtime.h` (new no-op `sv0_runtime_init`, ABI slot
  reserved). Verified: 3 manual smoke fixtures (AC-001/002/003) plus the
  existing 112-program native behavior corpus
  (`verify_behavior_corpus_native.py`, part of `./scripts/sv0 test`) passing
  unmodified through the new shape — stronger evidence than adding synthetic
  fixtures, so none were added. Two `./scripts/sv0 test` failures observed
  (an SML-side checker self-test, and `self-host-sv0-loop` exit 232 for
  `lib/checker.sv0`) were confirmed **pre-existing and unrelated**: the SML
  one runs via `make -C sv0c test`, entirely independent of the two files
  touched here; the 232 one was reproduced byte-for-byte by `git stash`ing
  this change, rebuilding, and re-running the same check on the clean tree
  before restoring. Neither blocks this slice. `codegen.sv0`/`driver.sv0`
  deliberately untouched per the plan's documented scope.

  **NEX-017 landed** (planned via `EnterPlanMode` again, lighter-weight than
  NEX-016 — pure Python, no compiler changes): `scripts/native_exe_entry_reserved.py`
  — `validate_no_reserved_collisions`, rejecting a user-declared top-level
  `fn sv0_user_main`/`fn sv0_runtime_init` at the driver layer, before the
  host compiler ever runs. Verified first that today a colliding fixture
  produces a raw `cc` redefinition/conflicting-types error (host-compile,
  exit 6) rather than a clean early ENTRY diagnostic — chose driver-side
  rejection over compiler-side mangling specifically to avoid needing to
  rewrite call sites for a renamed function, and left the host compiler's
  own error as a defense-in-depth backstop. Deliberately scoped to the 2
  symbols NEX-016 introduced, not the ~20 other `sv0_`-prefixed runtime
  symbols. 5-case selftest, confirmed load-bearing by mutation (disabling
  the check fails all 3 collision cases).

  **NEX-018 landed**: `scripts/native_exe_staging.py` —
  `validate_staging_c` (PIPE-011: rejects staging C missing the runtime ABI
  include marker) + `hash_staging_c` (SHA-256). PIPE-006 (byte-identical C
  between `--emit=c` and `--emit=exe`) holds by construction — one emission
  call site, no per-mode divergence — proved with two independent fake-
  emitter invocations compared byte-for-byte and by hash. Confirmed
  load-bearing by mutation (disabling the marker check fails that case).

  **F0/R0 core-compiler + entry-ABI band (NEX-011…018) is now fully closed,
  8/8, all green in `sv0 test-guards`, all mutation-verified.** Combined
  with the fully-closed §31.1 F0 band, every NEX-### slice through NEX-018
  is done. Next band is R0 runtime + host toolchain (NEX-019…027).

  **NEX-019 landed** (first slice of the R0 runtime+toolchain band, planned
  via `EnterPlanMode` — see `/Users/sasank/.claude/plans/starry-discovering-quokka.md`):
  `scripts/native_exe_runtime.py` — `resolve_runtime_dir`/`RuntimeLocation`,
  derived from this script's own toolchain-root location, never cwd/env/
  project-relative. Confirmed load-bearing by mutation (disabling the
  missing-file check fails the fail-closed case).

  **NEX-020 landed** (touches `sv0c` — a plain JSON metadata file, not
  compiler logic, same risk class as the README SHA-pin bumps):
  `scripts/native_exe_runtime_manifest.py` — `load_manifest`/
  `verify_manifest`/`write_manifest`, plus the new checked-in
  `sv0c/runtime/runtime-manifest.json` (schema/ABI version 1, SHA-256
  hashes, OD-004 resolved). Confirmed load-bearing by mutation (disabling
  the header-hash comparison fails the corrupted-header case).

  **NEX-021 landed**: `scripts/native_exe_cc_select.py` — `select_cc`, the
  R0-relevant precedence tiers (`--cc` → `CC` env → `PATH` `cc`). Confirmed
  load-bearing by mutation: letting an invalid explicit `--cc` silently fall
  through to `CC`/`PATH` (the exact bug TOOL-002 exists to prevent) fails
  both explicit-rejection selftest cases.

  **NEX-022 landed**: `scripts/native_exe_cc_probe.py` — `probe_compiler`,
  `--version` + family classification followed by an actual compile+link+run
  of a minimal hosted program (catches a compiler that answers `--version`
  but can't produce a working binary, which a bare version check would
  miss). Confirmed load-bearing by mutation (disabling the compile-failure
  check still fails, one stage later, at the run step).

  **NEX-023 landed**: `scripts/native_exe_argv_builder.py` —
  `build_dev_profile_argv`, the one canonical Appendix B argv builder;
  OD-005 resolved (`-std=gnu99`, never `c99`). Confirmed load-bearing by
  mutation (swapping to `-std=c99` fails both the argv snapshot and the
  dedicated dialect check).

  **NEX-024 landed**: `scripts/native_exe_env.py` — `sanitized_child_env`,
  an explicit allowlist (OD-006 resolved) rather than a denylist, so a
  future injection variable can't slip through unnoticed. Confirmed
  load-bearing by mutation: reimplementing it as a copy-then-strip denylist
  immediately lets an unrelated var leak through.

  **NEX-025 landed**: `scripts/native_exe_host_compile.py` —
  `run_host_compile`, composing `native_exe_subprocess.run_argv` +
  `native_exe_publish.validate_temp_output` rather than reimplementing
  either. Confirmed load-bearing by mutation: disabling the nonzero-exit
  check misclassifies the phase (`HOST_LINK` instead of `HOST_COMPILE`) and
  loses the original stderr — caught immediately.

  **NEX-026 landed**: `scripts/native_exe_output_path.py` —
  `default_output_path`/`validate_output_path`/`ensure_output_parent_dir`.
  Reuses `native_exe_publish`'s lexists/islink symlink-safety pattern,
  applied pre-flight on the *destination* rather than post-flight on the
  temp source. Confirmed load-bearing by mutation (removing the
  symlink-leaf check fails that case).

  **NEX-027 landed**: `scripts/native_exe_human_output.py` —
  `format_success_message`/`maybe_print_success`/`render_argv_for_display`.
  Confirmed load-bearing by mutation (dropping `shlex.quote` fails the
  hostile-argv round-trip case).

  **R0 runtime + host toolchain band (NEX-019…027) is now fully closed,
  9/9, all green in `sv0 test-guards`, all mutation-verified.** OD-004,
  OD-005, and OD-006 all resolved along the way. Combined with the
  fully-closed F0 and F0/R0 bands, every NEX-### slice through NEX-027 is
  done (25 of 58). Next band is R0 corpus/project/reliability (NEX-028…038).

  **Re-scoping pass on NEX-028…038** (planned via `EnterPlanMode`): a direct
  codebase look found none of NEX-001…027's pieces had been wired together
  into a real build yet, and several of the stock slice descriptions
  (029/032/033/036) overlapped heavily with what NEX-007/008/019/021/023/024/026
  already mutation-tested at the unit level. Re-sliced around **NEX-028
  absorbing the actual assembly function** (`build_native_executable`) —
  every later slice in the band reuses it instead of deriving its own
  mini-pipeline. Also found: `sv0c/test/behavior/manifest.txt` already has
  112 rows that all pass through the native path today; real fixtures
  already exist for Box (`test/integration/box_expr/`) and project mode
  (`test/integration/modules/`); Z3 4.16.0 is installed in this dev
  environment, so the verified-contract-mode leg is directly testable, not
  just documented. Full re-slicing recorded in
  `/Users/sasank/.claude/plans/starry-discovering-quokka.md`.

  **NEX-028 landed** (re-scoped, see above): `scripts/native_exe_build.py`
  — `build_native_executable`, the first real wiring of the whole pipeline,
  and `scripts/native_exe_behavior_corpus.py`, proving all 112 behavior-manifest
  rows build+run correctly through it (not a hand-rolled separate recipe).
  Mutation testing caught a real selftest gap on the first pass — the
  fixture set was positive-only, so removing entry validation went
  unnoticed until a no-main negative case was added — fixed before landing.

  **NEX-029 landed**: `scripts/native_exe_runtime_feature_smoke.py` — Box
  (`test/integration/box_expr/box_expr.sv0`, real existing fixture) +
  stdout/stderr channel separation on a real compiled `requires`-violation
  binary. Found and corrected a stale assumption along the way: a doc
  comment in `test/integration/contract_violation/README.md` claims native
  contract checks are dropped — verified directly against the current
  compiler that this is no longer true (the `requires` check fires
  correctly, diagnostic on stderr, `println` on stdout). Filesystem I/O
  left undone, documented, since no fixture exists yet. Confirmed
  genuinely discriminating (not just "passes trivially") by swapping the
  stdout/stderr assertions and by asserting a deliberately wrong Box exit
  code — both fail as expected against the real binary.

  **NEX-030 landed**: `scripts/native_exe_project_mode.py` — reuses
  `build_native_executable` unchanged against the real, already-existing
  `sv0c/test/integration/modules/` project fixture (AC-006: exit 42 =
  `bump(41)`). Confirmed genuinely discriminating (a deliberately wrong
  expected exit fails against the real build).

  **NEX-031 landed**: `scripts/native_exe_contract_mode.py` —
  `compute_proof_lines`, reusing `scripts/sv0`'s own `run_emit_verified`
  pass-1 machinery (`build/sv0-megatu-verify` + `scripts/sv0-z3.sh`) rather
  than reimplementing Z3 orchestration. Tested against the real, installed
  Z3 4.16.0, not just documented. Confirmed load-bearing by mutation: making
  the z3-absent path falsely claim a proof (the exact bug PIPE-009 exists to
  prevent) is caught immediately; a wrong disabled-mode exit assertion fails
  too, confirming the check-stripping behavior is real.

  **NEX-032 landed**: `scripts/native_exe_preservation.py` — drives the
  real `build_native_executable` through all 6 directly-injectable failure
  phases, confirming a seeded known-good output survives byte-for-byte and
  mode-for-mode every time. Added a `runtime_override` seam to
  `build_native_executable` to make the RUNTIME phase injectable
  end-to-end. Mid-implementation discovery: `sanitized_child_env`
  correctly (by design) strips the env var used to steer the fake host
  compiler in tests — fixed by baking the mode into a tiny wrapper script.
  Confirmed genuinely load-bearing (not just individually-already-proven
  lower layers) by mutating `build_native_executable`'s entry-validation
  call and watching this test catch it too.

  **NEX-033 landed**: `scripts/native_exe_hardening_smoke.py` —
  consolidation over NEX-008/026, not new logic. Added a `scratch_base_dir`
  test seam to `build_native_executable` so the neighbor-scratch check is
  genuinely adjacency-sensitive (the neighbor lives under the same parent
  the real build's own scratch dir uses), not just "some unrelated
  directory elsewhere." Confirmed genuinely load-bearing: mutating the real
  symlink-rejection check and mutating scratch cleanup to a prefix-glob
  (SEC-005's exact bug class) are both caught end-to-end here, not just by
  their own originating modules' isolated tests.

  **NEX-034 landed** — the most genuinely new capability in this band:
  extended `native_exe_subprocess.py` with `run_argv_cancellable`, launching
  the child as a process-group leader (`start_new_session=True`) so
  cancellation can terminate the whole group, not just the immediate child.
  **Caught a real gap in my own test, not the production code**: the first
  mutation check (targeting only the leader PID) silently passed, because
  the test's timing let the background-grandchild marker-file check get
  skipped before the marker was ever created — fixed the test's timing and
  made "marker never created" a hard failure instead of a silent skip, then
  re-confirmed the mutation is genuinely caught. A useful reminder that "the
  mutation check passed" isn't itself proof of anything until you've
  confirmed *why* it passed.

  **NEX-035 landed**: `scripts/native_exe_parallel_builds.py` — 6 real
  concurrent `build_native_executable` calls under `threading`, each
  program's exit code its own fingerprint. Confirmed dramatically
  load-bearing: removing `native_exe_core_compiler.py`'s `flock` call
  reliably reproduces real cross-contamination and outright `read_file`
  panics across all 6 builds, on every one of 3 separate runs.

  **NEX-036 landed** — another real bug caught in the test, not the
  production code: `scripts/native_exe_hostile_paths.py`'s first version
  embedded a full sentinel *path* (with slashes) inside a hostile output
  filename, so `ensure_output_parent_dir` correctly rejected it as a
  malformed path before the test could exercise anything real — every
  "hostile" build was silently caught as an unrelated, legitimate
  `BuildError`, and mutating the exit-code assertion had zero observable
  effect because that code was never reached. Root-caused with debug
  tracing (not guesswork), fixed by using a bare sentinel *name* with no
  embedded separators. Confirmed genuinely load-bearing afterward: 6/6
  hostile shapes now build+run+exit correctly end to end, and a wrong
  expected-exit mutation fails all 6. The "no shell injection" check itself
  holds by construction here (hostile paths never become subprocess argv —
  they're laundered through fixed scratch-internal names first), documented
  as a defensive guard rather than an active mutation-proof.

  **NEX-037 landed** (documentation, not a CI change): recorded the required
  R0 host-matrix job configuration in `sv0c/doc/native-executable.md`
  against current CI reality (one Ubuntu job, no Clang leg, no macOS job at
  all) — the macOS arm64/Apple Clang leg has been continuously proven on
  this dev machine throughout the whole session; the Linux legs aren't
  locally reproducible here. Adding the actual CI jobs is a separate change
  with its own review (billed minutes, supported-platform claim), correctly
  left undone in this doc-only slice.

  **NEX-038 landed**: `sv0c/doc/native-executable.md` — user docs
  (DOC-001…003), stating current status honestly (engine real and tested,
  public CLI not wired up yet). Every example was verified against the real
  engine before being written down — the direct-Python-call example was
  wrong twice on the first attempts (missing `PYTHONPATH`; an explicit
  output path that doesn't auto-create its parent per ART-007) and was
  corrected against actual runs, not left as unverified prose.

  **R0 corpus/project/reliability band (NEX-028…038) is now fully closed,
  11/11, all green in `sv0 test-guards`, all mutation-verified (where
  applicable) or explicitly, honestly scoped as documentation-only where
  not.** Combined with the already-closed F0 (NEX-001…010) and F0/R0
  (NEX-011…018) bands and the R0 runtime+toolchain band (NEX-019…027), the
  entire R0 release is now closed: 20 slices from NEX-019 through NEX-038,
  every one of NEX-001 through NEX-038 done. Next up is R0.1 (NEX-039…047:
  `sv0.toml`, `--message-format=json`, build records, `--keep-c`,
  installed-layout tests) — a genuinely different kind of work (config
  parsing, JSON schemas) from anything in this band.

  **Re-slicing pass on NEX-039…047 for the R0.1 band** (planned via
  `EnterPlanMode`): confirmed `sv0c/LICENSE-APACHE`/`LICENSE-MIT` already
  exist (no new licensing decision needed for NEX-045) and Python 3.13 here
  has `tomllib` built in (no third-party TOML dependency for NEX-043).
  Structural decision: `--emit=c`'s "front half" duplicates a few lines of
  *sequencing* from `build_native_executable` rather than factoring out a
  shared internal helper — refactoring already-mutation-tested code carries
  more risk than five lines of repeated glue. Full re-slicing recorded in
  `/Users/sasank/.claude/plans/starry-discovering-quokka.md`.

  **NEX-039 landed**: `scripts/native_exe_emit_c.py` — `emit_c_only`, plus
  `native_exe_staging.write_text_atomically` (new, mirrors
  `native_exe_publish`'s atomic-write pattern for text). Confirmed load-bearing
  by mutation, on the second attempt: the first mutation (calling the fake
  cc with `--version`) silently passed because that mode never reaches the
  fake tool's own call-recording logic — root-caused and fixed by invoking
  the fake tool in a mode that actually records, which then caught the
  regression immediately.

  **NEX-040 landed**: `scripts/native_exe_keep_c.py` + a new `keep_c_path`
  parameter on `build_native_executable` — writes the staging C atomically
  right after `validate_staging_c` succeeds, unconditionally, before host
  compile ever runs, so it survives a later host-compile/link failure.
  Selftest needed a wrapper-script indirection (baking
  `SV0_FAKE_CC_MODE=fail` into an executable, the same trick
  `native_exe_preservation.py` uses) rather than a plain env-var injection,
  since `sanitized_child_env` correctly strips that var by design. Confirmed
  load-bearing by mutation: moving the write to after a successful host
  compile correctly fails the forced-failure retention case.

  **NEX-041 landed**: `scripts/native_exe_json_output.py` — `PhaseTimer` +
  `build_event`/`encode_event` matching §18.6's exact JSON schema. `total`
  is computed as the sum of already-rounded per-phase values (not a
  separately rounded raw sum) so it's exactly consistent with its parts by
  construction. Confirmed load-bearing by mutation: removing the
  nonnegative clamp is caught immediately.

  **NEX-042 landed**: `scripts/native_exe_build_record.py` — `build_record`
  matches Appendix C's exact shape, computing the two hash-bearing pieces
  itself (`hashlib.sha256` over the real published bytes and each input
  source file) rather than trusting caller-supplied digests, since the
  whole point of a build record is independent verifiability. Confirmed
  load-bearing by mutation: hardcoding the artifact hash is caught
  immediately by the digest-mismatch check.

  **NEX-043 landed**: `scripts/native_exe_config.py` — `discover_config`/
  `load_config`/`resolve_precedence`, using Python's built-in `tomllib`
  (no third-party dependency). Confirmed load-bearing by two independent
  mutations: dropping the key/type validation, and reordering the
  precedence tiers -- both caught immediately by their respective cases.

  **NEX-044 landed**: `native_exe_cc_probe.py` gained `probe_compiler_cached`
  (in-process, keyed by `os.path.realpath`). Its selftest had to wrap the
  *real* host `cc` rather than the fake tool, since `probe_compiler` runs
  the produced binary and a fake placeholder file isn't a real executable.
  Confirmed load-bearing by mutation: bypassing the cache lookup is caught
  immediately by the invocation-count checks.

  **NEX-045 landed**: `sv0c` gained `runtime/abi-version.txt` and
  `runtime/licenses/{LICENSE-APACHE,LICENSE-MIT}` (copies of the
  already-existing top-level license files -- no new licensing decision).
  New `scripts/native_exe_runtime_bundle.py` verifies the full §15.1 bundle
  shape. Confirmed load-bearing by mutation: checking only the first
  required member is caught immediately by the per-member missing-member
  loop.

## related

- `task/sv0c-runtime-executable-checklist.Rmd` — the `NEX-###` slice backlog
- `~/Documents/project-specs/sv0c-runtime-executable/SPEC.md` — governing spec
- `task/sv0-toolchain-milestone-5-llvm-crypto.Rmd` — active M5 (LLVM backend);
  explicitly a separate, non-conflicting future backend per spec §3.3/§4.5
- `task/sv0-toolchain-milestone-4-checklist.Rmd` — checklist-convention
  precedent (one-line result + commit per landed slice)
- `task/sv0-vsc-extension-plan.Rmd` / `-checklist.Rmd` — structural precedent
  for a spec-governed hub+checklist pair with no `roadmap_parent`
- `task/sv0-toolchain-roadmap-full.Rmd` — roadmap index
