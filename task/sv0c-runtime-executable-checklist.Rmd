---
created: "2026-08-23T00:00:00-06:00"
updated: "2026-08-23T00:00:00-06:00"
id: task-sv0c-runtime-executable-checklist
key: sv0c-runtime-executable-checklist
state: draft
tags:
  - sv0
  - sv0c
  - native-executable
  - c-backend
  - host-driver
  - planning
title: "sv0c native runtime executable output: slice backlog (NEX-###)"
type: task
---

# sv0c native runtime executable output: slice backlog

## purpose

The trackable **`NEX-###`** slice backlog for the native-executable-output
feature specified at `~/Documents/project-specs/sv0c-runtime-executable/SPEC.md`
§31. The **definition of done**, scope decisions, release ladder, and gates
live in the hub **`task/sv0c-runtime-executable.Rmd`**. This file is a
**living index** — flip status as slices land and keep it aligned with the
hub's phase narrative. No implementation has started (confirmed by repo audit
2026-08-23: only the test-convenience `./scripts/sv0 compile-run` exists); every
slice below is `todo`.

## how to use

- Pick the next slice by band (F0 → F0/R0 → R0 → R0.1 → R1); within a band
  slices are ordered by dependency exactly as spec §30.2 lists them.
- Each slice names the spec **requirement ID(s)** it implements and the exact
  **red test / acceptance** signal from spec §31. Cite the requirement and
  acceptance ID (`AC-0##`) in the implementation PR (spec Appendix D
  traceability rule).
- Every slice follows the spec's five-state cycle (§30.1): **Contract → Red →
  Minimal green → Adversarial expansion → Refactor and evidence**. A slice is
  not done at "minimal green" — filesystem/process/path/runtime-selection/
  publication slices require the adversarial-expansion state too.
- Use the fake emitter (§26.2) and fake C compiler (§26.3) contracts for every
  slice before wiring a real host compiler in; this is what lets PIPE/SEC
  tests run without rebuilding the self-hosted compiler per case.
- Add a one-line result + commit/PR when a slice lands, same convention as the
  M4 checklist (`task/sv0-toolchain-milestone-4-checklist.Rmd`).
- Reset `/tmp/.sv0_drv_path` to empty before any gate/push that touches this
  area, and never run these driver scripts concurrently with a pre-push
  `sv0 test` until NEX-011 retires the global control file from this path —
  see `[[feedback_drv_path_reset]]`.

## progress dashboard (sv0-track)

<!-- sv0-track:begin {"id":"nex.checklist.session","title":"sv0c native runtime executable checklist — session hooks","kind":"checklist-region","ingest":"gfm","milestone":"NEX"} -->
- [ ] Slice status flipped + one-line result recorded when a `NEX-###` lands
- [ ] Requirement + acceptance ID(s) cited in the implementation PR
- [ ] Hub (`task/sv0c-runtime-executable.Rmd`) phase narrative kept aligned with slice status
- [ ] `./scripts/sv0 test-guards` after `task/` edits; `./scripts/sv0 test` when driver/compiler code is integrated
<!-- sv0-track:end {"id":"nex.checklist.session"} -->

## configuration

/env: SPEC_ROOT=~/Documents/project-specs/sv0c-runtime-executable
/env: SV0C_ROOT=sv0c
/env: SV0TOOLCHAIN_ROOT=.

## slice backlog

Status: **todo** / **wip** / **done** / **deferred** / **blocked**.

### F0 — contract, harness, and state machine (spec §31.1)

| ID | slice | requirement(s) | status | red test / acceptance |
|----|-------|-----------------|--------|-----------|
| NEX-001 | Add requirement/evidence IDs and a traceability schema | GOV-001…005, TEST-001 | **done** | `scripts/native_exe_requirements.json` (140-id catalog, spec §23/§24) + `scripts/verify_native_exe_traceability.py`, wired into `sv0 test-guards`. Fails on any active-release (default F0) requirement uncited by a `NEX-###` slice row; `--selftest` covers 5 cases incl. range expansion. Verified: injecting a synthetic uncited F0 id exits 1 with the exact requirement listed; real catalog is 0/140 blocking at F0 (27 higher-release ids reported informational, not yet due). Fixed 4 real F0 citation gaps this surfaced (ERR-002→NEX-012, PIPE-002→NEX-013, SEC-002→NEX-019, TEST-001→NEX-001) before landing. |
| NEX-002 | Parse `--emit=exe`, `--project`, `-o`, conflicts, and `--` | CLI-001…006 | **done** | `scripts/native_exe_cli.py` (`parse_args`/`ParsedArgs`/`UsageError`), 15-case table-driven corpus (`--selftest`), wired into `sv0 test-guards`. Verified load-bearing by mutation: removing the `-o -` guard fails exactly that one case, nothing else. No production entry point yet — wiring into `./scripts/sv0 native-compile` is a later slice once NEX-003's request type exists. |
| NEX-003 | Define the normalized `NativeBuildRequest` type and phase enum (Appendix A shape) | PIPE-001 | **done** | `scripts/native_exe_phases.py` (`Phase`/`PhaseSequencer`, §12.5 state machine — happy-path snapshot + skip/backward/post-terminal rejection, 5 cases) and `scripts/native_exe_request.py` (`NativeBuildRequest`/`normalize_request`, Appendix A shape, consumes `native_exe_cli.ParsedArgs`, 7 cases incl. `--profile=release` rejection). Both wired into `sv0 test-guards`; phase machine verified load-bearing by mutation (loosening the ordering check fails the skip-detection case). |
| NEX-004 | Add a fake core-compiler executable (§26.2 modes: valid C, empty, partial+nonzero, warning-on-stderr, diagnostic+nonzero, hang-until-killed, argv/env recording, side-effect sentinel) | TEST-002 | **done** | `scripts/native_exe_fake_emitter.py` — mode selected via `SV0_FAKE_EMITTER_MODE` env var (never argv, so it's indistinguishable from a real subprocess call), all 7 §26.2 modes + unknown-mode rejection, `SV0_FAKE_EMITTER_RECORD`/`_SENTINEL` env hooks, 8-case `--selftest` run as real subprocesses. Wired into `sv0 test-guards`. Verified load-bearing by mutation (dropping the `diag` mode's stderr write fails that case). |
| NEX-005 | Add a fake C compiler/linker executable (§26.3 modes: record argv/env + valid output, nonzero w/ ordered streams, zero-exit-no-output, empty output, directory-at-output, sleep-until-cancel, write-then-fail, metacharacter-misjoin probe, family/version response) | TEST-002 | **done** | `scripts/native_exe_fake_cc.py` — mode via `SV0_FAKE_CC_MODE` env var (argv stays exactly what a real driver would pass); all 7 §26.3 build/link modes + `--version` probe response + literal (never shell-interpreted) hostile-argv passthrough, 9-case `--selftest` run as real subprocesses. Wired into `sv0 test-guards`. Verified load-bearing by mutation (making `zero-no-output` write the file anyway fails that case). |
| NEX-006 | Centralize error phase → exit-class mapping (spec §18.2 table: 0/2/3/4/5/6/7/8/70/130) | ERR-001 | **done** | `scripts/native_exe_errors.py` — `DiagnosticPhase` (12 §18.1 phases + `INTERRUPTED`), `exit_code_for`, `BuildError` carrying its own `exit_code`; one parameterized case per phase, exhaustiveness-asserted against the enum. Wired into `sv0 test-guards`. Verified load-bearing by mutation (misassigning `host-compile`'s code fails exactly that phase). |
| NEX-007 | Implement temporary-output validation + atomic publication abstraction | ART-002…004 | **done** | `scripts/native_exe_publish.py` — `validate_temp_output` (rejects missing/symlink/directory/non-regular/empty/non-executable, `BuildError(HOST_LINK)`) + `publish_atomically` (validates before ever touching `final_path`; a post-validation rename failure is classified `PUBLISH`/exit 8, not `HOST_LINK`). ART-004 preservation driven through `native_exe_fake_cc.py`'s real failure modes (byte + mode unchanged). Wired into `sv0 test-guards`. Verified load-bearing: removing the pre-publish `validate_temp_output` call reproduces exactly the bug this module exists to prevent (7 failures incl. final-output corruption). |
| NEX-008 | Implement unique, private scratch-directory lifecycle (owner-only perms, unpredictable leaf) | ART-009…010 | **done** | `scripts/native_exe_scratch.py` — `ScratchDir` context manager (`tempfile.mkdtemp` + owner-only perm assertion; cleanup only ever `rmtree`s the exact path it created, never a glob/prefix scan). 5-case `--selftest` incl. the literal red test: a neighbor's sentinel file survives cleanup of an unrelated scratch dir. Wired into `sv0 test-guards`. Verified load-bearing: replacing exact-path cleanup with a prefix-glob (the SEC-005 bug class this exists to prevent) fails exactly the neighbor-survival case. |
| NEX-009 | Implement a direct argv subprocess abstraction (no shell) | TOOL-003, SEC-001 | **done** | `scripts/native_exe_subprocess.py` — `run_argv` (`shell` is not a parameter; always `shell=False`; a bare string argument is a `TypeError`, not a silent shell command). 6-case `--selftest` incl. the literal red test: a `; $(touch ...) \`touch ...\` \| touch ...` payload in one argv element is passed through literally, sentinel never created. Wired into `sv0 test-guards`. Verified load-bearing by mutation (joining argv + `shell=True` fails 4 of 6 cases). |
| NEX-010 | Reject VM/executable flag combinations and `.sv0b` input before any tool invocation | GOV-002…003 | **done** | Extended `scripts/native_exe_cli.py` (`parse_args`): `--target=vm` (any position) and a `.sv0b`-suffixed input both raise `UsageError` with a distinct "bytecode isolation" message, exit 2, before `ParsedArgs` is ever constructed — 3 new cases (18 total). Wired into `sv0 test-guards` via NEX-002's existing entry. GOV-003 (`sv0vm` never invoked) holds by construction: no subprocess-invoking code exists anywhere in `native_exe_cli.py`; a real call-log assertion becomes possible once driver orchestration exists (post-F0). Verified load-bearing by mutation: first pass caught a real test gap (the generic "unknown option" fallback already produced *a* `UsageError`, silently passing a message-blind test) — strengthened the corpus to assert on message content, then confirmed removing either guard fails its case. |

### F0/R0 — core compiler and entry ABI (spec §31.2)

| ID | slice | requirement(s) | status | red test / acceptance |
|----|-------|-----------------|--------|-----------|
| NEX-011 | Add a reentrant core-compiler request interface; retire the global control file from the stable path | REL-004 | **done (migration shim — see note)** | `scripts/native_exe_core_compiler.py` — `CoreCompilerClient`/`CoreCompilerRequest`, a POSIX-`flock`-serialized wrapper around the legacy `/tmp/.sv0_drv_path` binary (spec §22.2's explicitly sanctioned migration mechanism, **not** the stable reentrant ABI AE-008 ultimately requires). Investigated the real fix first: sv0 has no argv/CLI-args host function at all — the native compiler's CLI entry is synthesized at build time by `scripts/build-sv0-megatu-native.sh` string-injecting a `/tmp/.sv0_drv_path` read into generated sv0 source, because `read_file(path)` is the only external-input channel the language has. A real fix needs a new host function across resolver/checker/C-lowering/runtime (+ `sv0doc`, which owns language capabilities) — scoped out as its own future slice, not done here. 4-case `--selftest`: (1) demonstrates real cross-contamination *without* the lock via a fake compiler that reads-then-sleeps-then-echoes the control file (widens the race window deterministically; stable across 15 repeated runs); (2) 6 concurrent requests through the real `CoreCompilerClient.invoke()` are always correctly isolated; (3) control-file format builders match `scripts/sv0`'s own `run_compile`/`run_emit_verified` exactly; (4) control file is always reset to empty after `invoke()`. Wired into `sv0 test-guards`. Verified load-bearing: removing the `flock` call reproduces cross-contamination in 10/10 runs. **Known limitation, documented in the module docstring**: this lock only protects concurrent calls made *through this module* — an unrelated `scripts/sv0` subcommand or the self-host loop touching the control file outside this protocol can still race against it. |
| NEX-012 | Normalize core-compiler stdout/stderr/exit protocol | PIPE-003…005, ERR-002 | **done** | `scripts/native_exe_emit.py` — `classify_emission` (nonzero exit → `FRONTEND` regardless of stdout content, priority-ordered before stdout inspection; zero exit + empty stdout → `EMIT_C`; stderr preserved verbatim and never merged into stdout even on success). 5-case `--selftest` driven through `native_exe_fake_emitter.py`'s real §26.2 modes. Wired into `sv0 test-guards`. Verified load-bearing by mutation: checking stdout emptiness before exit code (the exact ordering bug PIPE-003 exists to prevent) fails the `partial` case. |
| NEX-013 | Reject source/project with no `main` | ENTRY-001, ENTRY-004, PIPE-002 | **done** | `scripts/native_exe_entry_scan.py` — `validate_entry_exists`, a comment/string-aware source scan (adapted from `check_sv0_block_comment_nesting.py`'s tokenizer) for a top-level `fn main` declaration, file or project mode. Scoped as a **conservative pre-filter**, not project composition or full semantic entry validation (that stays compiler-owned per §13.3; investigated first — the C backend currently *fabricates* `int main(void) { return 0; }` for a missing `main`, byte-identical to a real trivial one in the emitted C, which is exactly why detection has to happen on source, not emitted output). 8-case `--selftest` incl. main-only-in-a-comment/string-literal/hidden-dir all correctly not counting. Wired into `sv0 test-guards`. Verified load-bearing by mutation (dropping comment-masking and inverting the file-mode check both fail multiple cases each). |
| NEX-014 | Reject duplicate project entries named `main` | ENTRY-001 | **done** | Extended `scripts/native_exe_entry_scan.py`: `count_main_declarations` + `validate_entry_exists` now reject >1 occurrence, whether within one file or spread across separate project files. 2 new cases (10 total). Wired into `sv0 test-guards` via NEX-013's existing entry. Verified load-bearing by mutation: relaxing either the file-mode or project-mode uniqueness check (`>= 1` instead of `== 1`) fails exactly its corresponding duplicate-detection case. |
| NEX-015 | Validate entry parameter and return types (only zero-param, `i32`/unit) | ENTRY-002…003 | **done** | `scripts/native_exe_entry_signature.py` — `extract_main_signatures` (balanced-paren param extraction + return-type text up to `{`, reusing NEX-013's comment-masking) + `validate_entry_signature`. 11-case `--selftest`: 3 accepted shapes (`i32`, explicit `()`, implicit) + 7 rejected shapes (params, bool, wrong int width, struct, enum-shaped, reference, string) + 1 project-mode case. Wired into `sv0 test-guards`. Verified load-bearing by mutation: disabling either the params check or the return-type check fails exactly its corresponding cases (1 and 7 respectively). |
| NEX-016 | Emit `sv0_user_main` plus the hosted C `int main(argc, argv)` adapter and `sv0_runtime_init` call | ENTRY-005…007 | **done** | First real compiler change (not Python scaffolding). Investigated first: 3 different C-backend `main`-emission sites exist (`codegen.sv0` — dead for output, `megaTU-main.sv0` — the real target, actually run by `build/sv0-megatu-native`/`sv0 compile`, `driver.sv0` — native-parity testing only). Modified `sv0c/lib/megaTU-main.sv0`'s `megatu_emit_program`: a recognized zero-param, `i32`/unit-returning `main` now emits `static <ret> sv0_user_main(void)` + a real hosted `int main(int argc, char **argv)` calling the new `sv0_runtime_init(argc, argv)` (added to `sv0c/runtime/sv0_runtime.h`, no-op, ABI slot reserved per §15.5) before invoking it; any other signature shape falls back to the old plain-`main` emission unchanged (defense in depth — NEX-015 already gates this in the Python driver). The "no main" fabrication fallback is untouched. Verified: 3 manual smoke fixtures (`i32` return 42 + println, explicit-unit, implicit-unit) all compile+link+run correctly (AC-001/002/003); a no-main fixture still gets the old fabricated no-op; a with-params fixture still gets the old unwrapped shape. Full `./scripts/sv0 test` run: the existing 112-program native behavior corpus (`verify_behavior_corpus_native.py`) passed unmodified through the new adapter shape — stronger evidence than adding synthetic fixtures would be, so none were added. Two pre-existing, unrelated failures observed (`checker rejects int/bool binop` — SML-side self-test, confirmed via `make -C sv0c test` in isolation; `self-host-sv0-loop: native run exit 232 for lib/checker.sv0` — confirmed by reproducing byte-for-byte on a `git stash`'d clean tree before restoring); neither touches `megaTU-main.sv0`/`sv0_runtime.h`. `codegen.sv0` and `driver.sv0` deliberately untouched (plan's documented scope). |
| NEX-017 | Reserve and mangle internal entry-adapter symbols | ENTRY-008 | **done** | `scripts/native_exe_entry_reserved.py` — `validate_no_reserved_collisions`, a comment/string-aware scan (reusing `native_exe_entry_scan`'s masking + file discovery) rejecting a top-level `fn sv0_user_main`/`fn sv0_runtime_init` in user source, file or project mode. Rejected at the Python driver layer (source diagnostic, exit 4) rather than compiler-side mangling — verified first that a colliding fixture today produces a raw `cc` "redefinition"/"conflicting types" error (host-compile, exit 6) instead of a clean early one; that host-compiler behavior remains as a defense-in-depth backstop for direct/unvalidated invocation. Deliberately scoped to exactly the 2 symbols NEX-016 introduced, not the ~20 other `sv0_`-prefixed runtime symbols (pre-existing, differently-scoped risk). 5-case `--selftest` incl. both reserved names, project mode, and comment/string-only mentions not counting. Wired into `sv0 test-guards`. Verified load-bearing by mutation: disabling the collision check fails all 3 collision cases. |
| NEX-018 | Validate and hash the exact staging C before host compile | PIPE-006, PIPE-011 | **done** | `scripts/native_exe_staging.py` — `validate_staging_c` (rejects missing runtime ABI include marker, `BuildError(EMIT_C)`/exit 4) + `hash_staging_c` (SHA-256, OD-008; reused later by `--keep-c`, NEX-040). PIPE-006 byte-identity holds by construction (one `classify_emission` call site for both modes, no per-mode divergence) — proved with two independent fake-emitter invocations compared byte-for-byte and by hash. 4-case `--selftest`. Wired into `sv0 test-guards`. Verified load-bearing by mutation: disabling the marker check fails the missing-marker case. **F0/R0 core-compiler + entry-ABI band (NEX-011…018) is now fully closed, 8/8.** |

### R0 — runtime and host toolchain (spec §31.3)

| ID | slice | requirement(s) | status | red test / acceptance |
|----|-------|-----------------|--------|-----------|
| NEX-019 | Build the compiler-relative runtime resolver (never cwd/`CPATH`/project-relative) | RT-001…003, SEC-002 | **done** | `scripts/native_exe_runtime.py` — `resolve_runtime_dir`/`RuntimeLocation`, derived from `__file__`'s own toolchain root (matching every `verify_*.py`'s `--root` default), never cwd/env/project-relative. 3-case `--selftest`: resolves correctly from an unrelated cwd; a fake toolchain root with no `sv0c/runtime` fails closed rather than wandering to a project-local shadow file; the real root resolves to the real files. Wired into `sv0 test-guards`. Verified load-bearing by mutation (disabling the missing-file check fails the fail-closed case). |
| NEX-020 | Runtime ABI manifest + content-hash verification | RT-004…005 | **done** | `scripts/native_exe_runtime_manifest.py` — `load_manifest`/`verify_manifest` (`BuildError(RUNTIME)`/exit 7 on missing manifest, schema/ABI mismatch, or hash mismatch) + `write_manifest` (`--write` regeneration mode). Added `sv0c/runtime/runtime-manifest.json` (schema/ABI version 1, SHA-256 of `sv0_runtime.h`/`.c`, OD-004). 4-case `--selftest`. Wired into `sv0 test-guards`. Verified load-bearing by mutation (disabling the header-hash comparison fails the corrupted-header case). |
| NEX-021 | Implement C-compiler selection precedence (`--cc` → `sv0.toml` → `SV0_CC` → `CC` → sanitized `PATH` `cc`) | TOOL-001…002 | **done** | `scripts/native_exe_cc_select.py` — `select_cc`/`CcSelection`, the R0-relevant tiers (`sv0.toml`/`SV0_CC` are R0.1, not implemented yet): explicit `--cc` → `CC` env → `PATH` `cc` (via `shutil.which`). 6-case `--selftest`. Wired into `sv0 test-guards`. Verified load-bearing by mutation: letting an invalid explicit `--cc` silently fall through to `CC`/`PATH` (the exact bug TOOL-002 exists to prevent) fails both explicit-rejection cases. |
| NEX-022 | Family/version/minimal-hosted-program capability probe | TOOL-005 | **done** | `scripts/native_exe_cc_probe.py` — `probe_compiler`/`CompilerInfo`: `--version` capture + family classification, then an actual compile+link+run of a minimal hosted C program (catches what a bare `--version` check would miss — a compiler that answers but can't produce a working binary). 4-case `--selftest`: real `cc` end-to-end; a tool that fails `--version`; `native_exe_fake_cc.py`'s `fail` mode caught at the compile stage; a missing tool. Wired into `sv0 test-guards`. Verified load-bearing by mutation (disabling the compile-failure check still fails, one stage later, at the run step — confirming the probe never silently accepts a broken compiler either way). |
| NEX-023 | Central dev-profile argv builder (Appendix B canonical argv: `-std=gnu99 -O0 -g -I<runtime> program.c sv0_runtime.c -o <tmp>`) | TOOL-004, RT-006 | **done** | `scripts/native_exe_argv_builder.py` — `build_dev_profile_argv`, the one canonical builder (GOV-008: no duplicated recipes). OD-005 resolved: `-std=gnu99`, never `c99`. 3-case `--selftest`: exact argv snapshot, trusted `-I` precedes program C, `-std=c99` never appears. Wired into `sv0 test-guards`. Verified load-bearing by mutation (swapping to `-std=c99` fails both the snapshot and the dedicated dialect check). |
| NEX-024 | Sanitized child environment (§16.7 allowlist) + trusted include directory ordered before inherited/untrusted paths | SEC-006, TOOL-007…008 | **done** | `scripts/native_exe_env.py` — `sanitized_child_env`, an allowlist (not a denylist): `PATH`/`LANG`/`LC_ALL`/`TMPDIR`+`TEMP`+`TMP`/`SDKROOT`/`DEVELOPER_DIR` only. `CPATH`/`C_INCLUDE_PATH`/`OBJC_INCLUDE_PATH`/`LIBRARY_PATH`/`LD_LIBRARY_PATH`/`DYLD_LIBRARY_PATH` are stripped even when the parent sets them (OD-006 resolved). Trusted-`-I`-first ordering already proven in NEX-023's argv builder. 4-case `--selftest`. Wired into `sv0 test-guards`. Verified load-bearing by mutation: an allowlist reimplemented as a denylist (copy-then-strip) lets an unrelated var leak through, caught immediately. |
| NEX-025 | Host compile/link failure diagnostics and temp-output validation | TOOL-009…010, ERR-003…004 | **done** | `scripts/native_exe_host_compile.py` — `run_host_compile`, composing `native_exe_subprocess.run_argv` + `native_exe_publish.validate_temp_output` (reused, not reimplemented): nonzero exit → `HOST_COMPILE` with stderr verbatim; zero exit still routes through the existing `HOST_LINK`-classified output validation (TOOL-010) — both map to exit 6, so this is a labeling distinction, not behavioral. 4-case `--selftest` driven through `native_exe_fake_cc.py`'s real modes. Wired into `sv0 test-guards`. Verified load-bearing by mutation (disabling the nonzero-exit check misclassifies the phase and loses the original stderr — caught immediately). |
| NEX-026 | Default/explicit output paths, directory-creation rules, permission and symlink handling | ART-001, ART-005…011 | **done** | `scripts/native_exe_output_path.py` — `default_output_path` (§12.1: `<cwd>/build/native/<stem>`, trailing separators stripped for project mode) + `validate_output_path` (rejects empty/`-`/existing-directory/existing-symlink leaf, reusing `native_exe_publish`'s lexists/islink pattern applied pre-flight instead of post-flight) + `ensure_output_parent_dir` (creates the default `build/native` dir; an explicit `-o`'s missing parent is a hard error, never silently created, ART-007). 6-case-group `--selftest`. Wired into `sv0 test-guards`. Verified load-bearing by mutation (removing the symlink-leaf check fails that case). Relative `-o`-vs-cwd resolution and CLI-level empty/`-` rejection were already covered by NEX-002/NEX-003; umask/executable-bit checks are covered where the temp output is actually created (`native_exe_publish`, `native_exe_fake_cc`). |
| NEX-027 | Human quiet/verbose/success/failure output (stdout empty in exe mode, success line to stderr, `--quiet` never suppresses errors) | ERR-005…008 | **done** | `scripts/native_exe_human_output.py` — `format_success_message` (matches the spec's own §18.3 example verbatim), `maybe_print_success` (quiet-aware, structurally has no error path to suppress — errors stay `native_exe_errors.BuildError`'s job), `render_argv_for_display` (`shlex.quote`-safe, diagnostic-only, never re-parsed). 6-case `--selftest` incl. a hostile-argv round-trip check. Wired into `sv0 test-guards`. Verified load-bearing by mutation (dropping `shlex.quote` fails the round-trip case). **R0 runtime + host toolchain band (NEX-019…027) is now fully closed, 9/9.** |

### R0 — corpus, project, and reliability (spec §31.4)

| ID | slice | requirement(s) | status | red test / acceptance |
|----|-------|-----------------|--------|-----------|
| NEX-028 | Derive a native-executable manifest from `sv0c/test/behavior/manifest.txt` | TEST-004 | **done** | **Re-scoped (see hub refinement log): absorbed the first real assembly of the whole driver.** `scripts/native_exe_build.py` — `build_native_executable`/`BuildResult`, composing every NEX-007…027 module in the §13.1 phase order, zero new logic beyond sequencing. 5-case `--selftest`: real `i32`/unit builds run+verified, default output path, `--quiet`, and a no-main fixture rejected *before* the compiler ever runs (confirmed by checking no output exists). `scripts/native_exe_behavior_corpus.py` runs `build_native_executable` against all 112 rows of `sv0c/test/behavior/manifest.txt` — real, full sweep, not a subset; wired into `sv0 test` alongside the existing ad hoc `verify_behavior_corpus_native.py`. Wired into `sv0 test-guards` via a fast 3-row `--selftest` subset. Verified load-bearing by mutation: (1) removing the entry-validation calls in `build_native_executable` was initially NOT caught — all selftest fixtures were positive-only — fixed by adding the no-main negative case, which then failed exactly as expected; (2) skipping `run_host_compile` fails downstream at publish; (3) removing the exit-code comparison in the corpus runner lets a deliberately wrong expected exit pass silently, confirmed both broken and real behavior directly. |
| NEX-029 | Runtime-feature native suite (println, strings, Vec, Box, filesystem I/O where supported) | RT-007…008 | **done** | println/string/Vec already proven by NEX-028's full 112-row corpus sweep. `scripts/native_exe_runtime_feature_smoke.py` adds: **Box** (`sv0c/test/integration/box_expr/box_expr.sv0`, a real existing fixture, built via `build_native_executable`, links + exits 0); **stdout/stderr channel separation on a real compiled binary** (a `requires`-violation fixture: `println` on stdout, `sv0 contract violation: ...` on stderr, exit 1 — this is where I discovered a stale doc comment in `test/integration/contract_violation/README.md` claiming native contract checks are dropped; verified directly against the real compiler that they are NOT). **Filesystem I/O**: no fixture exists anywhere in the repo; documented as deliberately not invented (RT-007 applies "where supported"). 2-case-group `--selftest`. Wired into `sv0 test-guards`. Verified genuinely discriminating by swapping the stdout/stderr assertions (fails against the real binary, confirming the check isn't vacuous) and by asserting a deliberately wrong Box exit code (fails as expected). |
| NEX-030 | Project-mode executable build via compiler-owned composition (module/`use` semantics, deterministic ordering, one required `main`) | CLI-009, PIPE-007 | **done** | `scripts/native_exe_project_mode.py` — reuses `build_native_executable("project", ...)` unchanged against the real, already-existing `sv0c/test/integration/modules/` fixture (`main.sv0` importing `lib::bump` from `lib/lib.sv0`); no new fixture needed. 1-case `--selftest` (AC-006: exit 42 = `bump(41)`). Wired into `sv0 test-guards`. Verified genuinely discriminating (asserting a deliberately wrong expected exit fails against the real build). |
| NEX-031 | Runtime/verified/disabled contract-mode executable matrix, sound Z3-absent degradation | PIPE-008…009 | **done** | `scripts/native_exe_contract_mode.py` — `compute_proof_lines`, mirroring `scripts/sv0`'s own `run_emit_verified` pass 1 (same compiled `build/sv0-megatu-verify` + `scripts/sv0-z3.sh`, not reimplemented) rather than `build_native_executable` doing Z3 orchestration itself. 4-case-group `--selftest` against real fixtures: `runtime` (requires satisfied, exit 6), `disabled` (a requires-violating input that would abort in `runtime` mode instead runs to completion, exit 8), `verified` with the real installed Z3 4.16.0 (an `ensures` actually proven and stripped, exit 8), and Z3-absent degradation (PATH-hiding override; proof file empty, build still succeeds, no false "verified" claim). Wired into `sv0 test-guards`. Verified load-bearing by mutation: making the z3-absent path falsely claim a proof is caught immediately; asserting the wrong disabled-mode exit code fails, confirming the check strip is real. |
| NEX-032 | Prior-output preservation tests for every failure phase | ART-004, REL-002 | **done** | `scripts/native_exe_preservation.py` — drives the real `build_native_executable` through all 6 directly-injectable failure phases (ENTRY, FRONTEND, EMIT_C, HOST_COMPILE, HOST_LINK, RUNTIME — added a `runtime_override` seam to `build_native_executable` to make RUNTIME injectable), seeding a known-good output first and confirming its bytes+mode are untouched after each. PUBLISH-phase preservation stays covered by `native_exe_publish.py`'s own corpus, not re-derived. Wired into `sv0 test-guards`. Verified load-bearing: (1) discovered mid-implementation that `sanitized_child_env` (correctly, by design) strips the env var used to steer the fake host compiler — fixed by baking the mode into a tiny wrapper script instead of relying on env passthrough; (2) mutating `build_native_executable` to skip entry validation is caught end-to-end by this test too (not just NEX-028's own selftest); (3) a direct self-check proves the byte/mode comparison itself discriminates a genuine corruption, since every other injected failure is already independently caught by lower-level mutation-tested modules (a property of defense in depth, not a test weakness). |
| NEX-033 | Symlink rejection, scratch ownership, cleanup scoping, and race hardening | SEC-003…005 | **done** | `scripts/native_exe_hardening_smoke.py` — consolidation, no new logic: drives the real `build_native_executable` with (1) a final output symlinked to a protected sentinel (AC-018: rejected, target untouched, symlink itself untouched), and (2) a neighbor scratch directory sharing the same parent as the real build's own scratch dir (via a new `scratch_base_dir` test seam on `build_native_executable`) — the neighbor's sentinel survives the real build's cleanup. Wired into `sv0 test-guards`. Verified genuinely load-bearing: mutating the real symlink-rejection check in `native_exe_output_path.py` is caught end-to-end here; mutating `native_exe_scratch.py`'s cleanup to a prefix-glob (the exact SEC-005 bug class) is caught here too, not just by NEX-008's own isolated test. |
| NEX-034 | Child process-group cancellation (SIGINT/SIGTERM/API cancel) | SEC-009, REL-003 | **done** | Extended `scripts/native_exe_subprocess.py`: `run_argv_cancellable`/`Cancelled`/`_terminate_process_group` — launches via `start_new_session=True` (process-group leader, portable macOS/Linux), terminates the *whole group* on a `threading.Event` or `KeyboardInterrupt` (SIGTERM, escalating to SIGKILL after a grace period), and `Cancelled` is a distinct exception type so a cancelled run can never be mistaken for a `CommandResult`. 3 new cases (9 total) driven against `native_exe_fake_cc.py`'s real `hang` mode: prompt cancellation, whole-process-group termination (a wrapper spawning a background grandchild — both die), and the uncancelled happy path still works. Wired into `sv0 test-guards`. Verified load-bearing by mutation (targeting only the leader PID instead of the group) — **the first attempt at this mutation check silently passed**: the test's own timing let the check get vacuously skipped (the background marker file was never created before cancellation fired). Fixed the test (touch immediately before looping; longer cancel delay; hard-fail if the marker was never created) before re-confirming the mutation is genuinely caught. |
| NEX-035 | Parallel distinct-output builds share no mutable state | REL-001 | **done** | `scripts/native_exe_parallel_builds.py` — 6 real concurrent `build_native_executable` calls under `threading` (AC-020), each with its own program whose only content is a distinguishing exit code (its "fingerprint"); confirms every build produces its own correct binary with zero cross-contamination. Stable across 5 repeated runs. Wired into `sv0 test-guards`. Verified dramatically load-bearing by mutation: removing `native_exe_core_compiler.py`'s `flock` call (the exact bug REL-001/NEX-011 exist to prevent) reliably reproduces real cross-contamination and `read_file` panics across all 6 concurrent builds, every time, across 3 separate runs. |
| NEX-036 | Cross-platform hostile-path corpus (spaces, Unicode, quotes, semicolons, `$()`, backticks, parens, wildcards, leading hyphen) | PORT-004, SEC-001 | **done** | `scripts/native_exe_hostile_paths.py` — 6 hostile filename shapes (spaces, Unicode, leading hyphen, `;`, `$()`, backtick) as both source and output paths, built end to end via `build_native_executable`, run, exit code checked. **A real bug in the test itself, found and fixed before landing**: the first version embedded a full sentinel *path* (with slashes) inside the hostile output filename, which made `ensure_output_parent_dir` correctly reject it as a malformed path before ever reaching any code the test meant to exercise — every build was silently "passing" via a clean, unrelated `BuildError`, and exit-code mutations had zero effect because that code path never ran. Fixed by using a bare sentinel *name* (no embedded separators) as one real filename component; confirmed all 6 shapes then build+run+exit correctly (debug-traced to prove real success, not a caught error). Wired into `sv0 test-guards`. Verified genuinely load-bearing this time: a wrong expected-exit-code mutation now fails all 6 cases. The "no shell injection" sentinel check itself currently holds by construction in this architecture (a hostile user path never becomes a subprocess argv token — it's always laundered through fixed scratch-internal names first) rather than by an active mutation-proof; documented honestly as a defensive guard against a future regression, not overclaimed. |
| NEX-037 | R0 host-matrix jobs: Linux default `cc`, Linux Clang, macOS Apple Clang arm64, all dev profile | PORT-001 | **done** | Documented (not applied) in `sv0c/doc/native-executable.md`'s "R0 host matrix and required CI jobs" section — the macOS arm64/Apple Clang leg has been continuously proven on this dev machine throughout NEX-001…036; the Linux legs aren't locally reproducible here. Current CI reality recorded honestly (`.github/workflows/ci.yml`: one Ubuntu job, no Clang leg, no macOS job) alongside the 3 required job snippets. Adding the jobs is a separate CI-config change with its own review (billed minutes, supported-platform claim) — deliberately not applied in this doc-only slice. |
| NEX-038 | User docs: single-file/project/explicit-output/contracts/manual-run examples, `.sv0b`-not-converted statement, supported hosts/compilers/limitations | DOC-001…003 | **done** | `sv0c/doc/native-executable.md` — single-file/project/explicit-output/contract-mode/manual-run examples (DOC-001), an explicit `.sv0b`-never-converted statement (DOC-002), supported-hosts/current-limitations text (DOC-003). States current status honestly: the engine (NEX-001…036) is real and tested end to end, but the public `sv0c --emit=exe` CLI isn't wired up yet. Every example was verified against the real engine, not left as unverified prose — the first draft of the direct-Python-call example was wrong twice (missing `PYTHONPATH`, then an explicit output path that doesn't auto-create its parent per ART-007) and was corrected against actual runs. **R0 corpus/project/reliability band (NEX-028…038) is now fully closed, 11/11 — the entire R0 release is closed, 20/20 slices (NEX-019…038), on top of the already-closed F0 and F0/R0 bands (NEX-001…018).** |

### R0.1 — inspection, configuration, and provenance (spec §31.5)

| ID | slice | requirement(s) | status | red test / acceptance |
|----|-------|-----------------|--------|-----------|
| NEX-039 | `--emit=c -o` writes C atomically and never invokes the host compiler | CLI-014 | **done** | `scripts/native_exe_emit_c.py` — `emit_c_only`, running exactly `build_native_executable`'s "front half" (entry validation → output-path validation → core-compiler invoke → emission classify/staging validate) via the same already-tested functions, then `native_exe_staging.write_text_atomically` (new: temp-write-then-`os.replace`, mirroring `native_exe_publish`'s pattern for a text artifact). Never imports `native_exe_cc_select`/`cc_probe`/`host_compile` at all. 3-case `--selftest`. Wired into `sv0 test-guards`. Verified load-bearing by mutation: injecting a real (recorded) fake-cc invocation after the write is caught immediately — a first attempt at this mutation using `--version` silently passed because that mode never reaches the fake tool's own recording logic, caught and fixed before landing. |
| NEX-040 | `--keep-c` retains exact emitted C on both link success and link failure | CLI-015, ART-012 | **done** | `scripts/native_exe_keep_c.py` — new `keep_c_path` parameter on `build_native_executable` (NEX-028) writes the staging C via `write_text_atomically` (NEX-039) unconditionally, right after `validate_staging_c` succeeds and before host compile ever runs — so it's retained whether the *later* host-compile/link step succeeds or fails. 3-case `--selftest`: success-case retention, forced-failure-case retention (`explicit_cc` pointed at a wrapper baking `SV0_FAKE_CC_MODE=fail` into the executable itself, since `sanitized_child_env` correctly strips that var from a plain env-var injection — same pattern as `native_exe_preservation.py`'s `_make_cc_wrapper`), and a hash-equality check (`hash_staging_c`, NEX-018) between both retained copies. Wired into `sv0 test-guards`. Verified load-bearing by mutation: moving the retention write to after `run_host_compile` succeeds correctly fails the forced-failure case ("retained C was NOT written despite the later host-compile failure"). |
| NEX-041 | `--message-format=json` newline-delimited event encoder, schema, and phase timing | CLI-016, ERR-009…011 | **done** | `scripts/native_exe_json_output.py` — `PhaseTimer` (named-phase durations via `time.monotonic()`, clamped nonnegative, `total` computed as the sum of the already-rounded per-phase values so it's exactly consistent with its parts) + `build_event`/`encode_event` matching §18.6's exact schema (schema_version, event, success, phase, input, output, backend, profile, contract_mode_requested/effective, compiler sub-object, timings_ms). 4-case `--selftest`: real-timer nonnegative + total-consistency check, a zero-duration phase, a successful-build event round-tripped through `json.loads` against the required-key/type schema, and a failed-build event (`success: false`, `output: null`). Wired into `sv0 test-guards`. Verified load-bearing by mutation: removing the nonnegative clamp (and offsetting durations by -1000ms) is caught immediately by both the nonnegative and zero-duration checks. |
| NEX-042 | Atomic build-record JSON with content/output hashes (Appendix C shape) | REPRO-002…003 | **done** | `scripts/native_exe_build_record.py` — `build_record` matches Appendix C's exact shape (artifact kind/path/sha256/size computed via `hashlib.sha256` over the real published bytes; input source hashes; `sv0c`/runtime/host/`c_compiler`/profile/contract-mode/`hermetic` fields as caller-supplied identity data this module has no way to derive itself) + `write_build_record_atomically` (reuses `write_text_atomically`, NEX-039, with `json.dumps`). 4-case `--selftest`: a real `build_native_executable` run, the record's `artifact.sha256` checked against a direct hash of the published file, the source hash checked the same way, an atomic-write round-trip through `json.loads`, and a post-publish corruption check proving the digest is a real content hash (not a placeholder that always agrees with itself). Wired into `sv0 test-guards`. Verified load-bearing by mutation: replacing the artifact hash with a hardcoded constant is caught immediately by the digest-mismatch check. |
| NEX-043 | `sv0.toml` `[build]` schema, discovery precedence, and relative-path resolution rules (§17) | Section 17 | **done** | `scripts/native_exe_config.py` — `discover_config` (R0.1's simple beside-the-file/project rule, §17.3, no parent-workspace walk); `load_config` via `tomllib`, validating exactly the 6 documented `[build]` keys and their types, **rejecting** (not silently ignoring, per §17.2) an unrecognized key, a wrong-typed value, or a missing `[build]` table; `resolve_precedence(cli, config, env, default)` encoding §11.4's CLI > config > env > default rule via an `_UNSET` sentinel so an explicit falsy value at a higher tier still wins. 6-case `--selftest`: well-formed parse, empty-dir discovery miss, unrecognized-key rejection, wrong-type rejection, missing-`[build]`-table rejection, and the full 4-tier precedence matrix including the falsy-value-wins case. Wired into `sv0 test-guards`. Verified load-bearing by two independent mutations: removing the key/type validation is caught by both rejection cases; reordering the precedence tiers is caught by all three precedence-matrix cases. |
| NEX-044 | Record compiler identity/probe results (path, family, version) with bounded re-probing | TOOL-006 | **done** | `scripts/native_exe_cc_probe.py` — new `probe_compiler_cached(cc_path, cache=None)`, keyed by `os.path.realpath(cc_path)` (so two argv spellings of the same tool share one cached probe), reusing the first `probe_compiler` result rather than re-invoking `--version`/the compile-probe on every call within one process (in-process only; a persistent cross-invocation disk cache is R1/PERF-007's fuller form). New 5th `--selftest` case: a wrapper around the *real* host `cc` (not the fake tool, since `probe_compiler` actually runs the produced binary and a fake placeholder file isn't a real executable) counts its own underlying invocations via a counter file; two cached calls against the same identity show only one probe's worth of invocations, and a fresh-cache dict correctly forces a re-probe. Wired into `sv0 test-guards`. Verified load-bearing by mutation: bypassing the cache lookup is caught immediately by the invocation-count checks. |
| NEX-045 | Installed runtime bundle manifest + licenses (`runtime/{sv0_runtime.h,.c,abi-version.txt,runtime-manifest.json,licenses/}`) | RT-009 | **done** | `sv0c` (data only): new `runtime/abi-version.txt` (`"1\n"`, matching `native_exe_runtime_manifest.ABI_VERSION`) and `runtime/licenses/{LICENSE-APACHE,LICENSE-MIT}` (copies of the already-existing top-level license files, not a new licensing decision). `scripts/native_exe_runtime_bundle.py` — `verify_bundle_contents(runtime_dir)`, confirming the full §15.1 shape plus that `abi-version.txt`'s content actually matches the expected ABI version. 8-case `--selftest`: the real in-repo bundle verifies clean; a temp copy missing each of the 6 required members individually fails closed, one at a time (proving every member is load-bearing, not just the first checked); a wrong ABI-version value is rejected even with every file present. Wired into `sv0 test-guards`. Verified load-bearing by mutation: checking only the first required member (ignoring the rest) is caught immediately by the missing-member loop. |
| NEX-046 | Installed-layout E2E outside a source checkout (path containing spaces, no checkout present) | AC-023 | **done** | `scripts/native_exe_installed_layout.py` — copies exactly the installed artifacts (the full `sv0c/runtime/*` bundle post-NEX-045, and `build/sv0-megatu-native`) into a fresh temp directory whose path contains a space, with no `sv0c` source tree alongside it at all, then runs a real single-file build from there via `build_native_executable`'s existing `runtime_override`/`compiler_path` seams (NEX-032/033, no new seams needed) pointed at the copy. 4-case `--selftest`: the copied bundle verifies (NEX-045), the copied runtime manifest verifies (NEX-020), a real build+run+exit-code check succeeds entirely off the copy, and a sanity check confirms the test setup itself has no `sv0c` tree beside it. Wired into `sv0 test-guards`. Verified load-bearing by mutation: corrupting the copied header (proving the pipeline genuinely reads from the copy, not a cached/real location) is caught immediately by the manifest-verification and build failures. |
| NEX-047 | Phase-timing + reference benchmark harness (frontend/emission, host compile/link, publication, total) | PERF-001…004 | **done** | `scripts/native_exe_benchmark.py` — `run_benchmark` wraps `build_native_executable` with `PhaseTimer` (NEX-041) across 4 representative fixtures: a minimal program, the median-size and largest files in `sv0c/test/behavior/manifest.txt` (by byte size), and the `sv0c/test/integration/modules/` project fixture (NEX-030's own). Reports `{fixture: timings_ms}`; a broken fixture records `{"error": ...}` rather than aborting the whole report. Confirmed against a real report (`--run`): all 4 fixtures present, timings 60–120ms range. `--selftest`: every fixture's timings have the expected schema keys and are nonnegative. This is the harness existing and running (its own schema/fixture-selection proof), not a performance regression gate -- that comparison is R1/PERF-005's job once a baseline exists. Wired into `sv0 test-guards`. Verified load-bearing by mutation: dropping the `total` key from a fixture's timings is caught immediately by the schema check. **R0.1 — inspection, configuration, and provenance band (NEX-039…047) is now fully closed, 9/9.** |

### R1 — optimization, reproducibility, and stability (spec §31.6)

Re-sliced into 34 bite-sized sub-slices (planned via
`/Users/sasank/.claude/plans/starry-discovering-quokka.md`): each spec-level
`NEX-04x` row is too large to land as one TDD slice (sanitizer
certification, release-profile enablement, ABI versioning, reproducibility
evidence, a perf regression harness, a signed-release workflow, and a
legacy-recipe dedup sweep are each multi-part work), so it becomes a
letter-suffixed group (`NEX-048a`, `NEX-048b`, …) — same convention as the
`PC-2b/2c/2d` M3 Phase C epic slicing. The spec's own `NEX-04x` number stays
the requirement-traceability anchor; the letter is the actual unit of work.
Investigation already done while scoping (not assumptions): `REL-004`
(retiring `/tmp/.sv0_drv_path`) needs a genuinely new sv0c-side reentrant
core-compiler entry point that doesn't exist yet, flagged below as NEX-055c
with its own dedicated future planning pass; `GOV-008` is already R0-closed
for the Python driver itself, but three legacy shell scripts
(`scripts/pc3b6-native-project-acceptance.sh`,
`scripts/sv0-megatu-native-parity.sh`,
`scripts/sv0-megatu-corpus-parity.sh`) still hand-roll `cc -std=c99 -O0 -w`
recipes with blanket `-w`, which is what NEX-058/049c actually migrate;
`ENTRY-010` has no code today (only the runtime *bundle* is ABI-versioned,
not the entry contract itself); CI today is a single Ubuntu job with no
Clang/macOS/sanitizer/release legs, so every CI-job sub-slice below is
genuinely new, not a doc-only restatement.

#### NEX-048 — TOOL-012: C/runtime UB and strict-aliasing audit

| ID | slice | status | red test / acceptance |
|----|-------|--------|-----------|
| NEX-048a | Write the audit doc (`sv0c/doc/native-executable-ub-audit.md`): enumerate every raw pointer punning / union-based box storage / signed-overflow-reliant site in `sv0_runtime.h`/`.c` and the megaTU C emitter; classify each "safe under `-O2`" / "needs `-fno-strict-aliasing`" / "needs a representation change". Investigation + decision record; gates NEX-048b and all of NEX-051. | **done** | `sv0c/doc/native-executable-ub-audit.md` — 7 sites enumerated from reading the actual source (not from memory): `sv0__box_deref_raw`'s pointer-cast-and-deref through the `intptr_t`-typed box pool (Site 1, needs `-fno-strict-aliasing` — write side uses `memcpy`, read side casts-and-derefs); the deliberately-documented cross-reinterpretation between `lowering.sv0::Value` and `codegen.sv0::Value`'s layout-compatible-but-nominally-distinct C structs (Site 2, same root cause, same flag); the flat tagged-struct payload-slot-reuse pattern (Site 3, already mitigated by BUGS.md #8, no action); undocumented signed-overflow policy for negation/div/mod at the type-minimum value (Site 4, needs fixture coverage — NEX-048b's job, not a flag); unchecked box-pool indexing and the `Vec<T>` handle table (Sites 5/6, safe by design); string operations (Site 7, safe). Conclusion: `native_exe_argv_builder.build_release_profile_argv` (NEX-051a) SHALL include `-fno-strict-aliasing` alongside `-O2`. Landed in `sv0c` (commit `de3dad7`), pointer bumped + README pin updated in the same commit. |
| NEX-048b | Turn 048a's flagged sites into concrete UBSan/ASan fixture programs under `sv0c/test/behavior/` (one per flagged pattern: box punning, vector bounds, signed-overflow-adjacent arithmetic), with documented expected sanitizer verdicts. Fixtures are inert until NEX-050 wires a sanitizer job. | **done** | `sv0c/doc/native-executable-sanitizer-fixtures.md` registers 5 fixtures. Two safe stress fixtures landed in the ordinary behavior-corpus PASS manifest (`box_deref_chain_stress.sv0`, Site 1, exits 210; `vec_growth_stress.sv0`, Site 6, exits 188) — both verified against the real driver, exit codes observed not hand-computed; full 114-program corpus confirmed green (`native_exe_behavior_corpus.py`). Three UB-only fixtures for Site 4's signed-overflow edge cases (`ub_arith_neg_i32_min.sv0`, `ub_arith_div_i32_min_by_neg1.sv0`, `ub_arith_mod_i32_min_by_neg1.sv0`) deliberately kept OUT of the manifest since their outcome is platform-dependent by definition — observed directly on this dev machine (arm64/Apple Clang, `-O0`): none crash today (ARM's `SDIV`/negate don't trap the way x86's `IDIV` does), feeding directly into NEX-051d's host-matrix rationale. Site 2 gets no dedicated fixture (compiler-internal, not user-reachable; already exercised by the existing self-host suite). Landed in `sv0c` (commit `fafa7d4`), pointer bumped + README pin updated in the same commit. |

#### NEX-049 — TEST-008: warning policy, remove blanket suppression

| ID | slice | status | red test / acceptance |
|----|-------|--------|-----------|
| NEX-049a | `scripts/native_exe_warning_policy.py`: accepted GCC/Clang-common warning flags for the stable gate + a narrow suppression list, each suppression entry carrying a one-line rationale (argv-native data, not a shell flag string, per §16.6). | **done** | `ACCEPTED_WARNING_FLAGS = [-Wall, -Wextra]`; `SUPPRESSED_WARNINGS` (4 entries, all grounded in NEX-049b's real corpus run, not speculative: `-Wno-parentheses-equality`, `-Wno-unused-variable`, `-Wno-unused-but-set-variable`, `-Wno-integer-overflow`); a new `TRACKED_GAPS` list (distinct from suppression — a real, known correctness gap deliberately left *visible*, not hidden) holding `-Wsometimes-uninitialized`, discovered while grounding this policy: every enum `match` lowers to an if/else-if chain with an EMPTY final else branch, so the result temp is provably uninitialized on any tag Clang can't prove exhaustive — confirmed via generated-C inspection (`enum_match_payload.sv0` + 14 others), not assumed; the real fix (a defensive `sv0_panic` else-branch in match codegen) is filed as a follow-up task rather than rushed into this policy slice. `validate_policy` checks all three lists pairwise-disjoint (by bare warning name) + every suppression/tracked entry has a non-empty rationale. 7-case `--selftest`. Wired into `sv0 test-guards`. Verified load-bearing by mutation: skipping all overlap/rationale checks is caught by 5 of the 7 cases at once. |
| NEX-049b | Run the full behavior corpus through the accepted warning flags; report warnings actually emitted, classified fixed/suppressed/unclassified — an unclassified warning is a hard failure (§26.6's enforcement point). | **done** | `scripts/native_exe_warning_report.py` — compiles every one of the 114 behavior-corpus fixtures' emitted C under `-Wall -Wextra` (`emit_c_only`, NEX-039, feeding a real `cc -c` invocation), classifying every observed warning into suppressed/tracked/unclassified by its `[-W<name>]` tag. Real run: 56 suppressed, 16 tracked (all `-Wsometimes-uninitialized`), **0 unclassified** — the corpus is genuinely clean under the accepted flags today, not just "not blocked." Selftest additionally asserts both the suppressed and tracked buckets caught at least one real warning each (not dead classification branches). Wired into `sv0 test-guards`. Verified load-bearing by mutation: collapsing all three buckets into "suppressed" is caught by the tracked-bucket-empty check. |
| NEX-049c | Prove the behavior corpus still passes cleanly under 049a's flags (no blanket `-w`) — the acceptance check that unblocks NEX-058's actual script edits. | **done** | `scripts/native_exe_no_blanket_suppression.py` — the static half of the "no blanket suppression" claim: confirms `native_exe_argv_builder.py`'s own source contains no bare `-w` token (word-bounded regex, so it never false-positives on `-Wall`/`-Wextra`/`-Wno-*`), complementing NEX-049b's behavioral proof that the corpus is warning-clean. This is the evidence NEX-058 points back to rather than re-deriving. 4-case `--selftest`. Wired into `sv0 test-guards`. Verified load-bearing by mutation: a no-op check body is caught by both the bare-`-w`-detection cases. |

#### NEX-050 — TEST-007: ASan/UBSan matrix + intentional-leak classification

| ID | slice | status | red test / acceptance |
|----|-------|--------|-----------|
| NEX-050a | `scripts/native_exe_sanitizer_build.py`: wraps the real `build_native_executable` with `-fsanitize=address,undefined` via a new `extra_cc_args` seam on `build_dev_profile_argv` (one argv-native seam, no flag-string parsing). | **done** | New `extra_cc_args` param threaded through `build_dev_profile_argv` → `build_native_executable` (omitted ⇒ byte-identical argv to before). `native_exe_sanitizer_build.build_sanitized_executable` always adds `-fsanitize=address,undefined`. 2-case `--selftest`: `ub_arith_div_i32_min_by_neg1.sv0` (NEX-048b) triggers a real UBSan runtime-error diagnostic (confirmed by direct observation: `"runtime error: division of -2147483648 by -1 cannot be represented in type 'int'"` — UBSan's default mode prints-and-continues rather than aborting, so the check is on stderr content, not exit code); `box_deref_chain_stress.sv0` still builds/runs correctly (exit 210) under the same instrumentation. Wired into `sv0 test-guards`. Verified load-bearing by mutation: emptying `SANITIZE_FLAGS` is caught immediately. |
| NEX-050b | Run 048b's fixtures under 050a in both `-O2` and unoptimized builds; classify intentional process-lifetime allocations via a documented suppression file (e.g. `sv0c/runtime/lsan-suppressions.txt`), rationale per entry. | **done** | `sv0c/runtime/lsan-suppressions.txt` documents the `Vec<T>` handle table's intentional never-freed allocation (arena-style by design) — with a directly-observed, honestly-recorded platform limitation: LeakSanitizer isn't supported on macOS at all, so the suppression file is inert-but-present on this dev machine and load-bearing only on NEX-050c's Linux CI job; the box pool needs no entry since it's a static array, never `malloc`'d. `scripts/native_exe_sanitizer_matrix.py` runs 2 safe + 3 UB fixtures at both `-O0` and `-O2` (an extra `-O2` in `extra_cc_args` overrides the dev profile's fixed `-O0` — confirmed both GCC/Clang honor the last-specified `-O` flag, not assumed): all 2×2 safe-fixture cases clean, all 3×2 UB-fixture cases correctly report a UBSan runtime error. Wired into `sv0 test-guards`. Verified load-bearing by mutation: emptying the sanitize flags is caught across all 6 UB-fixture cases at once. |
| NEX-050c | Wire a new CI job running 050a across the full behavior corpus on the Ubuntu runner (first genuinely new CI job in this plan). | **done** | `scripts/native_exe_sanitizer_corpus.py` — every one of the 114 real behavior-corpus fixtures built+run under `-fsanitize=address,undefined`, asserting both the expected exit code AND zero sanitizer stderr, with a narrow `KNOWN_SANITIZER_FINDINGS` substring allow-list (one entry: `overflow_wrap_mask.sv0`, the pre-existing fixture that deliberately exercises Site 4's overflow-wraps assumption — a known, already-surfaced gap, not a new bug). Real full sweep run locally: **114 programs, 1 known/expected finding, 0 unexpected findings**. Pragmatic deviation from "new CI job" noted honestly: landed as a new *step* inside the existing `verify` job in `.github/workflows/ci.yml` (placed after `./scripts/sv0 test`, which already guarantees `build/sv0-megatu-native` exists) rather than a wholly separate job re-paying the SML/NJ bootstrap cost — substance (a real, wired sanitizer CI check) is unchanged. 3-case `--selftest` (2 ordinary rows + the one known-finding row, hardcoded by name rather than derived from the allow-list itself — the first mutation attempt derived the selftest's sample from `KNOWN_SANITIZER_FINDINGS`, so emptying that dict silently shrank the sample instead of being caught; fixed and reverified). Wired into `sv0 test-guards`. Verified load-bearing by mutation (after the fix): emptying the allow-list is caught. |

#### NEX-051 — TOOL-012, PORT-005: enable `--profile=release`

| ID | slice | status | red test / acceptance |
|----|-------|--------|-----------|
| NEX-051a | `native_exe_argv_builder.build_release_profile_argv`: `-O2` + whatever 048a's audit requires (e.g. `-fno-strict-aliasing`) — explicitly not `-O3`/LTO/`-ffast-math`/`NDEBUG`-contract-stripping. | **done** | New `build_release_profile_argv` alongside `build_dev_profile_argv`: `-O2 -fno-strict-aliasing` (the audit's own concluded mitigation for Sites 1/2), same dialect/ordering/`extra_cc_args` seam. 5-case `--selftest` (extends the existing argv-builder module): argv has `-O2`+`-fno-strict-aliasing`, none of §16.5's prohibited flags (`-O3`, LTO, `-ffast-math`, `NDEBUG`) appear, never `-std=c99`, honors `extra_cc_args`. Wired into `sv0 test-guards` (existing NEX-023 guard, relabeled). Verified load-bearing by mutation: a version with `-O3` and no aliasing flag is caught by all 3 relevant checks at once. |
| NEX-051b | Behavioral-parity gate: full behavior corpus through both dev and release argv, byte-identical observable stdout/stderr/exit. | **done** | `scripts/native_exe_release_parity.py` — builds every fixture twice (dev argv, release argv-equivalent flags), diffing `(exit, stdout, stderr)`; `overflow_wrap_mask.sv0` (the one fixture already known, per the audit's Site 4, to rely on real signed-overflow UB) is the sole allowed divergence — asserting parity on it would bake in one optimization level's UB-manifestation as guaranteed behavior. Real full sweep run locally: **114 programs, exactly 1 known-divergent, 0 unexpected divergences**. Since the real corpus never actually diverges, the comparison itself (`observations_diverge`) was extracted as a pure function and mutation-tested directly with synthetic tuples (an end-to-end-only test could never have proven this load-bearing). Wired into `sv0 test-guards`. Verified load-bearing by mutation: a version that never reports a divergence is caught by all 3 synthetic-divergence unit cases. |
| NEX-051c | Wire `--profile=release` into `build_native_executable` (`profile: str = "dev"` parameter) — gated on 048a/050/051b all green; deliberately last in this group. | **done** | New `profile: str = "dev"` parameter, validated at phase 0 (before entry validation, matching NEX-028 case5's "usage errors first" precedent — a real, load-bearing gap: the first version validated it later in the pipeline, deep after the core compiler ran; the mutation test for phase-ordering then crashed with an unhandled exception rather than a clean assertion failure because the nonexistent-path input it used to prove ordering hit an unrelated pre-existing bug — `validate_entry_exists` has no clean handling for a nonexistent file at all — flagged as its own follow-up task rather than fixed inline; the correct implementation never reaches that code at all, since profile validation now intercepts first). `"dev"` (default) is byte-identical to before; any other value is `BuildError(USAGE)`, never a silent fallback (§11.4). 2 new selftest cases (6/7 — total 7): a real `profile="release"` build+run succeeds and reports `profile=release`; an unknown profile is rejected before any real work runs. Wired into `sv0 test-guards`. Verified load-bearing by two mutations: dropping the validation entirely is caught by case 7; moving it back after entry validation is caught (the ordering proof). |
| NEX-051d | Extend CI to the declared host matrix in dev *and* release profile (actually applies the macOS/Linux-Clang legs NEX-037 documented but didn't apply). | todo | PORT-005 (every supported host tuple passes both profiles); transcript recorded before landing. |

#### NEX-052 — ART-013, REL-002: same-output lock/publication protocol

| ID | slice | status | red test / acceptance |
|----|-------|--------|-----------|
| NEX-052a | `scripts/native_exe_output_lock.py`: ownership/token lock adjacent to the exact normalized output path (not basename-derived), with stale-lock recovery (dead-PID reclaim), per §22.5. | todo | Two racing processes: one wins, one waits/fails cleanly; a stale lock is reclaimed; a live lock is respected. |
| NEX-052b | Wire 052a into `native_exe_publish.publish_atomically` so same-output builds serialize only at publication (§22.1), reusing NEX-035's fingerprint-verification approach for the stress test. | todo | AC-021 stress (N concurrent same-output builds, distinct fingerprints: published artifact is always fully one build's, never mixed). |

#### NEX-053 — REPRO-004: pinned double-build reproducibility

| ID | slice | status | red test / acceptance |
|----|-------|--------|-----------|
| NEX-053a | `scripts/native_exe_repro_harness.py`: build the same fixture twice with pinned compiler/runtime/flags/environment (§21.3's narrow definition), normalizing known nondeterminism (scratch path, embedded timestamps/build-IDs) before comparing. | todo | Two builds in two different scratch dirs produce byte-identical artifacts; a varied scratch path alone doesn't change the hash. |
| NEX-053b | Extend `native_exe_build_record.py` with a `reproducibility` field (byte- vs. semantically-reproducible, per §21.3's fallback); CI job running 053a and recording the classification. | todo | AC-025; report distinguishes byte- from semantic-reproducibility honestly. |

#### NEX-054 — ENTRY-010, RT-009: stable entry/runtime ABI manifest

| ID | slice | status | red test / acceptance |
|----|-------|--------|-----------|
| NEX-054a | `scripts/native_exe_entry_abi.py`: a new `ENTRY_ABI_VERSION` (parallel to, not reusing, the runtime bundle's `ABI_VERSION`) covering the reserved-symbol set + `sv0_user_main`/hosted-adapter shape, snapshotted in `sv0c/runtime/entry-abi-manifest.json`. | todo | Current reserved-symbol list + adapter shape hash-match the snapshot; mutation — changing either without a version bump is caught. |
| NEX-054b | Install-mismatch test (reusing NEX-046's installed-layout machinery): an installed layout declaring an unsupported entry-ABI version fails closed with a clear diagnostic. | todo | ABI-manifest snapshot + install-mismatch rejection tests. |
| NEX-054c | Publish `sv0c/doc/entry-abi.md`: reserved-symbol list, adapter contract, versioning/compatibility policy (folds into NEX-057, not duplicated there). | todo | ENTRY-010's "documented" half satisfied directly. |

#### NEX-055 — PERF-005…007: regression budget + concurrent perf certification

| ID | slice | status | red test / acceptance |
|----|-------|--------|-----------|
| NEX-055a | Persist NEX-047's benchmark report as a versioned baseline (`sv0c/doc/perf-baseline.json`) + `scripts/native_exe_perf_regression.py` comparing a fresh run against it, failing on >20% p95 regression without a waiver-file entry. | todo | Synthetic 25% regression fails; 10% passes; waivered 25% passes with the waiver recorded. |
| NEX-055b | `scripts/native_exe_concurrent_perf.py`: 2- and 4-process concurrent distinct-output builds (reusing NEX-035's harness shape), timed, asserting real wall-clock parallelism. | todo | PERF-006 (total time well under N × single-build time); if `/tmp/.sv0_drv_path` serializes it, documents the gap honestly against NEX-055c rather than claiming the gate is met. |
| NEX-055c | `scripts/native_exe_core_compiler_reentrant.py` — retires `CoreCompilerClient`'s `flock`+`/tmp/.sv0_drv_path` shim via a genuinely new sv0c-side reentrant entry point. **Highest-risk item in this band** — needs its own dedicated `EnterPlanMode` investigation pass when reached, the same way NEX-016/017 did for comparably-sized sv0c changes. | todo | REL-004 (static guard: the global control file no longer appears in the stable executable path) + concurrency test. |

#### NEX-056 — TEST-009: signed release-candidate evidence workflow

| ID | slice | status | red test / acceptance |
|----|-------|--------|-----------|
| NEX-056a | `scripts/native_exe_release_evidence.py`: assembles one evidence bundle (revision SHA, clean-tree check, build record, benchmark report, reproducibility classification) into one JSON document. | todo | A dirty tree is rejected; a clean tree with all four inputs produces a well-formed bundle. |
| NEX-056b | Document the actual signing step (`git tag -s` / an external signing process) as a checklist-driven workflow doc — policy, not a fabricated-signature script. | todo | Workflow/policy inspection against the retained-evidence list. |

#### NEX-057 — DOC-005: publish stable docs

| ID | slice | status | red test / acceptance |
|----|-------|--------|-----------|
| NEX-057 | Extend `sv0c/doc/native-executable.md` (or split if length demands) to cover the runtime ABI (links 054c), the build-record schema (Appendix C, already in NEX-042), profiles (051's argv shapes), and the compatibility policy (GOV-009's versioned-change-control statement). | todo | Every claim checked against a real script/test already landed in this plan — no aspirational claims, matching NEX-038's discipline. |

#### NEX-058 — GOV-008: remove/migrate duplicated manual C-link recipes

| ID | slice | status | red test / acceptance |
|----|-------|--------|-----------|
| NEX-058 | Migrate `pc3b6-native-project-acceptance.sh`, `sv0-megatu-native-parity.sh`, `sv0-megatu-corpus-parity.sh` off hand-rolled `cc -std=c99 -O0 -w ...` onto the canonical driver; add a static duplication guard (`test-guards` check for `cc -std=`/`gcc -std=`/`clang -std=` outside `native_exe_argv_builder.py`). | todo | Static duplication guard + full differential-parity pass (each script's pass/fail behavior unchanged, before/after output diffed) before deletion. |

## dependency notes (spec §30.2, condensed)

Slices within a band are already dependency-ordered above; across bands, the
load-bearing chain is: CLI/state-machine (NEX-001…003) → fake harnesses
(NEX-004…005) → publication + scratch abstractions (NEX-007…008) → argv
subprocess + VM rejection (NEX-009…010) → reentrant core-compiler interface
(NEX-011, retires the global control file) → entry ABI (NEX-013…018) → one
real single-file link (first real-`cc` slice inside NEX-019…023) → runtime
feature/behavior-corpus migration (NEX-028…029) → project mode (NEX-030) →
contract-mode matrix (NEX-031) → hostile paths/symlinks/cancellation/parallel
builds (NEX-033…036) → platform matrix + installed-layout (NEX-037, NEX-046)
→ retained-C/JSON/config/provenance (NEX-039…047) → UB/warnings/release
profile (NEX-048…051) → same-output locking + reproducibility (NEX-052…053)
→ stable ABI + release evidence + doc publication + recipe de-duplication
(NEX-054…058).

## related

- `task/sv0c-runtime-executable.Rmd` — hub: scope, release ladder, risks, open decisions
- `~/Documents/project-specs/sv0c-runtime-executable/SPEC.md` — governing spec (§31 is this checklist's source of truth; §27 AC-001…025 are the acceptance IDs cited above)
- `task/sv0-toolchain-milestone-4-checklist.Rmd` — checklist-convention precedent
- `task/sv0-vsc-extension-checklist.Rmd` — structural precedent for a spec-governed `<ID>-###` slice checklist
