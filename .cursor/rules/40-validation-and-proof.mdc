---
description: Validation discipline, evidence requirements, and truthfulness about what was actually checked
globs:
  - "**/*"
alwaysApply: true
---

Validation statements must be precise. **This is significantly harder than it looks** — validation gaps compound; see **`42-transliteration-complexity-invariant.mdc`**.

## Hard requirements

- never claim a command was run unless it was actually run
- never claim tests passed unless they were actually run and passed
- never imply validation coverage that did not happen
- if validation was not run, say so explicitly

## Reporting rules

When reporting validation, name the exact command or class of check used.

Examples of acceptable reporting style:

- “ran targeted compiler check”
- “ran the relevant pytest target”
- “updated docs only; no code validation run”
- “did not run tests in this edit”

Do not use vague statements like “verified” or “confirmed” without saying how.

## Validation strategy

Prefer the narrowest relevant validation first, then broaden only if needed.

Possible categories include:

- targeted unit tests
- submodule-local compile/check targets
- integration tests
- root-level orchestration checks
- documentation/path consistency checks

## Completeness expectations

For a behavior change, validate behavior.
For a command or interface change, validate the affected callers and docs.
For a documentation change, validate that commands, paths, and references remain consistent.

## Routine GitHub ship loop (integration work)

Treat **push + green CI** as part of the default closure for work that is meant to land on **`origin`** (not optional polish after the fact):

1. **Local validation first** — run the narrowest commands appropriate to the change class (see **`.cursor/commands/continue-development.md`** **Fast validation loop**), then broaden before push when lists, goldens, **`sml/`**, or cross-submodule boundaries moved.
2. **Push order** — when **submodule** commits exist, **push the submodule repository first**, then the **meta-repo** (gitlink + README **sv0c** table if applicable). See **`29-submodule-git-visibility.mdc`**.
3. **CI verification** — immediately after each push that can trigger workflows, follow **## CI verification after push** below. Repeat until the latest run for that push is **success** (or document a **blocker** in the progress run log if you cannot reach green).
4. **Session reporting** — do not describe an integration session as finished if you pushed code or tests and have **not** yet confirmed CI for that push (naming the run id), unless no workflow ran and you state that explicitly.

**Doc-only or rule-only** pushes that do not trigger Actions may skip CI verification; say so in the report.

## CI verification after push

After pushing to the remote (meta-repo or submodule), verify that GitHub Actions CI passes:

1. Run `gh run list --limit 3` to find the triggered workflow run (use `--workflow <name>` if the repo has multiple workflows and the default list is noisy).
2. If the run is still in progress, use `gh run watch <id>` or poll with `gh run view <id>` until it completes.
3. If the run **fails**, inspect with `gh run view <id> --log-failed`, diagnose the failure, fix it, and push again.
4. Do **not** declare a session complete while CI is red on the latest push.
5. When reporting, name the run id and its final status (e.g. "CI run 24430687052: success").

This invariant applies to every push that includes code or test changes. Documentation-only pushes may skip CI verification if no workflow is triggered.

## Honesty over appearance

If some validation could not be run, say that plainly.
If a change is based on static reasoning only, say that plainly.
Do not substitute confidence for evidence.
