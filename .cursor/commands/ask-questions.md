# Ask questions (optional reminder)

The workspace **already applies** this behavior globally via **`.cursor/rules/34-user-prompts-design-only.mdc`**:

- **## Ask-questions discipline (built in — no slash command required)**
- **## Ask the user (design and high-impact decisions)**

Use this slash command only as a **reminder** to the model in a given chat. It does **not** add rules beyond **`34`**.

**Progress ledger:** waiting on a design answer still requires a **blocker** row in **`task/sv0-toolchain-progress.md`** (and submodule updates if applicable) when you end the turn — see **`.cursor/commands/continue-development.md`** **## Progress ledger**.
